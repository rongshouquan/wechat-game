# 美术+战斗演出线 · 换号续接单（2026-07-15 · 取代 0712 旧单）

> **给接手会话的开场令**：你接手的是《我的星舰小队》**美术+战斗演出线**（07-13 Ron 体制令：美术线并入本线，出图/演出代码/组装一条龙一人管；Ron 只在大方向出手）。本单=现场快照+闸口，**不是真源**——按第 0 步把真源读全再动手。

## 第 0 步 · 真源核读闸（没交《真源核读表》＝不拟单不讲发现不动手）

按 CLAUDE.md ⚑路由表交表（文件｜章节｜一句原文要点·至少含 1 个具体数字）。命中范围：

1. `README-流程与进度.md` 活线头②全段+②现行口径+②资产态（活线头①=工程线的，只看不动）。
2. `GDD-附录D-战斗视觉总谱.md` **§2a（签名四要素）/§3（五件套·⚠飘字冲突挂起）/§4+§4a（背景动效层+高空构图定案）/§5（镜头语法）/§7（性能红线）**。
3. `美术线-总纲.md` **§5（滤镜 v5 现役挂载=转正记录）**+§1⑧（开工令引用制）+§8（L 批编号账·下一批=L79）。
4. `美术线-提示词规范.md` **§3（挂载参数）+§5 检查单（12 项·底线件禁区）**。
5. `GDD-附录D-星舰真源.md` §0 视觉档案字段定义+锋矢/铁壁/烈阳/晨曦/锁链五节（开火签名含落点行为）。
6. 代码真源（读代码本体，别只信本单）：
   - `game/assets/scripts/core/s7/fx/S7FxPlayModel.ts`（纯逻辑播放模型·参考画布 464×825·锚点表/切件表/尺寸阶/星流曲线/镜头/收尾全在此）
   - `game/assets/scripts/core/s7/fx/S7FxScript.ts`（指令流·layoutOverride=备战即战场）
   - `game/assets/scripts/ui/view/fx/S7BattleFxLayer.ts`（Cocos 渲染组件）
   - `game/assets/scripts/ui/view/S7DemoController.ts` 的 startPlayback/attachVScroll/unitName 三处接线区
   - `game/tools/fx-preview/preview2.html`（真模型驱动预览=自验眼睛）

## 一、现场态（2026-07-15 交接时刻）

- **主线进度**：战斗画面从零到"真皮肤+签名弹道+伤害数字+高空对打+磨精五件（星流情绪曲线/Boss推镜/末杀顿帧/治疗回涨/胜负收尾演出）"全套已进正式 Cocos 工程并 gate 全绿。对标自评：静帧 ~7 成、整体 ~6 成（差距=角色演技帧动画+UI 完成度）。
- **刚交付未复验**：Ron 真机三反馈修账已提交（commit cfa35737）——①备战即战场（layoutOverride）+Boss 断崖判定+无皮舰矢量兜底 ②上阵/九宫格显示舰名 ③四页拖动滚动（船坞/训练舱/上阵/选关）。**候 Ron 三复验点**：n043 站位与备战一致敌不挤坨／上阵页名字+滚动／选关页滚到底。
- **候拍栈**：①三复验点（上条）②总谱 §3"不做伤害飘字"（Ron 07-07 拍板⑦）vs 已上的伤害数字——Ron 07-15 口径"**先待定，先做着也可以**"（数字留在代码里，真源 §3 未动，等他一句"转正/撤"）③状态小图标（燃烧/短路等）缺回放数据，需引擎侧透传——要不要排，候 Ron ④其他页面滚动补接（背包/宝库/邮件等，helper 已通用，Ron 报哪页接哪页）。
- **git 态**：干净（截至本单，最后 commit=meta 收编）。**工程线在并行开"战斗批"**（他们自己有续接书《任务单-战斗批续接》，不归本线；README 活线头①是他们的，别动）。仓库根=F:\Claude Code（`樱奈儿海报/`等根目录无关文件夹别碰）。

## 二、已拍板集合（引用原话·别复述走样）

1. 体制：「美术那边我停了…你现在就兼任美术的职责吧…我负责提需求，你负责落地解决，遇到大方向需要拍的就来找我」（07-13）。
2. 对标：「放置军团、荒野乱斗、百炼英雄…看真机游玩的图片/视频。我希望你做出来的画面能够比他们好看，起码不能输他们」（07-13）。
3. 滤镜 v5 转正：L74 同题盲挑 Ron 选「**丙**」=训满 15 轮版（07-14·总纲 §5/规范 §3 已落档）。
4. 高空对打：「实际应该是我们都平行在空中对打才对」（07-14·总谱 §4a 已固化）。
5. Cocos 开工令：「对，开工」（07-14）。磨精开工令：「你现在开始磨精看看效果吧」（07-15）。
6. 飘字：「字体的话我还没想好先待定吧 你现在先做着也可以」（07-15）。
7. 成品优先纪律（仍有效）：省做法「同样的表现效果要用最低成本的方式去实现…成本划得来就行」；「看久不累眼」；2D 手绘锁定；星盗口径。

## 三、硬规格与工程铁律（新会话最容易踩的雷·全部实战验过）

1. **Cocos 手搓节点必须 `node.layer = parent.layer`**——漏了=UI 相机整层剔除（战斗隐身只弹结果·07-14 实案）。
2. **手写 image meta 禁 `"vertices": null`**（导入器崩 rawPosition·07-14 实案）；模板用 `F:\星舰小队本地出图\出图脚本库\l78_meta_gen.py`；Creator 首开会归一化 meta=预期，收编提交即可。
3. 微信端**禁展开 Set/Map**（`[...set]`→空数组）；弹体 48 上限/余烬 90/伤害数字 26（总谱 §7）；禁 shadowBlur。
4. **演出与逻辑分层**：战斗语义只在 S7FxScript（指令流）+S7FxPlayModel（播放态·可 vitest）两层纯 TS；渲染层（Cocos 组件/preview2）只读状态画图。改演出先改模型层→`npx esbuild tools/fx-preview/model-entry.ts --bundle --format=iife --global-name=S7FX --outfile=tools/fx-preview/fx-model.js` 重打包→preview2 自验→再谈 Cocos。
5. **备战即战场**：Demo 传 layoutOverride，玩家摆哪打哪——演出层自带阵型只给工具/预览用。
6. Boss 尺寸=断崖判定（最大血 ≥1.8× 次大）；尺寸阶 我 92/小兵 54/大副 78/船长 150（参考画布 464×825 像素系·Cocos 端 k=viewW/464 等比）。
7. 提交纪律：gate（test+typecheck+validate:configs:s7）退出码；只提交本线文件；工程线并行文件一律不碰不提交。
8. 素材域账本：包体红线 3.8/4.0MB（现战斗美术 31 件仅 0.80MB）；资源接入记 预计/实际体积+授权+meta 状态（交接单范式见 `美术素材/交接单/L78-战斗演出层素材交接单.md`）。

## 四、生产管线快照（换机器不换账·全在 Ron 本机）

- **ComfyUI**：`cd D:\ComfyUI-aki\ComfyUI-aki-v3\ComfyUI && ..\python\python.exe main.py --listen 127.0.0.1 --port 8188 --disable-auto-launch`（交接时在跑；重启机器后照此拉起）。出图参数=规范 §3（20 步/cfg3.2/euler+simple/shift3.1+**滤镜 v5@1.0+触发词 `xjxd style, `**〔LoraLoaderModelOnly·文件已在 ComfyUI loras 库〕）。
- **脚本 python**：`D:/ai-train/venv312/Scripts/python.exe`（bash 里裸 `python` 是坏存根·exit 49）。
- **可复用脚本库**：`F:\星舰小队本地出图\出图脚本库\`（12 件：量产 l75_full_rerender.py／高空板 l77／盲挑 l74／抠底 l75_matting／切件 l75_split_parts／入景烘焙 l76／降尺寸 l78_cocos_pack／meta 生成 l78_meta_gen／头像 l73／拼版×2／snap_server）。跑批模式=改 SOURCES/PLAN 后台跑+收割到 `F:\星舰小队本地出图\L{N}-*`。
- **看图台**：`F:\星舰小队本地出图\看图台.html`（每批必挂·新批在上）；**预览成品**：`F:\星舰小队本地出图\战斗预览\`（index.html=老壳独立实现·preview2.html=真模型驱动**以后用这个**）；快照验证=snap_server.py 起 8932 端口→浏览器开 `http://127.0.0.1:8932/preview2.html`→页内 `restart(); for(...) frame(st+i*16.7)` 合成帧定格→POST /snap 落 `game/tools/fx-preview/snapshot.jpg`（Read 看图）。
- **训练（如需滤镜升级）**：musubi-tuner 三步链模板=`D:\ai-train\configs\train_v5.bat`（⚠必带 `--fp8_base --fp8_scaled --blocks_to_swap 16`，漏了=7 天龟速实案）；教材库 `D:\ai-train\dataset\xjxd_v5\`。

## 五、下一步路线（Ron 已认的方向·顺序执行）

1. **候 Ron 三复验点回话**→按反馈修账（真机上大概率还有一两轮小修）。
2. **铺剩余 15 舰皮肤**（L79 起·滤镜量产）：流程=星舰真源该舰视觉档案→A 路品质词重抽 6-8（模板 l75_full_rerender.py 的 SHIPS 段·底线件全留）→拼版呈 Ron 挑母版→抠底（l75_matting）→烘焙（l76）→锚点实测（发射锚点 MUZZLES+有件的舰配 PART_RIGS）→降尺寸+meta 入 `resources/fx/units/`→SHIP_BODY/S7FX_SHIP_COLOR 补行→gate 提交。**每舰皮肤落地后黄圆兜底自动退场**。
3. **敌三族**（无人舰→废铁→污染·附录D 族语法+敌我两层制）＋**星域背景按域扩库**（总谱 §4 L1 四族基调·高空构图=§4a 口径·l77 模板改词）。
4. 演出增值挂起项：状态图标（候 Ron 排）／对象池上限真机压测（Ron 真机跑 3x 倍速看帧率）。

## 六、现场保护三条（工程线并行·07-13 老规矩）

禁 reset／禁盲 commit（先 `git status` 盘点认领，工程线的数值/剧本/战斗批文件一概不碰）／对方 README 段落只看不改。
