# 《我的星舰小队》流程与进度（总览 · 续接说明）

> **新对话从这里接：** 读本文件 →（改设计读 `系统玩法设计-v1.0.md`，**唯一真源**）→（总路线读 `v1.0落地总排期.md`）→（细节/遗留读 `进度日志-C1改造.md`）→（写代码看 `game/`）。
> **项目路径 `F:\Claude Code\《我的星舰小队》`**（Ron 自有副本，**不是** Codex 的 `F:\Codex\星舰小队\game`）。**走轻流程，不遵守 Codex 的 CLAUDE.md / 重看板。**
> **引擎 Cocos Creator 3.8.x + TS**；核心逻辑纯 TS（不依赖 cc、能 Node/Vitest 测），Cocos 只管显示/输入/资源/生命周期（v1.0 §4.6）。

## 📍 现在在哪（最新 2026-06-21 · 主界面 + C 抽卡 + D 打捞 + E 商人 + J 建筑全套(升级入口/养成/升阶升星) + G2 邮件领取 + F 关卡三选一发奖 + H 宝箱开箱/完整背包 交付，待 Ron 真机验）
- **战略（Ron 拍板）**：**全 v1.0 一次做满再上线**。总路线 = `v1.0落地总排期.md`（5 阶段：①灰盒把全系统接成可玩 → ②内容铺量 → ③数值校准 → ④美术 → ⑤上线工程；合规/美术两条长周期轨并行）。
- **进度**：① 玩法设计 ✅ 冻结 v1.0；② 数值 ✅ B1+轻量B2（精确值待第二块校准）；③ 代码 🔄 **在【阶段一·灰盒把全系统接成可玩】**。
  - 底座(块0~7) + C1b 最小可玩竖切片 + 升级变强闭环 ✅，**已在真实 Cocos+微信端跑通**；前置地基 G0(UI/安全区/像素比) / G1(广告mock) / G2(邮件) ✅；**A 块战前备战界面 ✅**；**B 单舰装配 ✅ 全部交付并 Ron 真机验过**——插件/星核/驾驶员 真装卸+真进战斗+计战力；备战居中九宫格(拖动换位)+「上阵界面」+ 战斗就地演;装配三段(详情弹窗·别船=两船互换·一键卸装/一键装配·顶部实时战力·点卡片外返回)。最后两处(装配顶部战力显示·关闭→返回)Ron 免测、已按"战力随装备实时变"实现。
  - **C 抽卡三池 step1（纯 TS 引擎）✅**：三池(招募/整备/专属)+按天轮换(每天开2类·3天一轮·专属每3天换1艘)+单抽/连抽+阶级地板保底(每N抽必出≥A/3★·已拥有折60)+重复折碎片(15)+专属池兑换进度(阈值/溢出折碎片/×2叠领/轮换忘领走邮件补发·零头清零)；邮件加 `unit`(本体)奖励类型。
  - **星港主界面 hub ✅(灰盒)**：参照基地主界面图——顶部货币栏(星矿/星贝/补给券·锚安全区)+ 7 建筑入口岛 + 3天/7天活动 + 背包/邮件 + **出战→最新关备战(真通)** + **星港补给站→抽卡界面(真通)**；其余入口点了弹"即将开放"/"未解锁"(后续块逐个填)。底部留小 DEV 行(升级旗舰/领离线/重置)。
  - **C 抽卡界面 step2 ✅(灰盒)**：三池切页(左→中→右=招募补给/专属补给/舰船补给·Ron 命名)+当前池说明(今日开放类别/当期专属)+保底进度 + 单抽/十连(扣补给券·**券不足该档变灰·点了不抽**)+出货标阶级/星级/专属(专属恒**A级·专属**·Ron 拍板§10.1) + 专属池兑换进度+领兑换箱(×2) + 赞助补给(看广告 mock 得券)；进/出刷新走轮换补发。
  - **D 信标打捞 ✅(灰盒·引擎+打捞港界面)**：引擎(`core/s7/S7Salvage*`)选档→占打捞队位(=打捞港等级)→选时长2/8/24h→真实时钟异步等→收菜结算(必得软货币+1种通用碎片随机其一+概率发现分层·随时长加掷骰·单次≤1·资源合并)+广告加速(按档减时·每日≤3/高级5·跨天重置)+经济护栏(无完整星核·完整星舰=C阶已有则折碎片15)；打捞港界面=选档+选时长+开打+任务列表(倒计时/收菜/看广告加速/DEV秒成)+收菜入账(资源/碎片/本体/插件/货舱/人口)。
  - **E 商人小站 ✅(灰盒·引擎+界面·Ron 2026-06-20 调整为轮换制)**：引擎(`core/s7/S7Merchant*`)**轮换制上货**(常驻补给券 + 轮换格随商人等级5→8·从池随机铺·刷新换一批·lv门槛如星空宝石lv3)+买(扣星贝/周期购买上限·跨刷新保留防套利)+刷新(免费+广告·**去付费**)+每日自动刷新+回收(信标/溢出星矿→星贝·折损·**随商人等级陆续解锁**:星矿lv2/信标lv4)；界面=刷新行(免费/广告)+货架列表(名/价/剩余+买·买不起变灰)+回收区(按等级解锁)+DEV升级键。**接通券/信标来源(喂抽卡/打捞)。** 顺修 onReset 漏重置 gacha/salvage/merchant 的 bug。
  - **J-step1 建筑升级入口 ✅(灰盒)**：全建筑统一升级弹框(等级/当前→下一级效果/花星矿·工人折扣·满级或买不起灰)；功能建筑(打捞港/商人)界面加升级键、被动建筑(居住舱/研究塔/星核展厅)点 hub 直接弹框；主界面建筑入口显等级 + 可升↑红点。**接通战斗加成**：研究塔(全队血攻+%)·星核展厅(收藏加成·×收集星核种数)经 `extraBlocks` 注入战斗(`playCurrentNode` 传 lineup)。商人免费刷新改"每升一级+1/天"(去自动换货)。补给站(抽卡率)/船坞(星舰等级上限)/训练舱(驾驶员升级)留 **J-step2**(避免升了没效果)。DEV 发 10万星矿便于测升级。
  - **J-step2 船坞/训练舱养成 ✅(灰盒)**：船坞界面(列拥有星舰→升级·花星矿/合金/舰碎片·**封顶=船坞等级×5**)、训练舱界面(列拥有驾驶员→升级·**新增 `upgradePilotOneLevel` 花驾驶记录**·封顶=训练舱等级×5)；升级服务加 `levelCap` 参数(到顶 cap_reached·提示升建筑)；hub 加训练舱入口、船坞接养成(替原 DEV 升旗舰键)。
  - **J-step3 建筑界面补全 ✅(灰盒)**：船坞每艘星舰加「装配」入口(跳已做的单舰装配界面·复用 B 块)；居住舱=人口中枢界面(显居民/工人+各加成)；星核展厅=收藏界面(列已收集星核+全队加成)；修了"升级/装配弹窗被后建浮层盖住"的置顶 bug。
  - **升阶/升星块 ✅(灰盒·建筑最后一块大的)**：星舰阶级 **C→B→A→S→SS(5阶)** / 驾驶员 1-5★；**升阶/升星只扣专属碎片**(C→B=20/B→A=40·A=60)·**通用碎片改背包手动转指定单位专属**(占位1:1·点使用→选单位→−/+/最大→转)；**开槽 gating 接进装配**(插件槽C=1/B=2/A=3·**星核槽S阶开**·doEquip+一键装配都拦)；升阶/升星加成**接进战力**(显示+实际战斗)；船坞/训练舱列表→「管理」面板(详情/升级/升阶升星/装配·**预留属性&技能详情位**)。存档 v15→v16(unitTiers)。同步 v1.0 §5.1/§5.3/§6。**补给站→抽卡出率**仍留第二块(抽卡无概率出率钩子)。
  - **G2 邮件领取地基 ✅(灰盒·`8d887c1`)**：邮件引擎(`S7Mailbox`·8 测)早已就绪，本次补**应用侧**——hub「邮件」入口→邮件界面(计数/未读/可领·一键领取/逐封领取/已领·已过期态)+ **领取→入账**(软货币→钱包·有非钱包键护栏；`exShard:<id>`→专属碎片库；chest→宝箱库；unit 本体→未拥有发本体/已拥有折 15 碎片)。**清掉「邮件领取应用侧未接」这条跨块待办**，C 轮换补发 / G 活动发奖的领奖管道打通。已验：引擎 8 测 + 应用侧逻辑审查 + 零新增类型错。**已加 DEV-TEMP「发测试邮件」键**(hub 底部工具行·发一封含 软货币/专属碎片/宝箱/本体(shp08) 的混合邮件·供 Ron 真机验领取入账·上线前删)。
  - **F 关卡三选一发奖 ✅(灰盒·step1引擎`c0599ad`+step2界面)**：打赢**首通**(`settlement.ok`天然判重·重复挑战不刷)→ 固定软货币(既有 S7NodeSettlement 发) + **按档稀缺池三选一**(普通/精英/Boss·严格照§8构成)。`S7LevelRewardService`(纯函数·16测)抽取 + 控制器发奖浮层(胜利后盖在结果弹窗上·**必须选1个才离开**)。随机指定专属碎片**从全部单位随机**(Ron拍板·界面显具体单位)；Boss 必给(首=过载核心core07/其余=星辉货舱·非三选一)；精英/Boss **看广告×2**(`clear_reward_double`·连固定软货币一起翻倍·唯一核不翻)。入账复用 applyMailReward/applySalvageReward 同款(资源→钱包·exclusiveShard→碎片库·plugin挑pluginId·chest→宝箱库·core→grantCore)。**真机可验**：n001-n005/n007/n008 普通档三选一、n006 精英档+看广告×2(存档已通需「重置存档」重走首通);Boss档(n018)因 n008+ 无遭遇暂不可达·引擎已测。**灰盒留后**：发奖未选时杀进程会丢三选一(首通已推进)——pendingLevelReward 未持久化·须选才离开兜常规·robust 版持久化留后。
  - **H 宝箱开箱 + 完整背包 ✅(灰盒·step1引擎`6edeb86`+step2界面)**：**星辉货舱开箱**(§10.6)——开箱给 3 选项(星核碎片/星空宝石/信标包)·**免费选 1·看广告(`cargo_extra_pick`)再选 1**(箱子在「免费选1」那刻才扣库存·没选不亏)。`S7ChestRewardService`(纯函数·10测)掷量。**完整背包改三页签**:资源(列所有背包资源+各单位专属碎片+拥有插件数)/宝箱(三类宝箱数量·星辉货舱「开箱」·行动/扩张宝藏占位随 G/I 接)/碎片转换(原通用→专属)。**顶部货币栏 3 chip 点击看说明**(§10.6 上下文)。zhRes 补 beacon*/starGem/fullCore。入账复用护栏(资源→钱包·信标包逐档加)。**真机可验**:背包三页签切换·星辉货舱开箱选 1+看广告再选 1(需先有货舱·打 Boss/打捞/邮件得·或没有就看不到「开箱」)·点货币 chip 看说明。
  - **96 套件 / 1057 测全绿(F+16·H+10)、一路零回归；S7 存档 v16（C gacha·D salvage·E merchant·J unitTiers·旧档 normalize 自动迁移·persist 显式列全防漏字段）。** 新代码 `tsc` 零新增类型错(现 5 既有建筑 `resources`→`S7ResourceState` 类型告警，全来自 J 建筑块 `buildBuildingUpgradeView`/`upgradeBuildingWithDiscount`·Babel 构建忽略·与本次无关；另 1 既有测试 `energy` 字段告警)。
- **合规**：纯广告**不需版号**（已确认）；剩 软著/备案/隐私/防沉迷 常规项（Ron 走流程，尽早办）。

## 🔜 下一步（新对话从这里接）
**① 先等 Ron 真机/预览验最近一批**（升阶/升星 + 建筑全套）：船坞→星舰「管理」升阶(C→B→A→S→SS·看槽位变多·S开星核槽·战力涨)+去装配验低阶装不满/升阶后能装更多+核；训练舱→驾驶员「管理」升星；背包→通用碎片转某单位专属(−/+/最大)；居住舱看人口、星核展厅看收藏。**G2 邮件**：hub 底部「发测试邮件」(DEV)→hub→邮件→领取入账(软货币/专属碎片/宝箱/本体·本体 shp08 首领发本体·再发再领折15碎片)。验出问题就改。

**② 先验 F + H(灰盒·新交付)**：
- **F 关卡三选一发奖**：打几关首通看发奖浮层——n001-n005/n007/n008 普通档三选一、n006 精英档+看广告×2；选一个看入账。**存档若已通这些关 → 用 hub 底部「重置存档」重走首通**(重复挑战不发三选一是设计)。Boss 档(过载核心/星辉货舱)因 n008+ 无遭遇暂不可达·引擎已测。
- **H 宝箱+背包**：hub「背包」→三页签(资源/宝箱/碎片转换)切换；有星辉货舱时「开箱」→选 1+看广告再选 1(货舱来源:其余Boss掉/打捞概率/邮件·没有就用「发测试邮件」DEV键发一个含宝箱的邮件领了再开)；点顶部 星矿/星贝/补给券 看说明。

**③ 阶段一剩余功能块**（顺序可调·**每块开工：我出细案[做什么+Cocos UI量+估时+估token]→ Ron 拍板 → 再做**·逻辑纯TS写测试·`npm test`零回归·里程碑commit·干净节点push）：
- **G. 活动接全**【重·🔄step1完成】：3天/7天 进度喂入 + 周期tick + 里程碑/完成/结算发奖 → **走邮件/背包**（§10.5）。**G-step1 ✅**(纯TS·`S7ActivityConfig`阈值/奖励/进度权重占位 + `S7ActivityService`解析helper[listMilestones/completionView/progressWeightFor/activityCycleConfig/settlementReward] + 11测·引擎本就八成完成)。**G-step2 待做**:喂入接线(推关/打捞/抽卡→addActivityProgress)+加载tick(到期结算宝藏走邮件)+活动界面×2(进度条/里程碑领/完成领/倒计时)+hub入口接真界面。邮件地基已就绪(G2)。
- **I. 星核三渠道 + 星空宝库**【中】：扩张宝藏三选一/碎片合成/宝石兑换/宝库限定核 + **专属星舰定向兑换(§9.3唯一来源)**（§5.4/§10.4）。
- **K. 人口数量来源**【轻】：主线救回 + 7天扩张 给居民/工人（§7/§11）。
- **L. 倍速1/2/3x + 入场仪式 + 轻量失败界面(单位输出统计)**【轻】（§4.3/§4.5）。
- **M. 新手引导**【重·收尾集成·阶段一验收标志】：前10关10步强引导 + D1-2满编 + D3首核 + D7首扩张；把 A/B/C/F/I/J/K 串成脚本化流程（§11）。
（已完成的 阶段一块：G0/G1/G2地基 · A备战 · B升阶升星 · C抽卡 · D打捞 · E商人 · F关卡发奖 · H宝箱开箱/背包 · J建筑 · 主界面hub。全清单见 `v1.0落地总排期.md` 阶段一。）

## 🧩 跨块待办 / 已知占位（新对话注意）
- **✅ 邮件领取应用侧已接(G2·`8d887c1`)**：邮件界面 + 「领取→入账(resource/chest/**unit本体**(已拥有折15)/`exShard:<id>`→专属碎片)」应用代码已写。C 轮换补发 / G 活动发奖的领奖管道打通。**DEV-TEMP**：hub 底部工具行「发测试邮件」键(发混合奖励邮件·供真机验领取入账·上线前删)。
- **补给站→抽卡出率**：留第二块（灰盒抽卡是"保底抽数"制·无概率出率钩子）。
- **DEV-TEMP（上线前清·都在 `S7DemoController`）**：开局发货(默认船/员/编队/插件/星核) + 各种资源种子(188券/信标/5万星贝/10万星矿+合金/5千驾驶记录/专属200/通用300) + 打捞「秒成」键 + 底部「领离线/重置存档」。
- **数值全 v0.1 占位** → 阶段三第二块统一校准（抽卡出率/保底抽数·打捞产出表·商人定价/解锁等级·升阶升星成本与战力涨幅·难度墙·经济产出消耗/毕业预算）。
- **各块详细交接** 见下方「📌 …交接要点」分节（C/D/E/J 各有）。

### 📌 C 块交接要点（接手前必看）
- **引擎在 `core/s7/S7GachaService.ts`（纯 TS·可测）**：关键函数 `refreshGachaToDay`(抽前/进界面/加载时必调·检测专属轮换+补发)、`drawGachaMany`(连抽·传 ticketsAvailable·返回 ticketsSpent 供调用方扣券)、`drawGachaOnce`、`claimExchangeBox`(兑换箱·claimAll=×2叠领)、`availableExchangeBoxes`、`currentExclusiveShipId`/`openUnitIds`(展示用)。
- **配置 `core/s7/S7GachaConfig.ts` 全是 v0.1 占位**（Ron 授权 Claude 定·第二块校准）：6派系/6角色分类(按现有12舰/10员手分)、专属舰=shp10/shp11、A级候选=占位、数值探针(保底10抽·专属兑换50抽·折15/60·都刻意调小便于灰盒演示)。第二块再决定是否迁进正式 config-resource。
- **补给券不在引擎扣**：券是钱包货币(`resources.supplyTicket`)，引擎不 import save——调用方先确保并按 `ticketsSpent` 扣券（与 buildSquadLineup 同样的解耦风格）。
- **邮件 `unit` 奖励 + `exShard:<id>` 约定**：轮换补发用邮件带 `{type:'unit',unitKind:'ship',unitId}`(本体) + `{type:'resource',resourceId:'exShard:<id>',amount}`(专属碎片)。**领取应用侧(step2 控制器)要：unit→已拥有则折碎片否则 grantShip；resourceId 带 `exShard:` 前缀→ addExclusiveShards(别进13键钱包)。**
- **graybox 留后**：阶级(C/B/A)真实养成效果(发A级起点)等升阶系统块；正式分类/出率/保底抽数/兑换阈值/顶级长保底=第二块；赞助补给每日上限数值=第二块。

### 📌 星港主界面 / C 抽卡界面 交接要点（接手前必看）
- **全在 `ui/view/S7DemoController.ts`（灰盒色块 Cocos 组件·1 控制器 + 浮层切换）**：首页 = `buildHubHome`(星港 hub)，抽卡浮层 = `buildGachaPanel` + `openGacha/closeGacha/refreshGacha/onGachaDraw/onGachaClaim/onGachaSponsorAd/switchGachaPool`。出战仍走既有 `openPrebattle()`。
- **DEV-TEMP（待删/待真界面替换）**：① 开局发 188 补给券(在 `ensureDemoSquadSeeded`·正式券来源=赞助补给/商人/活动)；② 首页底部小工具行「升级旗舰/领离线/重置」；③ 建筑「基地」面板(`buildBasePanel`)现**孤立不可达**(首页去掉了「基地」按钮·船坞/居住舱等暂占位)——待 J/养成块把船坞/居住舱接真界面时再接回。
- **抽卡灰盒未尽**：轮换补发已生成邮件(`gacha_rotation_makeup`·含 `unit`/`exShard:` 奖励)；**邮件界面 + 领取入账已做(G2·`8d887c1`)** → "忘领→轮换补发→去邮箱领(含 `exShard:`→exclusiveShards / `unit` 本体)"这条尾巴已闭环。专属兑换箱在抽卡界面内**当场领**(不经邮件)也已通。
- **数值/分类全 v0.1 占位**(见上 C 块交接)；安全区已锚(`getS7UsableBand`)。`npm run typecheck` 须先开 Cocos 生成 cc 类型(temp/declarations 已有)；现 5 个既有 `S7ResourceState` 类型告警(全来自 J 建筑升级代码·非本次引入·Babel 构建不受影响)。

### 📌 D 信标打捞 交接要点（接手前必看）
- **引擎 `core/s7/S7Salvage*`(纯 TS·可测)**：`startSalvage`(占队位·扣信标解耦·调用方按 `BEACON_RESOURCE[tier]` 扣 1 张)、`collectSalvage`(到点掷奖·返回 reward manifest)、`rollSalvageRewards`(必得+概率发现·导出供测)、`salvageAdSpeedup`(按档减时·每日上限·跨天重置)、`salvageRemainingMs/isSalvageDone/salvageTeamLimit`。配置 `S7SalvageConfig` 全 v0.1 占位(产出表/概率/减时/上限·第二块校准)。
- **打捞队上限 = 打捞港(`bld_salvage_port`)等级**(`salvageTeamLimit`)；demo 默认 lv1 → **1 队**(同时只能 1 趟·升打捞港加队，但建筑面板现孤立)。任务异步真实时钟(`Date.now`)计时·跨会话/离线照走。
- **收菜入账在控制器 `applySalvageReward`**：资源→钱包；`ship_body`→未拥有 grantShip / 已拥有折该船专属碎片(15·同抽卡重复)；`plugin`→挑 pluginId 入库(品质 fine/superior/legendary)；`chest`→宝箱库存；`population`→居民/工人(**居住舱封顶 §16 暂未接·灰盒留后**)。
- **DEV-TEMP（待删）**：① 开局发信标(普8/稀5/史3·在 `ensureDemoSquadSeeded`)；② 每个进行中任务的「秒成」按钮(真机不可能等 2-24h·仅供灰盒验收菜)。
- **广告**：加速走 `S7AdGateway('salvage_speedup')` mock；赞助补给(抽卡)走 `sponsor_supply` mock。每日上限数值=第二块。

### 📌 E 商人小站 交接要点（接手前必看·轮换制）
- **引擎 `core/s7/S7Merchant*`(纯 TS·可测)**：`refreshMerchantToCycle`(进店/加载时调·跨天重铺+清购买量/计数)、`generateMerchantStock`(常驻补给券+从轮换池随机铺 `merchantStockSlots(lv)` 格·无重复品类)、`buyMerchantOffer`(扣星贝+记购买量·返回 item 由应用侧 grant)、`refreshMerchantShop`(免费/广告刷·各周期上限·**无付费**)、`recycleBeacon`/`recycleStarOre`(→星贝·折损)、`offerRemaining`/`merchantStockSlots`。配置 `S7MerchantConfig` 全 v0.1 占位(定价/每日上限/轮换权重/折损/解锁等级·第二块校准；现有 shop_param 等表留第二块迁)。
- **轮换制(Ron 拍板)**：补给券常驻(主来源·永远可买)；其余货从 `rollPool` 随机铺、**每次刷新换一批**(解决"刷新没变化")；轮换格数随商人小站(`bld_merchant_station`)等级 `merchantStockSlots`(lv1→5…lv7→8)；星空宝石等高端物 `minMerchantLevel` 门槛。demo 默认 lv1。
- **回收随等级陆续解锁(Ron)**：溢出星矿 lv2 / 信标 lv4(`recycle.starOreUnlockLevel/beaconUnlockLevel`)；界面未到等级灰显"lvX解锁"。插件回收挪到背包(留后·`recyclePlugin` 引擎仍在)。
- **防套利**：刷新=全新一茬店，**货 + 购买次数一起刷新清零**(Ron 2026-06-21)；防套利改靠**刷新次数上限**(每天免费1+广告1)兜，不再靠购买次数跨刷新累计；另有回收折损。（注：补给券每日有效上限≈单批限量×(1+刷新次数)，第二块看是否要给券单独的"真·每日硬顶"。）
- **买入入账在控制器 `applyShopItem`**：资源→钱包；插件→`addOwnedPlugin`(挑 pluginId·品质来自商品)。
- **DEV-TEMP（待删）**：开局发 50000 星贝(`ensureDemoSquadSeeded`)；商人界面「DEV:升商人小站」键(免费升级·验回收/格子解锁·正式靠建筑升级)。广告刷新走 `merchant_refresh` mock。
- **⚠️ onReset 修复**：原 reset 漏了 gacha/salvage/merchant→刷新次数/保底/打捞任务残留；现已一并重置(见 `onReset`)。

### 📌 B 块收尾交接要点（接手前必看）
- **装配/编队模型（已稳定）**：`squad.shipLoadouts[shipId] = {pilotId, coreId, pluginInstanceIds}`——驾驶员/星核/插件**都按船记忆**，与编队(formation: 只 {slotRef,shipId})解耦；下场/换格/拖动都跟船。装/卸/交换走 `core/s7/S7ShipLoadout.ts`（equipPilot/Plugin/Core·均"来自别船→两船互换"、空闲→替换）。
- **战力**：`S7PrebattleView.shipPowerOf(...)` 是单舰战力唯一口径（成长+驾驶员+星核+插件·全占位）；**Ron 已拍板星核也计入战力（有意覆盖 v1.0 §6）**——见记忆 `power-core-counts-override-v1`。精确公式留第二块。
- **DEV-TEMP（待删·都在 `S7DemoController`）**：开局发货 seed（默认船/员/编队 + 6 插件实例 + 3 星核·含 core07 过载核心）——待 C/D/F/I 的获取系统接好后删；现"空就补发"，已有存档无需重置即可体验。
- **graybox 占位/留后**：升阶开槽 gating（现默认开满 3 插件+1 核槽）/ 驾驶员升星（战力改按星级）/ 一键升满+统一战力值展示(§6) / 插件精确词条 + 战力精确公式（第二块）/ core01-06 战斗质变（现仅 core07 有效果·内容块）/ 装备图标（现文字·美术阶段）/ 装配界面布局微调。
- **战斗演出**：已改"就地在备战界面演"（站位=备战站位）；过载核心效果在 n001 秒杀关看不出，验 n006/n007。
- ⚠️ **Ron 待办**：① 合规软著/备案尽早办（长周期轨）；② 编辑器确认 Canvas 竖屏设计分辨率；③ §14 节奏矛盾（毕业更早 vs 战力墙更陡）阶段三前二选一。

## 🧭 角色与协作（钉死）
- **Claude 负责**：顶层设计 / 数值体系 / 养成与长周期规划 / 全套工程代码 / 合规风控 / 排错重构。**出方案 → 大白话讲清 → Ron 确认 → 执行**；适合 Codex 的主动提。
- **Ron = 唯一拍板人**，不需懂代码/数值细节，只听大白话方案判断。**分工**：玩法/广告以 Ron 为主（我提建议）；关卡/UI 以 Claude 为主；顶层/数值/养成 Claude 主导草案——全 Ron 拍。数值是 **Claude 出草案、Ron 拍**（不是"等 Ron 给"）。
- **🔴 头号硬规则（凌驾一切）：严格按 `系统玩法设计-v1.0.md` 执行——不擅自发挥/脑补；设计有不清楚/缺失/歧义，先停下问 Ron 再做。** "怎么做"（UI/灰盒实现）可我主导；凡涉玩法/数值/系统行为/获取消耗，以 v1.0 为唯一真源，没定的先问。

## 🛠 每块工作流程
① 方案先行（做什么 + 估时 + 估 token，Ron 确认）；② 复杂/高风险先质疑讲取舍；③ 逻辑纯 TS、可 Node/vitest 测、不依赖 cc；④ 每块改完 `npm test` + `npm run validate:configs:s7` 零回归；⑤ 测试如实重定基（不为绿而绿）+ 完成前深度自检；⑥ 里程碑 commit、干净节点 push；⑦ 新增 `assets/*.ts` 配手写 `.ts.meta`（见 memory「cocos-meta-generation」）；⑧ 适合 Codex 的主动提。

## 🔒 关键已锁定决策（速记）
纯自动战斗 · 竖屏 · 九宫格上阵 5 舰（满编 D1-2）· 技能制（取消能量·三类触发）· 无限射程（打后排靠目标优先级）· 胜=清场/超时 120s 判负 · 四层（星舰打什么·驾驶员怎么打·插件调数值·星核质变）· 插件品质制（精良/优秀/传奇）+ 合成（3 同→1 高阶）· 星贝=回收/商人币 · 星核三渠道 + 随机为主 + 极品才稀缺 + 宝库限定核 · 新手核=过载核心（首 Boss 固定给·普攻变原子炮）· 无体力 · 纯广告首发（预留内购无 SDK）· 本地存档（预留云）· Cocos+TS 逻辑与引擎解耦 · 体验平滑不憋屈 · 数值真源铁律（B1 为准 / v1.0 设计真源 / 旧 Codex 配置自由改）· **全 v1.0 做满再上线** · **建筑升级入口（Ron 2026-06-21 拍板）= 放各建筑界面内统一升级栏（纯被动建筑点了弹小升级框）+ 主界面建筑显示等级 & 可升级红点；不做集中"基地管理"总览页**。

## ⚠️ 关键遗留（完整清单见 `进度日志-C1改造.md`）
- **大量"逻辑已立、消费/发放侧未接"**：插件/星核装配进备战界面(=B块)、抽卡重复折碎片、关卡三选一发奖、活动发奖走邮件、宝箱开箱、商人卖信标、建筑剩余效果（仅"居住舱→离线"已接）、人口数量来源 → 随阶段一各块接入。
- **数值全 v0.1 占位** → 第二块统一校准（信标打捞表 / 插件经济 / 抽卡出率 / 关卡墙逐节点 / 难度缓坡解"断崖" / 升级成本曲线）；毕业预算模拟器 `simulate-s7-progression.mjs` 已失效待重写。
- **原型遭遇**：n001→n007 连续可玩（割草→精英→头目卡墙），n008+ 无遭遇配置（推到那会优雅兜底）；难度数值=探针值待真机校准。
- 新增 `core/s7/*.ts` 共 22 个待 Cocos 打开认 `.ts.meta`；`npm run typecheck` 须先开 Cocos 生成 cc 类型。
- 工程清理（老项目"流程版"死代码·与 S7 零耦合）：留到阶段一换真场景 + 美术前包体清扫（见 `v1.0落地总排期.md`「工程清理」）。

## 📂 文件索引
- `系统玩法设计-v1.0.md` — 【根·唯一真源】完整系统/玩法设计（改动看 §17）。
- `v1.0落地总排期.md` — 【总路线】5 阶段排期 + 现状 + 关键决策/风险。
- `进度日志-C1改造.md` — 逐块详细开发记录 + 完整遗留清单。
- `设计决策记录-v0.1.md` — 早期决策日志（过程留痕；与根文件冲突以根文件为准）。
- `设计可视化-v0.1.md` + `设计可视化.html` — v1.0 设计的 Mermaid 图（直观看系统联动 / 找漏洞）。
- `第二块-数值设计/`：`节奏表-B1`、`轻量B2`、`建筑数值-6b3`、`词条与技能-种子参考`（旧稿摘的示意种子，做词条库/技能表时对照用）。

## Git
仓库根 `F:\Claude Code`（remote `rongshouquan/wechat-game`）；同目录无关内嵌仓库 `untangle`（**勿动**，已 .gitignore）。里程碑 commit、干净节点 push（本地 == origin/main）。
