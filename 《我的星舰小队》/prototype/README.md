# 《我的星舰小队》原型（prototype）

C1 原型的**纯 TS 逻辑核心**（战斗引擎 / 配置 / 养成），与 Cocos 表现层解耦，可在 Node 单独跑、确定性、未来可服务器复算。

## 运行（需 Node ≥ 23.6，直接跑 TS、无需构建/依赖）
- 跑战斗模拟：`npm run sim`
- 跑自动测试：`npm test`

## 进度
- **C1a-1 ✅**：战斗引擎骨架——属性 + 普攻 + 固定阵位目标选择（首发无限射程，默认打最前排）+ 防御减伤 + 胜负 + 确定性 tick。
- **C1a-2 ✅**：效果系统——技能三类触发(被动/条件/CD) + 驾驶员能力(目标选择)·天赋(过热/斩首/维护) + 插件(6 精良修正) + 星核质变(小太阳/超级护罩) + 护盾/破盾。
  - 已验证：换驾驶员/插件/核 → 战斗明显不同；小太阳清群碾压(无核败→有核 3.7s 胜)；破甲提前破盾(6s→4s)。测试 4/4。
- **C1a-3 ✅**：养成(升级/成长)/掉落/离线 + 过载核心(新手核·普攻变“原子炮”大范围) + 9 关切片循环模拟（打→掉落→升级→推进，Boss 首杀给核）。
  - 已验证：整条循环跑通(`node src/slice.ts` 切片清通)；Boss 是真墙(需练级)；过载核心伤害×2(清群 8.1s→5s)。测试 7/7。
  - 调参发现：v0.1 首个 Boss 偏肉(初版需 Lv23/138 离线)，已快速调平到 Lv11/12 离线 —— 精确数值待节奏表 + 真机校准。
- **C1a（纯 TS 逻辑核心）至此基本完成。**
- C1b（待做）：Cocos 表现层（场景/UI/色块/特效）+ 真机试玩。

## 文件
- `src/engine.ts`：战斗引擎核心。
- `src/content.ts`：最小集配置（数值取自 第二块/轻量B2-v0.1）。
- `src/sim.ts`：命令行战斗模拟器。
- `src/engine.test.ts`：引擎自动测试。
