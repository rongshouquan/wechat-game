# 《我的星舰小队》C1 改造 · 逐块详细开发日志（归档）

> 这是 C1「复用 Codex 成熟 Cocos 工程为底座、按 v1.0 改造」的**详细过程记录**（每块做了什么 / 测试 / 自检 / 遗留）。
> 高层进度看板在 `README-流程与进度.md`；本文件只放细节，避免 README 臃肿。设计唯一真源是 `系统玩法设计-v1.0.md`。

## 重大转向（2026-06-18）：复用旧工程为底座
发现 Codex 的 `F:\Codex\星舰小队\game` 是成熟 Cocos 3.8.8 工程（110 提交 / 82 脚本 / 57 vitest / 能 build 微信·真机过 / 灰盒 UI / 全套基建；战斗已≈v1.0：技能制·无能量·清场或超时·护盾破盾）。决定：
- **设计以 v1.0 为准**（冲突听根文件）。
- **复用该工程为技术底座、按 v1.0 改造**（不重做、不照搬成品）。
- **工作方式（选 b）**：把其 `game/` 复制进 `F:\Claude Code\《我的星舰小队》\game`，用本工作区轻流程接着干（丢掉 Codex 重流程；Codex 的 S7 设计文档留作参考）。
- `prototype/`（从零写的 TS 引擎）已被超越，留作 v1.0 逻辑参考。

## C1 改造步骤进度
1. ✅ **复制工程**：拷 `assets/ settings/ tests/ tools/` + `package.json package-lock.json tsconfig.json project.config.json .gitignore`；**未拷** `build/ library/ temp/ node_modules/ .creator/ profiles/ .git/ CLAUDE.md .claude/ project.private.config.json`（可重建/本地/Codex 重流程）。
2. ✅ **跑通**（2026-06-18）：`npm i`(79 包) → `npm test` 57 套件/672 测试全绿 → `validate:configs`/`validate:configs:s7`(43 表) 均绿。
   - 为让纯逻辑测试脱离 Cocos 编辑器在 Node 下跑，改了两处（都不影响 Cocos 编辑器）：① 根 `tsconfig.json` 去掉 `extends "./temp/tsconfig.cocos.json"` 使其自包含；② 新增 `vitest.config.ts`（pin `test.include` + esbuild `tsconfigRaw` + 锁 `pool:'forks'` 规避 Win worker 偶发崩）。
   - ⚠️ 仍需 Ron 手动一次：用 Cocos Creator 3.8.8 打开本工程确认能导入/构建（GUI 步骤无法代跑）。`npm run typecheck` 须先开过 Cocos 生成 `temp/declarations/cc*` 才过（仅缺 cc 类型声明，与逻辑无关）。
3. ✅ **精读 S7 引擎 + config + 亲自读源码复核**，产出 `C1改造路线图-现状对齐v1.0-v0.1.md`（现状↔v1.0 对齐表 + 8 块改造顺序 + 货币映射待拍板）。复核纠正盘点 3 错：敌方实为 3×7（非5×7）、超时配置驱动（非硬编码30s）、引擎仍用能量条（与「取消能量」冲突=最大一块改动）。利好：输入契约已含 pilot/core/plugin 槽位+校验、引擎已有 coreEffectRef 钩子、AoE 动作积木已具备。
4. 🔄 **逐块改造**（顺序已定，从块0起）：

### ✅ 块0 战场对齐（2026-06-18）
敌方 3×7→**5×7**，抽出单一真源 `core/s7/S7BattleGrid.ts`（引擎+配置校验器+.mjs 工具共用，改尺寸只改这一处+工具镜像）；超时 30/40/45→**120s**（§4.3）。易错点：网格尺寸原散落 7 处（引擎 const/出怪正则/越界消息、校验器 slot正则/footprint边界/maxConcurrent 21→35、.mjs 同款），已全部同步。
- ⚠️ 遗留观察：① n001 仍 w1+w2 分批刷怪，与 §8「一次性刷新不分批」冲突，待内容块；② 引擎"纵深=列"而 v1.0 文案"纵深=行"，仅命名差异（目标语义走 frontmost/backmost 不受影响），轴向 C1b 真机布局定。

### ✅ 块1 效果装配层（四层运行时共同地基）
- **块1a**：`core/s7/S7BattleEffectBlock.ts`（四类积木：修正/触发/行为/动作，对应 §4.6）+ `core/s7/S7BattleStatDerivation.ts`（纯函数 `deriveUnit(base, blocks)`：四层积木合并成最终单位）。叠加规则：同 stat flat 先加再乘(1+Σpct)、affix 累加、behavior 后覆盖前、action 按槽覆盖、trigger 收集。13 用例。
- **块1b**：`deriveUnit` 接进引擎——玩家单位输入新增可选 `effectBlocks?`；带积木走 `deriveUnit`、不带走原路径（零行为变化）。集成测试证明 +100% maxHp 翻倍、空积木与基线一致。

### ✅ 块2 能量条→三类触发（拍板：CD 走显式字段 / 开局即放 / 星核钩子平移留块3）
- **块2a-1**：`battle_unit_stat_param` 加 `ultimateCdSec`（大招 CD，17 行占位均 10s，无大招写 0）+ 双校验器认它。引擎尚未消费。
- **块2a-2**：引擎能量→三类触发。删 energy 全套（字段/常量/gainEnergy/受击涨能/日志/finalState.energy）；主循环 9→7 步，新增 `stepTriggers`(CD 开局即放读 ultimateCdSec / battle_start / hp_below；短路晕眩拦触发) + `fireTrigger`(星核钩子平移：首次触发后放一次 coreEffectRef)。实际只 5 个测试受影响。
  - 🔎 自检纠假过：stun 测试原把敌人改带大招却没设 CD→敌人根本不放招、断言空过；补 `ultimateCdSec:0.4` 让敌人真想放再验被晕压住。
- **块2b**：事件/条件型触发——`on_kill`/`on_hit`(可重复)、`ally_down`(一次性)。dealDamage 采集事件+阵亡计数；stepTriggers 评估后清标志(1 tick 延迟)。
  - 🔎 深度自检纠测试设计缺陷：触发标志原误用带 AoE 的 `eff_ult_shield_bubble`+星核钩子首触发放黑洞清场，致"多次触发"假过→改用纯自盾 `eff_state_shield`+`coreEffectRef:none` 隔离。

### ✅ 块3 星核运行时 + 新手核「过载核心」
- **块3a**：积木加 `set` 运算（质变设绝对值）；新增 `eff_atomic_cannon`（原子炮 AoE）；`core/s7/S7CoreEffects.ts` 解析器（coreId→效果积木）；**过载核心**=action(普攻槽→原子炮)+modifier(攻击间隔 set 10s)。集成测试（带对照）：装核→普攻变原子炮、开局即放、AoE 多目标、间隔 10s。
  - ⚠️ 原子炮 AoE 是"本舰射程内最近 16 个"(靠 maxTargets)，非字面"以目标列为中心半场"；精确形状/数值留第二块。
- **块3b**：打通生产装备路径。过载核心注册进 core_config(=**core07**)；lineup 加 `coreId`(适配器透传)；assembler `buildPlayerUnits` 解析 `coreBlocks(coreId)`→effectBlocks 喂引擎。
  - 透明：① 其余 6 核(core01-06)已注册可校验，但尚无战斗质变(coreBlocks 返回空)，内容留后；② 过载核心"Boss首杀掉落"(获取)属奖励/进度系统、未实现，块3 只做"装备即生效"。

### 🔄 块4 插件品质制（4a/4b/4c 战斗侧 + 4d 挪块6）
- **块4a**：`core/s7/S7PluginEffects.ts`——`pluginBlocks(pluginId,slotTag,quality)` 把插件实例→效果积木，品质缩放(精良×1/优秀×1.6/传奇×2.2 占位)、传奇额外加一条 affix；首版按槽位给占位修正(weapon=攻击+%/skill=攻击间隔-%/tactical=护甲+%)。lineup 加 `plugins?`；`buildPlayerUnits` 解析+校验(≤3/未知/同名/同槽/品质 5 类错误)并与 coreBlocks 合并。12 用例。
- **块4b 引擎消费 8 条定向词条**：
  - **4b-1**：`RtUnit` 加 `affixes`；`dealDamage` 加对Boss/对小怪加伤(按 isBoss 路由)、`heal` 加治疗强度。5 用例(含跨分支防假过)。
  - **4b-2**：余下 4 条——破盾值(叠 shieldMult)、暴击(critRate>0 才掷 RNG→零回归短路、命中×(1+critDmg)、日志带 crit)、控制抗性(仅 short_circuit/stun 缩时长)、技能急速(CD/(1+skillHaste))。+5 用例。占位语义精确值留第二块。
- **块4c 配置对齐**：
  - **4c-1**：插件槽名 `energy`→`skill`(技能/CD槽)。
  - **4c-2**：清旧「插件强化 1-15 级」制——删 enhance/growth 的 plugin 行、双校验器去 plugin 强化/成长校验、输入契约去 pluginEnhanceById，如实重定基 4 测试。⚠️ `simulate-s7-progression.mjs` 因此失效（见遗留）。
- **块4d**（已挪入块6d）：合成/回收/背包。

### ✅ 块5 驾驶员运行时（2026-06-18）
`core/s7/S7PilotEffects.ts`(pilotId→行为AI behavior 覆盖 targetingTag + 驾驶天赋占位 modifier，不加原始属性)；lineup 加 `pilotId`、适配器透传、`buildPlayerUnits` 解析+归属校验(unknown_pilot)。代表驾驶员 **pil03 晴岚=后排点杀**(backline_first)，余下留第二块。7 用例(死亡数对照防假过)。自检纠错：集成测试初版因 maxConcurrentOnField 被前排占额致后排不出怪→改设 2。

### 块6a 砍星核强化 + 货币键重构
- **6a-1**：砍星核 5 阶强化（对齐 §5.4「不做重复星核深层养成留 P1」）。enhance 清空、growth 删 core 段(12→8 行)、输入契约去 coreEnhance、双校验器改正向断言。
- **6a-2**：删废弃币 battleLog/pluginMat/coreMat——`S7_RESOURCE_KEYS` 12→9、双校验器/anchor/upgrade/reward_param(35条)/s7_save 测试同步。注：①升级成本不再扣 battleLog；②旧档加载自动丢弃废弃币（迁移=直接重置）。
- **推迟**：新增 starGem/pilotShardUniversal + 信标拆 3 档——推到块6余项/对应内容块（避免空挂、避免逼填毕业预算数值）。

### ✅ 块6b 建筑（整体完成：集对齐/骨架+存档/数值/离线/人口）
- **6b-1 建筑集对齐 v1.0 §7 八栋**(be4eec2)：删 command_tower(→主界面UI按钮)/observatory(取消奇迹)；starport→supply_station(解锁移 D1)；core_gallery 组 miracle→showcase 转条件预留普通建筑；新增 pilot_training_bay(驾驶员训练舱)。6 张配置表+双校验器+s7_config 同步。风控不变量保持(建筑不卡主线/no-ad、requiredLevelCap≤1、corePathRequired 仅 dock)。此层=标签层=合规风控锚点，不写数字。
- **6b-2 运行时骨架 + 存档 v3→v4**(ddc2848)：`core/s7/S7BuildingState.ts`(纯TS、与配置解耦：buildingId→等级 1-10 + 解锁/可升判定/升级) + `S7BuildingUpgradeService.ts`(`upgradeBuilding` 校验已解锁→未满级→成本合法→星矿够→扣矿升级；成本由调用方传入)。存档加 `buildings`、v3→v4(加性迁移)。**顺手修 bug**：`persistS7Save` 落盘漏写 `pluginInventory`→补回+加往返测试。11 例+2 手写 meta。
- **6b-3 数值落地**(19613b1)：`S7BuildingCost.ts`(成本=`round(120×L^1.3×重要度系数)` 纯星矿；核心船坞/训练舱×1.3、重要居住舱/研究塔/补给站×1.1、普通×1.0) + `S7BuildingEffects.ts`(等级→数值：船坞/训练舱 上限=楼级×5、居住舱 36→48h+产率2%/级、打捞队1/2/3、研究塔+1%/级、星核展厅"收藏图鉴"每种0.3→0.6%×种数封顶10%、补给站提A级/3★+0.5%/级占位、商人槽位2→6/刷新1/2/3)。清升级段占位材料标签(纯星矿)。11 例+2 meta。数值文档→v0.2 定稿。
- **6b-4a 离线产出纯函数 + 星域系数**(1f34275)：`S7OfflineProduction.ts`(纯TS·确定可复算 §12.5)：`computeOfflineGains(经过秒数,{habitatLevel,clearedStarfieldTier,...})`→软货币进账；速率=基础×星域系数(随已通关最高星域，主线进度永久乘区)×(1+居住舱产率+额外)；上限=居住舱存储 h、溢出标记。`starfieldCoefficient`(0-4→1.0/1.6/2.6/4.0/6.0)。
- **6b-4b 居民/工人 + 存档 v4→v5**(250f0ce)：`S7Population.ts`：居民 离线产率+1%/人(封顶+40%)·存储+0.25h/人(封顶+12h，守 §16 红线 48+12=60h≈2.5天)；工人 建筑升级-0.5%/人(封顶-30%)+`applyWorkerDiscount`。居民经离线 extra* 注入、工人折扣组合式叠在 S7BuildingCost 上(不破坏解耦)。存档加 `population`、v4→v5。
- **本轮拍板留痕（2026-06-19）**：①数值真源铁律=B1为准/v1.0设计真源/旧Codex配置(如 growth_band 等级驱动战力)自由改或无视；②满编5套压到 D1-2(§11)；③建筑升级纯星矿、成本按重要度3档；④星核展厅改"收藏图鉴"(保留等级、等级放大每种加成，反与研究塔冗余)；⑤星港补给站升级提A级/3★率(温和，并入§10.1)；⑥星核展厅解锁挂首核(约D3)非D7笔误。

### 块6d 插件合成/回收/背包（整体完成）
- **6d-1 实例库存+背包**：`S7PluginInventory.ts`(实例 `{instanceId,pluginId,quality}`+序号计数+add/remove/find)；存档加 `pluginInventory`、v2→v3(加性迁移)。
- **6d-2 合成**：`S7PluginCraftService.ts`——`synthesizePlugins(inv,3个id,configs)`：3 同槽同品质→1 高一阶、槽内随机词条；**本地确定性种子**：随机用 `inv.nextActionSeq` 派生确定性 RNG、递增隐藏防"挑输入凑词条"；消费 3 输入=天然幂等。9 用例。
- **6d-3 回收**：`S7PluginRecycleService.ts`——`recyclePlugin` 插件→星贝(starCargo)入账+出库；回收价=基值[品质]×折损率(占位，第二块按 recycle_param 插件行区间[20,40]定点)。4 用例。⚠️ 配置遗留：`recycle_param.recycle_normal_plugin.currencyGroup` 仍写已删的 `plugin_mat`(回收按 v1.0 出星贝，tag 订正留第二块)。

### ✅ 块6余项 货币扩键 + 两独立结构（2026-06-19，3 commit `9914200`/`c5d1105`/`bf47231`）
当初块6a 推迟"新增币 + 信标拆 3 档"的理由是"避免逼填毕业预算数值"。本轮**先解掉那个根因**再扩键。
- **关键架构动作：解耦"钱包全集"与"免费毕业预算追踪子集"**。`free_resource_anchor_param`(免费毕业预算表) 的校验器原先要求"钱包每多一种币就得在表里填各档预算数字"——这正是当初推迟的原因。改为：校验器用 `RESOURCE_VOCAB`(钱包全集，13 键，管奖励发放) 与 `ANCHOR_BUDGET_KEYS`(8 核心软货币，管毕业预算表) 两份清单分家。**以后扩钱包不再被逼填毕业预算数值**。.ts + .mjs 双校验器同步。
- **步骤1 钱包扩键 + 存档 v5→v6**：`S7_RESOURCE_KEYS` 9→13——+starGem(星空宝石)/pilotShardUniversal(通用驾驶员碎片)；信标 `beacon` 拆 `beaconCommon/Rare/Epic`(撤笼统 beacon)。存档 v5→v6 加性：新键默认 0；旧档笼统 beacon 量并入 beaconCommon(normalize 内幂等、不丢)。
- **步骤2 配置对齐**：`free_resource_anchor_param` 撤 beacon 列(8 行)+ 类型去 beacon 字段；`reward_param` 9 行 beacon 按 v1.0 §9.3 归档(Boss→beaconEpic、7天过程→beaconRare、3天/打捞/复盘→beaconCommon，**数量原样不动**)。Ron 拍板"彻底拆、按 v1.0 归档"。
- **步骤3 两独立结构 + 存档 v6→v7**：
  - `S7ExclusiveShardInventory.ts`：专属碎片"每单位"库存(unitId→数；升阶/升星料，来源=抽卡重复折碎片/关卡三选一随机指定)。get/add/spend，扣 0 删键，防原型污染。
  - `S7ChestInventory.ts`：宝箱×3(星辉货舱 starlightCargo / 行动宝藏 actionTreasure / 扩张宝藏 expansionTreasure) 未开计数。add/open(**只扣计数不发奖**，开箱掉落表=内容侧/第二块)。注意 starlightCargo(星辉货舱) ≠ 货币 starCargo(星贝)。
  - 存档 v6→v7 加性；persist **显式列全字段**(呼应 6b-2 漏 pluginInventory 教训)；2 新 `.ts` 配手写 `.ts.meta`。
- 🔎 **深度自检纠 2 处假过**：①存档隔离测试原用"S7 版本号 ≠ 流程版版本号"当独立性代理，流程版也升到 v7 后该代理失效(7==7)——**没瞎改版本号去躲**，而是改断言到真隔离本质(各用各 storage key)；②专属碎片"防原型污染"测试原把 `__proto__` 写进对象字面量(那是原型设置器、非 own key→Object.entries 看不到→守卫没被触达=假过)，改用 `JSON.parse` 构造真 own 键、并让 `"constructor":99` 整数值使"去掉守卫即变红"。
- 测试：73 套件 / 792 全绿(+2 结构测试文件 +存档往返/迁移)；validate:configs:s7 43 表绿；一路零回归。

### ✅ 块7a 活动进度引擎骨架（2026-06-19，commit 接块6余项之后）
v1.0 §10.5 活动(3天行动/7天扩张)的"肉"(里程碑阈值/目标项/奖励数值/进度怎么喂)大半在第二块/内容侧——先质疑确认后，Ron 拍板"做块7a 引擎骨架(内容数值留第二块)"。
- `S7ActivityProgress.ts`(纯TS·配置驱动·**不发奖**)：两活动 `action3`/`expansion7`(命名对齐 reward_param.sourceType)，每种 `{progress, claimedMilestones[], completionClaimed}`。
  - `addActivityProgress`(喂进度，有限正数)；`claimMilestone(id, threshold)`(进度≥阈值且未领才记入)；`claimCompletion(threshold)`(完成奖记账)。
  - **阈值由调用方按配置传入**(第二块细表填)、**发奖由调用方接**(发星辉货舱→`S7ChestInventory.addChest`、其余进钱包/背包)；本层只记领取状态，与配置/发放解耦。
- 存档 v7→v8 加性；persist 显式列全字段；1 新 `.ts` 配手写 `.ts.meta`。
- **刻意留块7b**：结算宝藏(行动/扩张宝藏) + 周期窗口(开启/到期滚动/邮件补发) + 跨期追赶(§12)——这些带时间。
- 测试：74 套件 / 801 全绿(+1 结构测试文件 +存档往返/迁移)；validate:configs:s7 43 表绿；零回归。

### ✅ 块7b 活动周期/结算/跨期追赶（2026-06-19，接块7a 之后）
Ron 拍板两决策：**周期=连续自动滚动**、**跨期追赶=未攒够即作废**(都选推荐项)。
- `S7ActivityProgress` 扩周期字段：每活动加 `cycleStartTime`(本轮起算 ms，0=未起算·首 tick 以 now 初始化)+`settlementCount`(累计结算次数，供 7 天扩张"第1次自选核/第N次随机"内容判定)。
- 核心 `tickActivityCycles(state, now, config)`(纯函数·确定可复算·now 由边界传入)：
  - 首次以 now 起算、不结算；之后每过完一个窗口：**progress 达完成阈值才产出结算事件**(settlementCount+1)，随后滚入下一轮(起点 +时长、progress/里程碑/完成全重置)。
  - **跨期追赶(§12)**：离开跨多轮——只有"离开当下那轮"可能达标结算，被略过的轮已重置 progress=0→自动作废(守"少玩3-4天明显落后"红线、不补偿)。时钟回退/now非法/阈值非法 均防御(不静默乱发，但时间照常滚)。
- 辅助：`activityCycleEndTime`(UI倒计时)、`settlementChestType`(3天→actionTreasure/7天→expansionTreasure，供调用方 addChest)。周期时长 72h/168h 为定义性常量；完成阈值走配置(第二块)。
- 存档 v8→v9 加性(旧档活动状态补 cycleStartTime/settlementCount=0)。
- **接线留 C1b/第二块**：进度喂入、`tickActivityCycles` 登录时调一次、阈值/奖励数值、结算发奖(发宝箱→邮件§10.6/背包)、开箱内容。
- 🔎 自检：构造"离开10天跨多轮"反例确认只结算应得那轮、其余作废；阈值非法只滚不发。自检无异常。
- 测试：74 套件 / 811 全绿(+10 块7b 用例)；validate:configs:s7 43 表绿；零回归。

### 🔄 C1b 最小可玩竖切片（step1 接线纯TS胶水 ✅ / step2 Cocos壳 待开）
探查结论：流程版整条循环已接好、S7 全"已立未接"；战斗引擎 `S7BattleRunService.run()` 是现成一次性函数；场景/面板/竖屏适配/存档适配器/音效可复用；最大难点在适配层(结果转显示、节点→奖励、AppContext 现绑流程版)。Ron 拍板 C1b 范围=「能跑能转的最小循环」，并把 C1b 拆成"我无头能干的胶水(step1)"+"需 Ron 在 Cocos 跑的壳(step2)"。
- **step1a `S7NodeSettlement.ts`**（纯core·不碰save·8用例对真实样例）：`resolveNodeRewardGrants`(节点→rewardAnchorRef→reward_pool_ref→首个param行→resources取min) + `applyResourceGrants`(只加账本已有键) + `settleS7NodeVictory`(纯函数；**首通才发奖+推进、重复挑战不刷靠 completeS7Node 顺序校验天然实现**)。简化(占位写清)：多ref取首个、量取min、只发软货币。
- **step1b `S7RunSession.ts`**（纯core·不碰save·6用例对真实样例）：`playS7Node`(跑战斗→`summary.winner`判胜→胜则结算，纯函数不改入参) + `S7RunSession`(持有 resources/progress 的轻量会话，`playCurrentNode` 跑+结算+应用；负/结算被拒则状态不变)。复用 `createS7DefaultDryRunLineup`(3舰)。**默认阵容确定性胜 n001→发 starOre90/hullAlloy25→推进 n002**。
- 🔎 **发现原型内容缺口**：样例 `battle_encounter_param` 仅 3 节点(n001/n018/n075，不连续)，推进到无遭遇节点(n002)抛 `missing_encounter`(不吞错透传，已写测试记录)。连续可玩循环需补一串早期节点遭遇配置。
- 🔎 **自检补漏**：step1a/1b 两个新 `.ts` 初次漏配 `.ts.meta`(工作流程⑦)——自检 `for f in *.ts; 缺 meta 报错` 时补齐，并全量核 32 个 core/s7 .ts 均有 meta。
- **step1c 连续遭遇内容**（无头可测）：给 n002-n005 补 normal 群怪遭遇——`battle_encounter_param` +4 行(enc_n002..n005) + `battle_spawn_param` +4 行(spawn_n002..n005_w1，单波一次性刷新§8，复用 bu_enemy_swarm 7 个铺前排)。
  - 关键约束(踩坑记)：遭遇必须与节点 context 对齐——`S7BattleEncounterAssembler.assertEncounterMatchesContext` 校验 stageType/templateRef/problemTagRef/**secondaryPressureTag** 全等节点声明。初版 secondaryPressureTag 全写 'none' 致 n002(节点=low_position)/n005(节点=low_backline_preview) 报 `encounter_context_mismatch`；按节点逐一改对。templateRef 也对齐节点(n004=t02)。
  - 实测 **n001→n005 五连战默认 3 舰确定性全胜**、逐关 +90矿/+25合金(累 450/125)、逐节点推进；n006(精英·无遭遇)为边界抛错(不吞)。
  - 如实重定基 2 测试：s7_run_session "推进 n002 抛错"已过时(n002 现可玩)→改为"n001-n005 连续循环 + n006 边界"；s7_battle_rt_config fixture 计数 遭遇 3→7 / 出怪 6→10。
- **step2 Cocos 色块壳（已真机预览验证）**：Ron 先完成 Cocos-open 硬前置(打开工程→✗0/控制台无红字→16 新 meta 认出/cc 类型生成)。
  - `S7DemoController.ts`(Cocos Component·程序化色块·薄壳)：满屏深色底板(吞触摸不点穿) + 标题 + 状态行(星矿/合金/当前节点/已通关) + 「出战」按钮 + 结果行 + 「重置存档」。点出战→`S7RunSession.playCurrentNode(固定种子)`→胜则发软货币+推进(会话内完成)→刷新+写回 S7 存档域落盘；无遭遇节点 try/catch 显示「暂无关卡」不崩。
  - `MainSceneController.mountS7Demo`：S7 配置预载成功后在 Canvas 顶层建覆盖节点 + 挂 `S7DemoController`，注入 runtime + `pickStorageAdapter()`；错误自包含，不动流程版 AppContext/存档/战斗。配 `.ts.meta`。
  - **预览验证通过**：点出战 5 连胜 n001→n006、星矿 0→450(5×90)/合金→125(5×25)、逐节点推进、已通关 5、n006「暂无关卡」优雅兜底、60FPS。**S7 逻辑首次在真实游戏里端到端跑通**。
  - **🐞 微信端排错（2026-06-19，重要踩坑记）**：首次构建到微信后**真机只出流程版、无演示层**；逐步定位发现 `S7ConfigRuntime` 预加载抛"s7 config validation failed: 51 error(s)"，但本地 `npm test`/`validate:configs:s7` 0 错。排查路径：①确认非网络/非文件夹错位/非缓存(两个 build 包内配置+编译代码均为最新)；②加 DEV-TEMP 探针打印微信运行时实际数据 → ship=12/core=7/plugin=18 全对，但整包校验仍 51 错(全是 plugin fitRefs 误报"shipXX/coreXX 不存在")；③锁定根因 = **Cocos 微信构建把 `new Set([...shipIds, ...coreIds])` 的"展开 Set"降级编成空数组** → 默认实体表为空 → 所有 fitRefs 失败。**修复**：`ConfigValidatorS7` 改用 `Set.forEach` 逐个 add(禁 `[...Set]`)；微信模拟器+真机验证 errs=0、演示层正常。**教训已存记忆 `cocos-no-spread-set-map`**：纯 TS 里禁止展开 Set/Map(数组安全)，本地正常≠微信正常。探针(DEV-TEMP)已清。
  - 边界(留后)：战斗逐帧回放(只显示结果文字)、n006+ 精英/Boss 遭遇、升级变强耦合、其它系统接线、正式美术。
- 测试：76 套件 / 826 全绿(cc 表现层不在 vitest 范围，逻辑零回归)；validate:configs:s7 43 表绿。

### 🔄 C1b 升级变强闭环（2026-06-19，步1-4 纯TS全测 / 步5 demo 接线待微信验证）
Ron 拍板"接升级变强(闭环)"+"先不做更难的关、用战力倍率放大血攻占位映射"。探查确认:growth_band/upgrade_cost 表冻结就位但运行时零消费,等级存档/升级服务/等级→战斗属性/装配吃等级全空白。
- **步1 `S7UnitLevelState`**：星舰/驾驶员两本 id→等级(1-40) 账本(默认空=全1级);存档 v9→v10 加性。
- **步2 `S7UnitGrowth`**：等级→战力(growth_band 段内线性,ship 120→2200)→`ratio=power(L)/power(1)`→按 (ratio-1) pct 同放大 maxHp+attack 的成长积木;pilot 占位返空(§5.2 升级无原始属性)。
- **步3 `S7UnitUpgradeService.upgradeShipOneLevel`**：读 upgrade_cost(段总成本÷10 占位每级)花星矿+合金升1级;校验满级/无成本行/资源不足,失败不改状态。
- **步4 装配器**：`S7BattleLineupUnitInput` 加 shipLevel?/pilotLevel?,`buildPlayerUnits` 织入成长积木;`S7RunSession` 加可选 unitLevels、出战按舰等级建编队。集成测试 n001 同种子 10级 vs 1级 → 血更厚+残血更多+成长积木织入,证"升级后同关更强"。
- **步5 `S7DemoController`**：加「升级旗舰」按钮(调升级服务)+旗舰等级显示;会话带 unitLevels(升级反映到战斗);重置含等级归1。**待 Ron 在微信重建验证**(我无头测不了画面)。
- **占位(第二块校准)**：战力倍率双乘血攻的放大幅度、逐级成本曲线、驾驶员天赋成长。
- 3 新 core/s7 .ts(+S7DemoController 扩) 配 .ts.meta;测试 80 套件/845 全绿;validate:configs:s7 43 表绿;零回归。

### 🔄 难度关卡 阶段①（A+B 打包·两关）（2026-06-19，实现+无头验收完成·待 Ron 真机试玩校准）
Ron 拍板"聪明地多做"=把候选 A(难度关卡)+B(色块回放)打包，内部并成两阶段：**阶段①= n006 精英「吃力能过」+ n007 头目「卡墙→升级破墙」(本段)**；阶段②= B 色块回放(待做)。
- **关键引擎发现**：单位**不会走位**，攻击只看格距 `d=敌列+1+(2-玩家列)+|行差| ≤ 射程`。现有 swarm 射程=1 → 只有敌方最前列且同排的那个够得着 → 这才是 n001-005「再多也无伤」的真因。**敌人要有威胁必须给"射程"**(能从后排打到玩家)，光堆血攻没用。
- **纯数据实现(无新代码逻辑·全改 `assets/resources/configs/s7/*.sample.json`)**：
  - `battle_unit_stat_param` +3 敌人行(unitRef 复用现有 enemyId)：`bu_enemy_swarm_tough`(enemy_swarm·460血/100攻/射程5)、`bu_enemy_burst_raider`(enemy_burst·650血/205攻/射程7·慢)、`bu_enemy_shield_warden`(enemy_shield·5200血/230攻/35甲/射程7/占2x2·n007 头目)。
  - `battle_encounter_param` +enc_n006(elite/t02/swarm/low_burst_preview·奖 reward_elite_basic)、+enc_n007(normal/t03/shield·奖 reward_mainline_basic)。
  - `battle_spawn_param` +spawn_n006_w1(8 强化群怪铺中前场)/w2(2 爆发兵后排·延迟3s)、+spawn_n007_boss(头目1)/adds(2 轻群怪)。
- **数值用探针实跑卡定(非手算·非为绿而绿)**，最终(种子 s7-demo)：**n006** 旗舰lv1 残62%(明显掉血·有来有回)→lv10 残90%(升级更轻松)，始终胜；**n007** lv1 团灭(墙·5 种子稳定败)→lv4 勉强39%→lv8 舒服71%→lv10 81%。**资源预算核**：n001-n006 首通共 540矿/150合金，升旗舰到 lv8≈7级花 154矿/98合金，预算内可破墙。
- **demo 无需改逻辑**(本就处理胜→发奖推进/败→不推进显残血/无遭遇→暂无关卡)；仅做显示改进：败因翻中文 + 败时提示"先升级旗舰再来"，便于试玩看懂。
- **测试**：新增 `tests/s7_difficulty_progression.test.ts`(5 测·断言 n006 吃力能过+升级更轻松、n007 lv1 卡墙+lv8 破墙·与升级/成长系统强绑定)；改 3 旧测试(rt_config 行数 17/7/10→20/9/14;run_session+encounter_assembler 的"暂无遭遇"边界 n006→**n008**)。**81 套件/850 全绿·validate:configs:s7 43 表绿·零回归**。
- **占位(待校准)**：敌人难度数值=首版探针值，**待 Ron 真机试玩定手感**(太easy/太难/劝退→我调血攻)；这是阶段①的"调参尾巴"。可玩段现延到 **n001→n007**，n008+ 仍无遭遇(暂无关卡兜底)。
- **Ron 试玩反馈(2026-06-19)**：n006 残62%/n007 团灭/lv4 破墙——与预测一致；但"爽感现在感受不出来"(因战斗一闪而过→正是阶段②要解决)，且"n001-5 无伤→n006 突变→n007 卡墙"节奏像断崖。**拍板：难度节奏曲线归第二块统一校准(别零敲碎打)，现在不调、继续推进 B**。已记入 README 第二块待补表 + 记忆 `difficulty-curve-tuning-in-block2`。

### ✅ 难度关卡 阶段②B 战斗演出（看得见的战斗过程）（2026-06-19，B1 纯TS全测 + B2 Cocos色块 真机验收通过）
> **术语订正**：自动战斗结果其实瞬间算完，这步是把"已算好的战斗按过程演 ~3 秒"给玩家看，**不是"回放/重看历史战斗"功能**；对内代码名 playback 保留，对外叫"战斗演出/看得见的战斗过程"。
> **Ron 真机验收通过（5 点标准全成立）**：①能看见双方色块对打不闪退 ②看得清谁打谁/谁掉血/谁先死 ③难度差可见(n006挨打仍赢/n007被压制) ④升级变强可见(升完更扛打/清场更快) ⑤放完正常出结果可推进、不卡不崩。速度/美观/数值精修明确不在本步验收(留后/第二块)。
把"一闪而出结果"改成看得见的双方色块对打(回应 Ron"感受不出爽")。回放**不重算战斗**，只播引擎已产出的事件日志。拆两半：
- **B1 `S7BattlePlayback.buildS7BattlePlayback`(纯TS·可测)**：吃 `S7AutoBattleResult` → 逐时刻关键帧(每帧:各单位 位置/血量%/生死/是否登场 + 本帧 攻击/伤害/死亡/登场)。位置取自 finalState.slotRef、血量时间线沿 damage/heal/unit_down 推、按相同 timeSec 聚帧;确定可复现。新增 `tests/s7_battle_playback_s7.test.ts`(7 测:花名册覆盖/时间不倒退/延迟登场时间线/**末帧血量与引擎最终态完全一致**/确实有攻击伤害/确定可复现/残血%口径一致)。
- **B2 `S7DemoController`(Cocos色块·叠加层·待真机验)**：出战后弹出战斗叠加层,双方色块按帧动(敌上我下·前排靠中线·攻击连线·出手描边·掉血血条·死亡变灰),配「跳过」,放完(或跳过)隐藏叠加层再亮结果。总时长压到 ~2.8 秒内(每帧 clamp 0.03-0.10s);回放期间守门禁止再出战/升级/重置。`update(dt)` 驱动帧推进。**我无头测不了画面**,需 Ron 在 Cocos 重建看效果(色块布局/速度/可读性手感)。
- **新增 `core/s7/S7BattlePlayback.ts` 配套手写 `.ts.meta`(uuid 929db9f0…)**,待 Cocos 打开认。esbuild 单文件转译查过语法 OK;全量 82 套件/857 全绿;零回归。
- **占位/待办**：回放速度/色块大小/布局=灰盒默认值待 Ron 真机调;头目"大块"靠 unitStatRef 含 warden/boss 粗判(无尺寸字段,显示用);暂无「重看」(可后加)。

### ✅ C 养成接入：离线产出 + 建筑升级（2026-06-19，C1/C2 纯TS全测 + C3 demo界面 真机验收通过）
> **Ron 真机验收通过**：建筑面板能开能升(花星矿)、升居住舱→离线收益变多(真联动)、离线领取入账、不崩。
Ron 选 C「给循环加养成深度」、"离线+建筑一起接"。先质疑澄清：6 个"已立未接"里只有 **离线产出/建筑升级** 现成可接，活动/宝箱/商人缺第二块数值内容、打捞 S7 侧未建（接=空壳）。Ron 拍板第②点：**只接"居住舱→离线"实联动，其余建筑效果只显示数字、不接玩法**（不动已验收难度）。存档 v10 已含 buildings(v4)/population(v5)/lastOnlineTime → **无需升版本**。
- **C1 `S7OfflineSettlement`(纯TS·可测)**：把存档(buildings/population/进度/lastOnlineTime)+now 喂现成 `computeOfflineGains`，算离线进账(星矿/合金/驾驶记录)；含边界(负/0)、未解锁居住舱=0、超时封顶溢出、居民加成。`applyOfflineGains` 只进钱包已有键。`tests/s7_offline_settlement.test.ts`(7测)。
- **C2 `S7BuildingUpgradeFlow`(纯TS·可测)**：`buildBuildingUpgradeView`(列每栋 等级/折后成本/买不买得起·只读) + `upgradeBuildingWithDiscount`(算工人折扣→调现成 `upgradeBuilding` 校验扣星矿升级)。`tests/s7_building_upgrade_flow.test.ts`(9测)。
- **C3 `S7DemoController`(Cocos·待真机验)**：init 读 buildings/population、**开局解锁 7 栋默认建筑到 lv1**、算上线离线收益；主界面加「基地」按钮(开建筑面板叠加层·7 行点行升级·显示等级/效果/折后成本) + 金色「领取离线收益」(仅有未领时显示·领后落盘刷时间戳防重领)；reset 一并重置建筑/人口/离线。**居住舱→离线 真联动**(升居住舱→离线存储/产率↑→下次离线更多)，其余效果只显示数字(标注"仅居住舱真生效")。
- **新增 `core/s7/S7OfflineSettlement.ts` + `S7BuildingUpgradeFlow.ts` 配套手写 .ts.meta**，待 Cocos 打开认。esbuild 单文件转译查语法 OK；**84 套件/873 全绿；零回归**。
- **占位/待办**：离线/成本/效果数值 v0.1 待第二块；其余建筑效果(船坞→等级上限/研究塔→战斗加成等)接玩法留后；离线要真机验需"玩→关→等约1分钟→重开"才见收益(攒够≥12秒才有正进账)；clearedStarfieldTier 现 demo=0(未通关整星域)。

5. 保持"逻辑纯 TS 可 Node 测"；每块改完 vitest + 提交。

### ✅ 阶段一（全 v1.0 做满）开工：前置地基 G0/G1/G2 + A 块（2026-06-20）
Ron 拍板"全 v1.0 一次做满再上线"，立总路线 `v1.0落地总排期.md`（5 阶段·已深度对抗式自检补强到施工图级）。进入阶段一【灰盒把全系统接成可玩】。
- **G0 UI地基**：`core/s7/S7SafeArea.ts`(安全区纯算法·照搬流程版已验证口径·S7自有不依赖cc/流程版) + `ui/S7UiLayout.ts`(cc薄壳读view/wx)。像素比由 Cocos 引擎按设计分辨率管(Ron 已确认竖屏)。s7_safe_area 测7条。
- **G1 广告mock网关**：`core/s7/S7AdGateway.ts` 5点位 + S7AdAdapter接口 + S7MockAdAdapter + setAdapter(阶段五换真SDK)。s7_ad_gateway 测5条。
- **G2 邮件**：`core/s7/S7Mailbox.ts` 收发/已读/领取(返回奖励·解耦)/过期/计数；存档 v11→v12。被 C/G 依赖。s7_mailbox 测7条。
- **A 战前备战界面（真机验收交付）**：
  - A-step1 `core/s7/S7Squad.ts`(拥有 船/员/核 + 编队 + buildSquadLineup 校验转阵容·驾驶员唯一性自动卸下)；存档 v10→v11。s7_squad 测10条。
  - A-step2a `core/s7/S7PrebattleView.ts`(敌情预览/我方VS推荐战力·如实读v1.0配置·占位战力)。s7_prebattle_view 测6条。
  - A-step2b `S7DemoController`(cc灰盒)：流程 主界面出战→战前备战(敌情预览上方大区/战力/3×3编队选船配驾驶员/选中船详情/选择关卡graybox跳关/返回星港/开始战斗·缺驾驶员拦出战)→胜负弹窗(胜=返回星港·下一关 / 败=返回星港·再次挑战·**弹窗背景保留刚结束的战斗画面·选了才切场景**)→战斗演出。
  - 真机迭代修过：驾驶员唯一性、出战扑空不弹回、底部三键、UI放大、布局重排(敌情上/编队下)、胜负弹窗保留战斗背景。**Ron 真机验收：功能OK·能看清能用(大小接美术再调)。交付。**
- **状态**：89套件/914测全绿·S7存档v12·配置校验过·零回归·全部已push。
- **DEV-TEMP/占位**：demo开局发货(默认拥有/编队·待抽卡发奖删)；选择关卡=graybox跳关(正式主线地图§8后做)；战力=占位(精确第二块)；UI大小=接美术调；建筑除居住舱外效果未接玩法。
### 🔄 B 单舰深装（2026-06-20，逻辑层全测交付 / 界面灰盒待 Ron 真机验）
把战前备战"选中船详情"的 插件×3槽+星核槽 做成真能装/卸、且**真进战斗生效**（接现成战斗装配层）。Ron 拍板"只实现+验证功能，UI 布局后调"。
- **逻辑层（纯TS·全测·里程碑 commit）**：
  - `S7ShipLoadout.ts`(新·配置解耦/注入 `pluginSlotOf` 解析器)：`equip/unequipPlugin` + `equip/unequipCore`，**先校验后改(原子)**。错误码：no_slot/not_owned_plugin/unknown_plugin/slot_type_occupied/dup_plugin/too_many_plugins/not_owned_core。规则按 v1.0 §5.3(3槽武器/技能/战术·同类不堆叠·同名不重复·单实例只装一船=装别船自动卸) + §5.4(一船1核·同名核一场只1个=同 coreId 装别船自动卸)。
  - **校验顺序订正**：dup_plugin(同名) 必须排在 slot_type_occupied(同槽) 前——同 pluginId 必同槽，若同槽先判 dup 永不可达。
  - `S7Squad`：formation 字段 `pluginIds`→`pluginInstanceIds`(语义订正·存的是实例号非 pluginId·该字段一直空→零迁移)；`buildSquadLineup` 加 `inventory?` 参数，把编队插件实例经库存解析成 `{pluginId,quality}` 喂战斗(不给库存=不解析·零回归)；新增 `not_owned_plugin`。
  - `S7RunSession`：透传 `pluginInventory` 给 buildSquadLineup → 出战时插件真生效。
  - **装备生效链**：formation.pluginInstanceIds → buildSquadLineup(经库存)→ lineup.plugins/coreId → 装配器 resolvePluginBlocks/coreBlocks(已存在·块3/4)→ effectBlocks → deriveUnit → 战斗。B 只接 formation→lineup 这段断点。
- **决策留痕**：插件 `fitRefs`/星核 `shipFitRefs` 是配置覆盖校验字段、**不作装备限制**(v1.0 §5.3/§5.4 未限定船型·"自由更换")；插件实例独占(一实例一船)；同名核一场只1个(§5.4)。
- **界面层（cc 灰盒·待真机验·单独 commit）**：备战详情显示真3槽/星核状态 + 「深装」按钮 → 深装叠加层(4槽位行点行=卸 + 拥有插件按钮点=按类型装入·有中文拦截 + 拥有星核按钮点=装)。DEV-TEMP 发货：6插件实例(覆盖三槽+品质)+3星核(含core07过载核心)；改"空就补发"(已有A存档无需重置)；重置就地清空库存复现实例号。
- **测试**：`s7_ship_loadout`(14·含端到端"装传奇武器+过载核心→effectBlocks真带积木"+不装的对照防假过) + `s7_squad`(+4 插件解析)。**90套件/932测全绿·validate:configs:s7 43表绿·零回归**。esbuild 单文件转译查 4 个改动源文件语法 OK。
- **存档**：未升版本(formation 已含 coreId/插件字段·加性兼容)；DEV-TEMP 插件库存/星核为开局发货、非存档结构变更。
- 🔎 **自检**：①端到端测试装上才有 effectBlocks、不装则无(对照防假过)——真触达"真进战斗"路径；②校验顺序反例(同名插件被同槽抢先)已修；③原子性测试(装失败不误卸原船)；④脏档兜底(3件含重复槽再装第4=too_many)；⑤字段改名核 normalize/assignSlot/save/测试全改到。自检无异常。
- **占位/留后**：升阶开槽 gating(默认开满)/驾驶员升星/一键升满+统一战力值(§6)/插件精确词条数值(第二块)/插件·星核获取来源(对应内容块)。

#### B 真机反馈三修（2026-06-20·Ron 验：插件/核都生效，余三点要调）
1. **站位对不上（纯表现层）**：根因=备战 3×3 格子 + 敌情预览的横竖轴跟战斗演出弄反了（备战"左右=列、上下=行"，战斗"左右=行、上下=纵深"）。改 `S7DemoController` 备战格(build/refresh 两处必须同步) + `drawEnemyPreview` 对齐战斗 `computePositions` 朝向：行=横向(左→右)、列=纵深(玩家 c2 前排在上·朝敌；敌方 col0 前排靠下·朝我方)。不动战斗逻辑/slotRef 语义。
2. **装备改"按船记忆"**（数据结构）：原来星核/插件记在 formation 槽上，船一下场槽没了装备就掉——违背 v1.0 §4.4「每舰记忆 loadout」。**改架构**：`S7FormationSlot` 去掉 coreId/pluginInstanceIds；`S7SquadState` 加 `shipLoadouts: Record<shipId,{coreId,pluginInstanceIds}>`（与编队解耦）。`S7ShipLoadout` 装/卸操作改按 shipId、独占跨所有船判定、门槛改 isShipOwned(原 no_slot→not_owned_ship)；`buildSquadLineup` 按 shipId 取装配。**存档迁移**：`normalizeS7Squad` 把旧档 formation 行上的 coreId/插件搬进 shipLoadouts（新结构优先、不覆盖）；load+persist 都过 normalize → Ron 现存档已装的不丢、无需升版本/重置。demo equip/卸/显示/reset 全改按 shipId。
3. **战力计插件+星核**（Ron 拍板·**有意覆盖 v1.0 §6**）：v1.0 原规定星核质变不计战力（只算星舰/插件/驾驶员升星），Ron 选"插件和星核都涨战力"。`S7PrebattleView.buildPrebattleView` 加 `inventory?` 参数，按船装配累加：插件按品质(占位 精良50/优秀80/传奇110) + 星核(占位 +150)。**精确公式留第二块**。
- **测试**：s7_squad 重写(编队槽瘦身 + 旧档迁移 + 新结构优先 + clearSlot 留装配 + buildSquadLineup 按船) / s7_ship_loadout 重写(按 shipId·含"下场再上阵装备仍在"端到端) / s7_prebattle_view +1(装配抬战力) / s7_save 默认 squad 形状 + 往返加 shipLoadouts。**90套件/939测全绿·validate:configs:s7 43表绿·esbuild 4源文件语法OK·零回归**。
- 🔎 自检：①站位改动 build/refresh 两处坐标公式必须一致(否则高亮框与点击格错位)——已同步同一公式；②装备迁移构造旧档反例验证搬迁+新结构优先；③"下场→再上阵 装备仍在"端到端测；④战力补测"装了比不装高"防假过；⑤全仓搜旧字段无残留(formation 行不再带装备)。自检无异常。
- **占位/留后**：升阶开槽 gating(默认开满)/驾驶员升星/一键升满+统一战力值(§6)/插件精确词条+战力精确公式(第二块)/插件·星核获取来源(对应内容块)。

#### B 真机反馈第4修：驾驶员也跟船 + 计战力（2026-06-20·Ron 验完三修后追加）
Ron 验完前三修通过，追加："驾驶员也要跟船走，且有战力加成。"
- **驾驶员入按船装配**：`S7Loadout` 加 `pilotId`（与 core/插件并列）；`S7FormationSlot` 进一步瘦身为 `{slotRef,shipId}`（驾驶员也不在格上）。新增 `getShipPilot` + 内部 `ensureLoadout`/`setShipPilot`(唯一性:一员只驾一船·配到本船时从别船卸下)。`assignSlot` 改为：放船后"本舰已记忆驾驶员则保留(换格/重新上阵不丢)、否则配传入的"；`setSlotPilot` 走 setShipPilot。`clearSlot` 下场仍只清编队位 → 驾驶员跟船保留。`buildSquadLineup` 的 no_pilot/dup_pilot/not_owned_pilot 改读 loadout.pilotId。`normalizeS7Squad` 迁移把旧档 formation 行的 pilotId 也搬进 shipLoadouts。
- **驾驶员计战力**：`buildPrebattleView` 加 `PILOT_POWER=100`(占位)，配了驾驶员的船 +100。无升星系统(留后)故先固定，升星后改按星级缩放(第二块)。注：驾驶员计战力本就**符合 v1.0 §6**(驾驶员升星可量化部分计入)，非覆盖；只有星核计入是覆盖。
- **demo**：新增 `pilotOf(shipId)` 读 shipLoadouts；`firstFreePilot` 改为"未被任意船(含下场船)记忆"判定；编队格/详情驾驶员行改按 shipId 读；reset 清 shipLoadouts 已含驾驶员。
- **测试**：s7_squad 重写(驾驶员入装配·下场再上阵带回·唯一性·迁移含驾驶员·错误码用 shipLoadouts 构造) / s7_prebattle_view +2(装配抬战力含驾驶员·驾驶员单独计战力) / s7_save squad 往返含 pilotId / s7_ship_loadout 脏档字面量补 pilotId。**90套件/939测全绿·validate:configs:s7 43表绿·esbuild 4源文件语法OK·零回归**。
- 🔎 自检：①"下场→重新上阵 驾驶员带回 + assignSlot 传入新驾驶员被忽略"端到端验；②驾驶员唯一性跨全船(含下场船)；③驾驶员单独计战力(同船同级只差驾驶员)防假过；④全仓搜 `.pilotId` 无残留读 formation 槽；⑤直接构造 shipLoadouts 字面量补齐 pilotId 防 typecheck 报。自检无异常。
- **占位/留后**：驾驶员升星(战力改按星级)/升阶开槽 gating/一键升满/精确战力公式(第二块)/各获取来源。

#### B 真机反馈第5修：备战/装配界面改版（2026-06-20·Ron 验完前4修后）
Ron 验完前4修通过，提 4 点界面改版需求：① 备战底部只列星舰、驾驶员挪进装配；② 点船不直接上场、按钮切「上场/下场」(选中在场船=下场/不在场=上场)；③ 上场需先选格子、否则提示；④ 装配分三段(驾驶员/插件按三类/星核·统称"装备")、每装备显示装在哪艘船、点装备先弹详情(取消/装备)、已在别船则二次确认移动、未装则直装。**全程纯表现层 + 已铺好的逻辑层(上一提)**，Ron 拍板"卸下走详情弹窗"(已在本舰的装备→详情右按钮变「卸下」)。
- **逻辑层(上一提已交付)**：S7ShipLoadout.equipPilot/unequipPilot；S7Squad.isShipDeployed/findPilotShip/findCoreShip/findPluginShip/导出 setShipPilot。
- **demo(本提)**：
  - 备战双选模型：`prebattleSelShip`(选中船) + `prebattleSelSlot`(目标格)。底部星舰按钮(存引用·标"已上场"+选中高亮)点=选中；点格=选中格(若格里有船连带选中船)；「上场/下场」单按钮按 isShipDeployed 切文字/行为(上场需选中格否则提示·下场清其格·驾驶员装备跟船保留)。删 onPrebattlePickShip/Pilot/Bench + firstFreePilot。
  - 装配面板重做：标题 + 提示 + **列表容器(每次刷新 removeAllChildren 重建三段)**：驾驶员(全部拥有员)/插件(按 weapon·skill·tactical 子分组)/星核(标 有效果/占位)。每项标记 ★本舰 / ▶在别船 / 未装。新增两弹窗(详情 + 移动确认)。点装备→openEquipDetail：在本舰=模式unequip(按钮"卸下")/在别船=模式move(点装备→移动确认弹窗→equip 唯一性自动从原船卸)/未装=模式equip(直装)。装备通用层 equipShipOf/equipDisplayName/doEquip/doUnequip 按 kind(pilot/plugin/core) 分派。
  - 上场不再自动配驾驶员(驾驶员统一进装配配)；开局发的默认编队仍自带驾驶员(seed 经 assignSlot 配)。
- **测试**：逻辑层测试上一提已加(equipPilot/unequipPilot 3例 + 查询 1例)；本提纯表现层(无 vitest)。esbuild 转译 demo 语法 OK；90套件/943测全绿不变·43表绿·零回归。**cc 表现层待 Ron 在 Cocos 真机验**。
- 🔎 自检：①备战格 build/refresh 坐标公式仍一致(沿用第4修)；②死代码清(selectedShipId/firstFreePilot/onLoadout* 旧handler/loadoutSlotLabels 字段 全删)；③装/卸/移动三模式分派核对(在本舰→卸/别船→确认移动/未装→直装)；④弹窗 z 序(详情/确认在装配面板之后 addChild→渲染在上)；⑤list removeAllChildren 重建无悬挂引用。无 vitest 兜底，真机验前我只能保证语法+类型推理+逻辑层测试。
- **占位/留后**：驾驶员升星/升阶开槽 gating/一键升满/精确战力(第二块)/各获取来源/装备图标(现文字占位·美术阶段)。

#### B 真机反馈第6修：插件替换 + 装备排序 + 装配UI放大（2026-06-20）
Ron 验完第5修，提 3 点：① 装配 UI 调大/布局更舒服；② 插件 bug——同槽已有插件时装空闲同槽插件"没反应"(原 equipPlugin 报 slot_type_occupied)；要改成直接替换、别船来的才二次确认；③ 所有装备按"已装备靠前 + 品质/等级越高越前"排序。
- **②插件替换（逻辑层·真因）**：`equipPlugin` 改语义——不再报 slot_type_occupied/dup_plugin/too_many，而是**直接替换**：装某插件时先把该实例从所有船卸下(独占)，再移除目标船上"同类槽位"的占位插件(被替下→空闲)，再装上。因每类槽≤1，插件数永远≤3、同名(同pluginId必同槽)自然不共存。删 equippedSlotInfo + 3 个错误码(S7LoadoutErrorCode 只剩 not_owned_ship/pilot/plugin/unknown_plugin/not_owned_core)。**demo 流程天然契合**：未装的插件=mode equip→直装(替换)；别船的=mode move→二次确认→equip(替换本舰同槽+从原船移走)；本舰的=卸下。星核/驾驶员本就覆盖式(无此问题)。
- **③排序（表现层）**：装配列表每段内排序 = 已装备(任意船)靠前 → 其内 sortKey(插件=品质权重 传奇3/优秀2/精良1·驾驶员/星核暂无等级=0)越大越前。
- **①UI放大（表现层）**：装配标题42/提示24/项 h72字24/段头26/弹窗标题38信息28按钮32、关闭按钮放大。布局首版·待 Ron 真机微调。
- **测试**：s7_ship_loadout 插件段重写(同槽替换/同名替换/三槽+换其一/别船移来替换·均断言"被替的回空闲"+≤3) + 用 findPluginShip 核空闲；driver/core 段不变。**90套件/942测全绿·43表绿·esbuild demo 语法OK·零回归**。排序/尺寸=cc表现层(无vitest)待真机验。
- 🔎 自检：①替换语义反例(同槽换→旧的回空闲·别船移来→原船没了+本舰旧的回空闲·三槽换其一不误删其它槽)；②删 slot_type/dup/too_many 后核 demo zhLoadoutErr 多余键无害(param:string)、装配器(另一层)的 too_many 保留不动；③S7_MAX_PLUGINS_PER_SHIP 现仅作文档常量(未用·保留)；④排序比较器稳定(equipped 优先再 sortKey)。无 vitest 兜底的排序/尺寸只能保证语法+逻辑层测试，画面待 Ron 验。
- **🔜 下一块 待 Ron 复验后拍板**（候选 C 抽卡三池 / 阶段一其余 D–N）。详见 README「🔜下一步」。

#### B 真机反馈第7修：备战界面大改 + 战斗就地演 + 结算选关键 + 点空白关（2026-06-20）
Ron 验完第6修，提一批：①备战去掉底部"拥有星舰"整块、下半只剩居中九宫格(空格显"+")+底部三键；②敌情预览与九宫格上下对称、中间留隙；③开始战斗→无关UI滑出、当前界面就地变战场(九宫格/敌情=实际战斗站位)；④新增「上阵界面」(点格弹出·下半·左选星舰右详情+装配+上下场·底部返回)；⑤所有浮层点空白=返回/取消；外加 装配排序加"本舰最前"级、结算弹窗加「选择关卡」(胜败都加)。Ron 已确认 ③ 的理解。
- **逻辑层**：无新增（复用既有 shipLoadouts/上下场/装配；新增 `shipPowerOf` 导出于 `S7PrebattleView` 作单舰战力单一口径，备战总和 + 上阵列表排序/显示共用）。
- **demo 表现层（纯重排，主要工作量）**：
  - **共用坐标** `fieldPlayerPos/fieldEnemyPos`：行=横向、列=纵深，上下对称中间留隙。备战九宫格、敌情预览、战斗演出 **同一套坐标** → "备战看到的站位 = 战斗站位"。
  - **备战重排**：可交互 UI 收进 `prebattleUiNode`(开战整体隐藏)；九宫格居中、空格画"+"；删底部船列表 + 右侧详情/装配/上下场(搬去上阵界面)。
  - **战斗就地演**：`startPlayback` 隐藏 prebattleUiNode + 显示备战内「跳过」键；`computePositions` 用 field 坐标；`drawFrame` 画在 `prebattleGfx`(满屏底板+中线+双方色块)；删独立 stageNode/stageGfx/旧 skipBtn。`finishPlayback`→结算窗；`dismissBattleScene` 复位 prebattleUiNode。
  - **上阵界面** `buildBoardingPanel`：下半 sheet(上半透出敌情)；左=拥有星舰列表(重建·上阵靠前→战力高靠前)、右=详情+装配+上下场(无选船则隐藏)、底部「返回」。点格 `onSelectPrebattleSlot`→开上阵界面(有船连带选中·空格右侧空白)。
  - **装配排序**改三级：本舰最前 → 已装备 → 战力(品质)高。
  - **结算弹窗**三键：选择关卡(左·→进备战开选关) / 返回星港(中) / 下一关·再次挑战(右)。
  - **点空白关**：上阵/装配/详情/移动确认/选关/基地/结算 各加 全屏捕获 panel根=关 + 卡片吸收节点(点卡片内不关)；备战主面板不算弹窗(保留吞触摸)。
- **测试**：逻辑层 `shipPowerOf` 经既有 prebattle 战力测试覆盖(装备抬战力/驾驶员抬战力·重构后仍绿)；纯表现层(无 vitest)。**90套件/942测全绿·43表绿·esbuild demo 语法OK·零回归**。
- 🔎 自检：①全仓搜已删字段/方法(prebattleDetailLabel/prebattleShipBtns/boardBtnLabel/onSelectShip/stageNode/stageGfx/skipBtn)无残留；②坐标三处共用同一 field 函数(站位必一致)；③点空白关靠"panel根=关 + 卡片吸收 + 按钮各自handler"(沿用既有"仅最顶命中节点收事件"的特性·与现有按钮共存已验)；④战斗就地：开战隐 UI、结算复位 UI(下次进备战正常)；⑤排序三级键序(本舰>已装>战力)。表现层重排无 vitest 兜底，画面/布局/手感待 Ron 真机验。
- **占位/留后**：装备图标(现文字)/UI 布局微调/驾驶员升星·升阶 gating/精确战力(第二块)/各获取来源。
- **🔜 下一块 待 Ron 复验后拍板**（候选 C 抽卡三池 / 阶段一其余 D–N）。

#### B 真机反馈第8修：修"点空白关"冒泡 bug + 装配 UI 调大（2026-06-20）
Ron 验第7修：①"点空白关"要改成"点浮层外才关、浮层内空白不关"，结算弹窗不要点空白关(必须按按钮)；②**装配界面功能全失效——点任意装备直接退回备战**(严重回归)，且 UI 要调大、平均分布别堆顶部。
- **真因（第7修引入的回归）**：Cocos 触摸事件**向上冒泡**到父节点。第7修把面板根挂了 `closeXxx` → 点子节点(装备按钮)冒泡到面板根 → 触发关闭，装配每点必关=废。上一轮加的"卡片吸收节点"也没用：普通 handler 不阻止冒泡。
- **正解 `addModalDismiss`**：**全屏背板(兄弟节点·内容之前 addChild)**接管关闭——点空白命中背板→关；内容(卡片吸收+按钮/列表)在背板之上，其点击只冒泡到 panel 根(**不挂关闭**)、不触达背板(背板非其祖先)。无需 stopPropagation。上阵/详情/移动确认/基地 改用它(点卡片外才关)。
- **满屏浮层(装配/选关)+结算弹窗**：面板根恢复吞触摸 no-op、不点空白关(只用 关闭/按钮)。
- **装配 UI 调大**：项 h 72→90、字 24→26、段头 26→30、advance/段间留白加大、起点上移——往下铺开别堆顶部(首版·待真机微调)。
- **测试**：纯表现层(无 vitest)；90套件/942测全绿不变·esbuild demo OK·零回归。全仓搜无残留"面板根=关闭"。
- 🔎 自检：①冒泡真因定位+背板修法;②满屏/结算改回不误关;③装配回归修复(点装备→详情弹窗);④全仓搜 `panel.on(...=>this.close` 无残留。表现层待 Ron 真机验。
- **🔜 下一块 待 Ron 复验后拍板**（候选 C 抽卡三池 / 阶段一其余 D–N）。

#### B 真机反馈第9修：九宫格拖动换位 + 上阵列表当前格船排首 + 装备改"互换" + 一键卸/装（2026-06-20）
Ron 验第8修，提 4 点：①九宫格星舰可拖动换位(拖到有船=互换/空位=移过去)；②点格进上阵界面时当前格的船排第一；③装备从别船装到本舰改成"两船互换"(原一次性单向移动·本舰旧的回空闲)；④装配界面加 一键卸装(左下)/一键装配(右下·空部位自动填战力最高的空闲装备)。
- **逻辑层(可测)**：
  - `S7Squad.moveOrSwapFormationSlot(from,to)`：拖到空位移动、拖到有船互换(改 slotRef)；驾驶员/装备跟船不受影响。+测试。
  - **装备"互换"**：`S7ShipLoadout.equipPlugin/equipCore/equipPilot` 改——装某装备时先记源船 + 本舰该位原占位，装上后**若该装备来自别船 B 且本舰原有同位装备 → 把本舰原装备回填到 B**(填 B 腾空的同位)；若来自空闲则无交换对象(本舰旧的回空闲·=替换)。equipPilot 不再走 setShipPilot(自带交换逻辑)。+插件/核/员各 互换/替换 测试。
- **demo 表现层**：
  - 九宫格 `onCellTouchEnd`：TOUCH_END 取松手 UI 坐标 → `convertToNodeSpaceAR` 转局部 → `cellAtLocal` 命中格；落在别格=拖动(`moveOrSwapFormationSlot`)、同格/格外=点击(进上阵界面)。
  - 上阵列表排序加"当前格船最前"级(再 上阵→战力)。
  - 移动确认文案：本舰该位有装备→"与XX互换该位装备"，空位→"从XX移到本舰"。
  - 一键卸装(`onLoadoutUnequipAll`)：卸本舰 员/核/全插件。一键装配(`onLoadoutAutoEquip`)：每个**空**部位填该类空闲装备里战力最高的(插件按品质·员/核取第一空闲)。装配底部三键 一键卸装/关闭/一键装配。
- **测试**：s7_squad +1(moveOrSwap) / s7_ship_loadout 重写"别船移来"为"互换"+加 核/员 互换、空闲替换 用例。**90套件/946测全绿·43表绿·esbuild OK·零回归**。一键卸/装、拖动、排序、文案=表现层待真机验。
- 🔎 自检：①互换语义(别船+本舰有占位→互换·本舰原装备回填源船腾空位;空闲来源→替换回空闲)三类各测;②moveOrSwap 移动/互换/起点空不动;③拖动用 convertToNodeSpaceAR(坐标换算我无法真机测·落点对不对待 Ron 验);④一键装配只填空位、不动已装;⑤setShipPilot 仍供 assignSlot(不交换·部署用)。

##### 第9修·补丁：拖动完全无反应的真因 = 漏听 TOUCH_CANCEL（2026-06-20）
Ron 验：其余都好，唯独**拖动完全不行**(拖到空位/有船都不动)。**真因**：Cocos 节点触摸——手指**在节点内松开**才触发 `TOUCH_END`，**拖到节点外松开触发 `TOUCH_CANCEL`**。拖动必然落在"起始格之外"松手→走 CANCEL，而我只听了 END→处理函数从不执行(点击=原地松=END，所以点击进上阵界面正常)。**与坐标无关**。
- 修：九宫格格子 **同时监听 TOUCH_END + TOUCH_CANCEL**，都走 `onCellTouchEnd`。
- 顺带：坐标换算改不依赖 `convertToNodeSpaceAR`(直接 UI 设计坐标→减半屏居中)；`cellAtLocal` 改"就近吸附最近格(容错半径≈一格距)"更宽容；加 **DEV-TEMP** 控制台日志(`[S7拖动]`·验收后删)便于万一坐标仍偏时让 Ron 读数定位。
- 90套件/946测全绿·esbuild OK·零回归。拖动待 Ron 真机复验(这次大概率成；若坐标偏，靠日志再校)。

#### ✅ B 块完成（2026-06-20）：装配顶部实时战力 + 关闭→返回 + 清 DEV-TEMP 日志
Ron 验"可以了"。最后两处小改(Ron 免测·按"实时变"实现)：
- 装配面板**顶部标题带实时战力**：`装配 — shipId　战力 N`，`refreshLoadout` 在每次装/卸/交换/一键后都调 → 战力随装备实时变(经 `shipPowerOf`)。
- 装配面板「关闭」→「**返回**」。
- 删拖动 DEV-TEMP 控制台日志(拖动已验通)。仅剩合理的"开局发货"DEV-TEMP(待 C/D/F/I 获取系统接好删)。
- 90套件/946测全绿·esbuild OK·零回归·已 push。
- **B 单舰装配 至此全部完成并真机验收**（A+B = 战前备战 + 单舰装配 闭环可玩）。**下一块 = C 抽卡三池（Ron 拍板·我先出细案）。** 接手要点见 README「📌 B 块收尾交接要点」。

## 第2.5块 · 包A 新系统灰盒补建（2026-07 起；设计=GDD-v2.0 S10.7-S10.10/S13，依据见评估文档 §8/§9）

### ✅ 块1 回港报告 + 巡逻收益（2026-07-03 完成；逻辑+UI 由上一会话产出、本会话收尾修复+提交）
- **做了什么**（GDD S10.10 + S13.2 #1）：
  - `core/s7/S7ReturnReport.ts`：开场唯一聚合弹窗数据层——离线产出+巡逻战报+已完成打捞 三段聚合；打捞奖励**确定性预掷**（种子=`report_<missionId>_<endTime>`，杀进程重进不换奖）；`claimReturnReportCurrencies` 只翻离线+巡逻软货币（**打捞实物不翻**，S13 #1）；`removeClaimedSalvageMissions` 按 id 精确移除。
  - `core/s7/S7PatrolProduction.ts`：已通关星域自动巡逻——合金30/驾驶记录20/星贝8 每小时（v0.1 占位≈离线同币 25%，对齐"委托+巡逻≤离线~50%"体检护栏）×星域系数；上限=居住舱存储+居民延长（与离线同口径）；星域档 0 → 零产出。
  - `core/s7/S7AdDailyCounter.ts`：广告点位每日次数通用载体（`{dayKey,count}` 跨天自动清零；块3/5 新点位直接复用）。**存档 v18→v19** 加 `adDaily`（加性迁移，旧档补默认不重置）。
  - `S7AdGateway` 点位 5→**6**（+`return_report_double`·每日 2 次）；音效事件 +`return_report`；UI：`S7DemoController` 回港报告弹窗（必领才关/两键并排"一键全领·📺全部翻倍"/**超上限或无软货币时翻倍键不出现**·零红点/广告没看完不消耗次数不关窗/弹窗挂过午夜的竞态退普通领取不吞奖/强引导期间压住·教程完成或跳过时补弹）。旧"领离线"按钮及通路整体移除（离线并入报告）。
- **怎么测的**：新增 3 测试文件（聚合/确定性/纯函数不动任务表/翻倍只作用软货币/脏键护栏/每日计数跨天/巡逻档0与上限）+ 存档 v19 round-trip/迁移 2 条。`npm test` 105 文件 **1126 全绿** + `typecheck` 0 错 + `validate:configs:s7` 绿。
- **🔎 本会话收尾修的问题**（上一会话断在这）：① **persist 漏写 `adDaily`**（手抄字段清单第二次踩坑，自写 round-trip 测试抓住）→ **结构性根除：`persistS7Save` 改为复用 `normalizeS7PlayerState`**，字段清单只活在 normalize 一处，以后漏写=编译错；② 两条旧断言重定基（版本 18→19、点位 5→6，均核实"为什么对"）；③ 清历史类型错 7 个：建筑函数 5 处改传 `playerState.resources`（与 `session.resources` 同对象、类型精确，不破坏 S7RunSession"core 不 import save"边界）+ 战报测试残留 `energy` 字段删除；④ 一处过时文案"领离线攒矿"→"回收攒矿"。**`npm run typecheck` 已零错，建议进每块必跑门禁**（vitest/esbuild 不查类型，漏字段这类坑只有 tsc 拦得住）。
- **DEV-TEMP 变更**：hub 工具行 −「领离线」+「**测回港**」（拨回 8h 重建报告弹窗·上线前删）。
- **遗留/交接**：① ~~dayKey=UTC 日界~~ **已解决（2026-07-03 Ron 拍板凌晨4点）**：全游戏统一日界 `s7DayKey`（北京时间凌晨 4 点重置，`S7AdDailyCounter` 自持），salvageDayKey/merchantDayKey/gachaDayIndex 全部委托它；老档已存 dayKey 在切换当天可能提前几小时重置一次，灰盒无影响；② 弱引导"首次回港"首触留教程收尾阶段（GDD-M 注记）；③ 巡逻/翻倍数值全 v0.1 占位；④ ~~meta 认领~~ **✅ Cocos 已认（2026-07-03，uuid 未改写、零报错）**；⑤ ~~真机验证~~ **✅ Ron 真机验收通过（2026-07-03）**：新档教程中杀进程重进不弹报告、老档回港正常弹窗。**块1 全部关账。**下一块=**块2 每日委托**（细案先过 Ron）。

### ✅ 块2 每日委托（2026-07-03 完成；细案+备战流程修订均 Ron 拍板）
- **设计修订（先于开工落 GDD）**：①"跳过备战"改**折中方案**（Ron 拍板）——每类首打自动进主线同款备战摆阵，之后面板「开打/调阵容」并排、默认一键沿用上次阵容；②**完美护航=运输船满血**（Ron 明确）。均已写入 GDD S10.8。
- **做了什么**（GDD S10.8）：
  - `core/s7/S7DailyCommission.ts`：护航/演习两类——懒结算发放（每类每日+2·跨天按天补发·**积压封顶 3 天量 6 次/居住舱 Lv5→8·Lv10→10**·首次触达只发当日份不追溯·同日重开不续杯防刷）；`watchedTiers` 记"首打必看"（同档之后秒结算）；奖励 护航=合金80+星贝10/演习=驾驶记录60（v0.1 占位·×离线星域系数·完美护航×1.25）；确定性种子=类型+日界+库存。**存档 v19→v20** 加 `commissions`（加性迁移；persist 走 normalize 复用不可能漏字段）。
  - **灰盒敌阵=已通关最高星域的首个战斗节点**（合成进度喂 dry-run 运行壳，旧内容必赢、随星域自动升档、零引擎改动；真实委托敌阵/涂装/靶机变体留数值校准+演出阶段）。
  - **运输船=复用召唤积木**（GDD 原案照做）：配置 +`bu_commission_transport`（**新 targetType `prop`**=场景道具单位·unitRef 恒 none·两套校验器同步支持）+`eff_commission_transport_summon`；护航时旗舰位挂 `battle_start` 触发块开场召唤到我方空格；战后查运输船满血判完美。**集成测试真跑引擎验证了召唤链路**。
  - UI（`S7DemoController`）：hub「委托」入口（出战上方·关5 强引导后解锁）；委托面板两行（库存/开打/调阵容/⚡秒结算）+积压说明+一键速刷+关闭；首打/调阵容→主线同款备战（敌情预览切委托敌阵·出战改打委托）；真打走既有战斗演出与结果窗（右键路由：胜→回委托列表/负→带阵容重试·不扣次数）；秒结算/速刷不播演出、`dispatch_done` 音效入账。委托战斗不推主线/不发三选一/不喂活动。
- **怎么测的**：新增 `s7_daily_commission.test.ts` 16 条（发放/积压/居住舱延长/同日幂等/防刷续杯/消耗/首打标记/奖励缩放+完美加成/种子确定性/节点选择/**引擎真跑召唤链路+确定性**/演习无运输船/完美判定边界）+ 存档 v20 round-trip/迁移 2 条 + 行数守卫重定基（28→29/19→20，块2 两行新增）。`npm test` 106 文件 **1144 全绿** + `typecheck` 0 错 + `validate:configs:s7` 绿（三件套门禁）。
- **自检结论**：①运输船召唤/胜负字面量/建筑id/字段名等假设全部用真跑测试或代码核实（曾抓到 `finalState.players` 字段名想当然）；②防刷关键性质（当日打完重开面板不续杯）补了显式断言；③速刷循环有消耗/失败双出口不会死循环；④重定基三处均核实"为什么对"；⑤"一键速刷解锁=两类当前档已看"是对"全档玩熟"的灰盒简化——记入遗留。
- **DEV-TEMP 变更**：委托面板 +「测隔天」（发放日拨回 1 天验跨天补发/积压·上线前删）。
- **遗留/交接**：① 委托数值全 v0.1 占位（产率/完美加成/敌阵强度"压一截"的真实公式）→ 第三块；② 护航/演习视觉三层区分（航道背景/劫掠涂装/全息靶机）+运输船演出留美术/演出阶段（现共用同一敌阵灰盒）；③ 速刷解锁条件的"全档玩熟"精确定义留数值校准；④ 新增 `S7DailyCommission.ts(.meta)` 待 Ron 用 Cocos 认一次（同块1 流程）；⑤ **真机验证点**：委托首打→备战→打完回列表、秒结算/速刷入账、「测隔天」积压。下一块=**块3 深空回廊**（细案先过 Ron）。

### ✅ 块2 真机验收修复批（2026-07-04·4件收敛为3件·均 Ron 拍板）
> 起因：块1/块2 真机后 Ron 反馈一批问题。原 4 件中**第④项委托结算并键取消**（委托玩法整体重构为"星港悬赏板"·结算流程会整个重做，本批不碰委托代码/附录B B5.1）。全部改动在 `S7DemoController.ts` 就地修（修 bug 不算扩单体）+ 1 个新测试文件。
- **① 升级显示刷新 bug**：船坞/训练舱「管理面板」点升级后 Lv/战力/成本不即时刷新（退出重进才对）、满级无升阶提示。**真因**：`onUpgradeUnit` 把反馈写到**被盖在下面的船坞 result 标签**（玩家看不见）、且升完只 `refreshUnitTrain`（刷底层列表）**没刷玩家正看着的管理面板**。**修**：反馈改写 `unitManageResultLabel` + 升级后调 `refreshUnitManage()`；`refreshUnitManage` 等级行满级时显式显示"已满级·升阶/升星解锁更高等级"（SS/5★ 顶级显"已满级·顶级"）。教程 step5/8 路径已核实先 `openUnitManage` 再 `onUpgradeUnit`（`unitManageId` 对得上·刷对单位）。
- **② n030 首Boss（三小项）**：
  - **②a 陨星弹发放链路（改回档键·非迁移）**：查出病根与假设有出入——**星核展厅根本没挂首Boss**（在开局默认解锁 8 栋名单里·教程结束即解锁）；`S7_DEMO_SEED_CORES` 含 core07 会给空星核存档白发陨星弹**掩盖问题**。Ron 拍板：不做老档补发迁移（上线全新档·补发=生产死代码）→ 改 **DEV-TEMP「回档n029」键**（hub 工具行·`devRollbackToFirstBoss`）：把 `session.progress` 拨到"首Boss(=`firstBossNodeId`=n030)待通关"(清 n030 及之后通关标记·字典序过滤) + 从 `ownedCores` 删 core07 并从各 `shipLoadouts` 卸下 → 反复可用，Ron 真机反复验"打n030→掉陨星弹→飘字/结算显示"。**并把 core07 从 `S7_DEMO_SEED_CORES` 剔除**（陨星弹只从 n030 掉·链路才真）。
  - **②b 结算窗显示大奖名**：`composeResultText` 原只列软货币；补一行"🎁 特殊大奖：{陨星弹/星辉货舱名}"，读 `pendingLevelReward.bossGrand`（captureLevelReward 先于本函数备好）。**对所有 Boss 通用**（n030=陨星弹/n060 等=星辉货舱一并覆盖·核查过 resolveBossGrand 路径 boss 皆走此）。
  - **②c 三阶段测试**：新增 `tests/s7_boss_phase_progression.test.ts`——用**仅内存的测试配置抬敌血**（bu_boss_n030 血 2600→8000·不写磁盘/不动正式数值）让首Boss战打满 → 断言 start/mid/final **按序全触发 + 我方胜**（多种子）；另一用例极高血(10万)+弱队 → **只出 start**（防"阶段永远全触发"假过·证 mid/final 真按血量门槛 gate）。踩坑记：boss 被一击打到 0 血时 `alive=false` 在同 tick 就设、阶段检查(findBoss 要 alive)会跳过——故必须抬血打满才观测得到切换。
- **③ 三选一↔广告翻倍两段式**：精英/Boss 首通改两段——**第一段**三选一面板去掉广告键（纯选1·必选才离开·防杀进程丢奖不变）；**第二段**选完回结算窗，浮现「📺看广告·必得软货币×2」(#3)单独一行居中在上 + 下排原三键。**#3 只翻首通必得软货币**（结算软货币+固定补充·唯一核/三选一选中项/Boss星辉货舱都不翻·Ron 确认口径）。新增 `pendingClearAd` 跨越"选完"这步承接第二段上下文、`dismissBattleScene` 统一清（三退出键都走它）。**普通关不变**（本就无广告键）。
- **测试/门禁**：新增 boss 三阶段测试 2 条；**`npm run gate` 全绿：107 文件 1146 测（+2）· typecheck 0 错 · configs 43 表 valid**·零回归。表现层（升级刷新/回档键/结算窗/两段式）无 vitest（项目约定表现层 Ron 真机验），靠逻辑推演+真机自验。
- **自检结论**：①两段式重构深度自检——逐路径追（Boss首通/普通首通/重复通关/败/'none'档）确认广告键只在"精英/Boss选完"亮、软货币恰好×2（基础发1份+广告补1份）、唯一核不翻；②回档键假设全核实（`session.progress` 是进度真源·`persist` 同步·照 onPickLevel 同款写法）；③②b bossGrand 在 composeResultText 前已备好（captureLevelReward 顺序）；④②c 造了防假过反例（极高血只出 start）；⑤清理：删未用的 `doubleLevelReward` 控制器导入、去掉 `pendingLevelReward.adDoubled` 字段。自检无异常。
- **DEV-TEMP 变更**：hub 工具行 +「回档n029」（`devRollbackToFirstBoss`·上线前删·已入 README 待删清单）；`S7_DEMO_SEED_CORES` 剔除 core07。
- **文档同步**：附录D 敌人「海盗大副」删"解锁星核展厅"半句（留深空回廊）；GDD-v2.0 S7 建筑解锁节奏 + GDD-M 第3天：星核展厅标"早期可用·与首Boss解耦·精确时机第三块定"；附录B B1.5 交互改两段式描述；README 决策/DEV-TEMP 清单同步。
- **🔎 Ron 真机自验点**：① **新档全链路**：打到 n030→打赢→看三选一（第一段·必选）→选完→结算窗见「🎁特殊大奖：陨星弹」+「📺看广告·必得软货币×2」单行→看广告→软货币翻倍飘字→三键返回；② **回档反复验**：hub 点「回档n029」→出战打 n030→重复①（可反复）；③ **升级即时刷新**：船坞/训练舱→管理→升级→当场见 Lv+1/战力/成本变；把某 C 阶船升到 Lv20→见"已满级·升阶解锁更高"；④ **两段式普通关不变**：打个普通关首通→三选一→结算窗**无**广告键。下一块=**块3 深空回廊**（细案先过 Ron）。

### ✅ 块2 真机验收反馈·第2轮收尾（2026-07-04·4件·均 Ron 真机拍板·终稿）
> 第1轮修完 Ron 真机再验，4 件都在结算链路上——**其中③是终稿、推翻上一轮"广告键放结算窗"改成"放三选一屏"**。全在 `S7DemoController.ts` + `core_config`/`battle_effect_param` 数据文件。
- **③ 三选一屏＝唯一奖励屏（终稿·重构结算流程）**：把奖励展示+广告键**全集中到三选一屏**——顶部固定奖励列表 + 必给大奖预告(精英/Boss) + 三张选卡；**选完**→三卡锁定(选中✓/其余灰)→**同屏下方浮现**（仅精英/Boss）「📺看广告·固定奖励×2」(#3)+「继续」；**结算窗瘦身**：`composeResultText` 去奖励明细，只留 banner+残血+推进(+救回人口)+📊+三键，**两屏不再重复**。普通关现状不动（选完直接关·#3待2.5块⑤广告网关补齐套此"选完浮现"结构）。撤掉上一轮 `resultAd*`/`pendingClearAd`/`onResultClearAdDouble` + 还原结算窗高度(dh 0.46→0.32)。
- **① 翻倍可感知反馈**：`onLevelRewardAdDouble` mock 完成→固定奖励行**就地刷成×2值** + 飘字"星矿 +30→+60，…" + 广告键变「✓固定奖励已翻倍」；重复点由 `softDoubled` 挡住。
- **② Boss大奖特写弹窗**：新增 `buildGrandRewardPopup`/`openGrandRewardPopup`——「继续」后、结算窗前，Boss首通弹独立弹窗（★首通大奖★+图标占位色块+大字奖名+效果说明〔取 `core_config.roleNote`·陨星弹="新手核·普攻质变为原子炮"〕+收下）；精英无 bossGrand→不弹。灰盒复用通用弹窗、正式演出随战斗演出块升级。
- **④ core07 显示名改正**：真机显示"过载核心"→真源§1 定名**陨星弹**。改 `core_config` name + 装配面板 header 文案 + effect note；`coreName()` 读配置→其余显示自动跟。**顺查发现 core01-06 名字(破盾棱镜星核等)与真源(银河烟花/小太阳…物品名·规定"不叫XX核")完全对不上**——Ron 拍板**只改 core07·余下维持**（core01-06 是旧灰盒占位·真源身份未定·哪颗对应真源哪颗属内容铺量·乱改=脑补，等星核16内容块统一落）。加 `s7_config_runtime` 断言 core07 name='陨星弹' 防回退。
- **测试/门禁**：`npm run gate` 全绿·107 文件 1146 测·typecheck 0 错·configs 43 表 valid·零回归。表现层重构无 vitest，靠深度自检+真机。
- **自检结论**：逐路径追（Boss首通/精英首通/普通首通/重复通关/败/'none'档）——广告键只在"精英/Boss选完"浮现、固定奖励恰好×2(基础1份+广告1份)、唯一核/选中项/星辉货舱不翻、大奖特写仅Boss、结算窗瘦身不丢进度/残血信息；构造反例（重复点广告不重复翻倍、普通关无浮现走原路径、教程关normal档选完即关不卡tutorial轮询）。清了残留 `pendingClearAd` 字段。自检无异常。
- **🔎 Ron 真机自验点（重点）**：① **三选一屏选完→翻倍键浮现→点了看到固定奖励数字就地翻倍**（+飘字+键变✓）；② **Boss打完（回档n029→打n030）→选完点继续→看到陨星弹特写弹窗**（名+"普攻质变为原子炮"+收下）；③ 结算窗**只剩** banner+📊+三键（无奖励明细/无广告键）；④ 普通关选完直接进结算窗（无浮现/无特写）；⑤ 装配面板星核 header 显"陨星弹"、展厅/大奖处核名都是"陨星弹"。下一块=**块3 深空回廊**（细案先过 Ron）。

### ✅ 块2 每日委托 → 星港悬赏板·整体重构（2026-07-04·设计 Ron 拍板定稿·步1-2 已 commit·步3 文档同步）
> 起因：块2 真机验收时第④项"委托结算并键"特意留下没做——委托玩法整体作废、重构为"星港悬赏板"（每天刷带**词缀**的悬赏卡·场场真打看戏找策略）。设计定稿见 GDD-v2.0 S10.8 重写版 + 附录B B5.1 重写版。分 3 步：步1 核心+配置+存档迁移+拆委托（commit `3f1ce41`），步2 独立 view + 悬赏战斗编排（commit `4005bc4`），步3 文档同步。

- **步1·核心逻辑（纯TS·可测）**：
  - `core/s7/S7StarportBounty.ts`（新）：确定性刷4卡（seed=`bounty_<dayKey>`·杀进程重进不换卡）/品质铜银金+**暗保底**（连 `PITY_DAYS`=4 天无金必出·`noGoldDays` 跨天线程）/词缀条数随品质（铜1/银2/金3）/积压封顶12·16·20（居住舱钩子·超容量掉最旧）/产出（护航合金+星贝·演习驾驶记录·品质倍率1/1.6/3·×星域系数·完美护航×1.25）/护航遇袭（按卡确定性 seed=`ambush_<id>`·占位15%）/金卡实物+遇袭小包**按"获得次数"轮换**（`goldPhysicalCount`/`ambushBonusCount`·Ron 拍板·确定性·避免同日多金给同实物）/`settleBountyCard`（结算=发奖+移除卡+推进轮换计数）/老档折算铜卡。
  - `core/s7/S7CommissionAffix.ts`（新）：`S7PositionType`（突击/护卫/炮击/支援/工程）+ 12词缀按定位型给我方注**既有积木**（stat→modifier / affix→affix·`commissionAffixBlocks`）+ `matchedCommissionAffixes`（备战标记用）。**零引擎改动**——Ron 拍板把2条引擎缺旋钮的词缀改写成纯活旋钮版（加护力场→治疗+50%血-40%·蜂群协议→CD-40%防-40%），半残词缀不上板；原护盾强度/召唤上限真旋钮记遗留。
  - 配置 `commission_affix_param`（新·12词缀·全占位）+ 两校验器登记（TS+Node 镜像·`validateCommissionAffix`）+ 手写 `.meta`；`battle_unit_stat_param` 补 `positionType`（**仅玩家船必填∈5定位型**·校验器硬拦·**灰盒占位**按 roleNote 归类〔突击3/护卫2/炮击2/支援2/工程3〕·Ron 扫过·star-core-config-names-stale 同类占位·星舰内容块随真源统一校准）。
  - 存档 **v20→v21**：`commissions`→`bounty`；**真迁移**（老档护航+演习 stock 之和折算等量铜卡·封顶12·折算卡无词缀）；`normalizeS7Bounty(raw.bounty, raw.commissions)` 一处收口。
  - 拆委托：删 `S7DailyCommission.ts`+测试；`S7DemoController` 旧委托面板/结算路由/备战钩子移除（~233行）；hub「委托」→「悬赏」。
- **步2·独立 view + 战斗编排**：
  - `ui/view/S7BountyBoardView.ts`（新·**首个 S7 独立 view**·守工作流程⑧）：全屏悬赏板·分页卡列表（主题×品质徽标+**词缀全文**+产出预览+出战）+积压行+返回；自建节点·TOUCH_END 直挂（cc 自动派发·无需控制器路由）·宿主接口 `S7BountyHost` 拿数据+回调。
  - `S7DemoController` 挂 view + 悬赏战斗流程：`openBounty`=日刷+落盘+开 view；出战=进主线同款备战（默认上次阵容·敌情预览切悬赏敌阵·信息行挂本场词缀全文=灰盒词缀标记）；`runBountyBattle`=按卡词缀给对应定位型（读 `battle_unit_stat_param.positionType`）注积木+护航附运输船·`battleRunService` 真打不推主线；胜=`settleBountyCard` 发奖入账+移除卡+遇袭判定·负=不罚卡留板；遇袭=结果窗返回时接一场星盗遭遇战（赢领小包·输不罚）；结算=**单键「返回悬赏板」**（openResultPopup 对悬赏藏"选关/下一关"·rule⑨·了结旧委托并键遗留）。
- **怎么测的**：新增 `s7_starport_bounty.test.ts`（33条·确定性/暗保底/积压容量/产出轮换/遇袭/结算/迁移/规范化）+ `s7_commission_affix.test.ts`（14条·定位型过滤/all/上阵条件/mod→积木）+ `s7_save` 迁移3条 + `s7_config` 校验反例5条 + 表数43→44/版本20→21 重定基。`npm run gate` 全绿（108文件**1179测**·typecheck0·configs44表）零回归。
- **深度自检抓到真问题**：第一版"暗保底状态机"测试**假过**（断言 `maxNoGold≥1`·即便线程写死传0 无金日也=1 照样绿）→ 换"逐日等价+累积≥2"强断言，并**真破坏 `refreshBountyBoard` 线程验证会红**（`expected 1 to be 2`）再改回。表现层逻辑追踪自检（6反例：赢移卡/输不罚/遇袭不双触发/bounty标记不泄漏主线/词缀按定位型/空板不崩）无异常。
- **DEV-TEMP 变更**：旧委托面板「测隔天」随面板删除（无新增·悬赏日刷 DEV-TEMP 待真机反馈按需补）。
- **遗留/交接**：① **数值全 v0.1 占位**（金卡概率8%/银32%/暗保底4天/品质倍率/产率/遇袭15%/完美25%/词缀各修正量/攻速↔间隔·CD↔skillHaste 换算）→ 第三块数值校准；② **定位型灰盒占位**（12船按 roleNote 归类·非真源）→ 星舰内容块随真源统一校准；③ 词缀**护盾强度/召唤上限**真旋钮记遗留→战斗演出+数值校准阶段引擎补全后可加回强化版词缀；④ **4 个新增待 Cocos 认 `.meta`**：`S7StarportBounty.ts`/`S7CommissionAffix.ts`/`S7BountyBoardView.ts`/`commission_affix_param.sample.json`；⑤ 真实悬赏敌阵/涂装/靶机/运输船演出 + 逐单位格词缀图标标记留美术/演出阶段（现共用灰盒敌阵+备战信息行）；⑥ 遇袭遭遇战灰盒复用同敌阵（真实星盗涂装留美术）。
- **🔎 Ron 真机自验点**：① 新档打到关5后 hub 见「悬赏」→ 点开见板（4卡·主题/品质/词缀全文/产出·积压行·分页·返回）；② 点卡出战→备战信息行见【悬赏】+词缀全文→真打（2×/3× 看词缀生效）→胜：结算窗**单键「返回悬赏板」**（无选关/下一关）→返回见卡消失、钱包+产出；③ 护航触发遇袭→结算窗提示"遭遇星盗"→返回接遭遇战→打完再单键返回板（小包入账）；④ 打输→单键返回→卡还在板上、钱包没变；⑤ 连开几张金卡看实物轮换（信标→碎片→加速券）；⑥ Cocos 认 4 新 meta。下一块=**块3 深空回廊**（细案先过 Ron）。

### ✅ 块2 悬赏板真机验收修复批①（2026-07-04·4件+1答复·均 Ron 真机反馈）
> 首轮真机 4 件反馈：①词缀/产出文案画在卡片框外 ②**灵魂bug**出战不进战斗场景（跳回主界面隔会弹结算）③要2个 DEV 测试键 ④"星域档 0"程序员0起数。答复⑤：v20→v21 迁移保留（开发期便利·上线工程随 DEV-TEMP 大扫除统一瘦身迁移链·已记 README 遗留）。改动=`S7DemoController.ts` + `S7BountyBoardView.ts`（修 bug 不算扩单体）。
- **②灵魂bug·真因与修法（最优先）**：主线战斗是**就地在备战舞台播放**（`startPlayback` 只藏 `prebattleUiNode`、战斗帧画在 `prebattleNode` 里的 `prebattleGfx` 上），主线出战**从不关备战层**；悬赏出战却先 `closePrebattle()` 把整个舞台节点藏了→战斗在隐形节点上照跑（`update` 不看 active）→"跳回主界面、隔会弹结算"。**修**：悬赏分支与主线同口径**不关备战层**；成功进播放才清悬赏模式标记（失败留在备战可改阵容重试·失败提示改写**备战信息行**〔面板开着 hubToast 被盖住看不见〕）；+`bgm_battle` 同主线。**遇袭同款修**：原先在 `dismissBattleScene()`（收舞台）之后启动遭遇战→同样隐形；改为返回时**只收结果窗、舞台留着**，同一舞台直接接打（`startPlayback` 全量重置 frameIdx/时钟/入场·背靠背二次播放安全）；遭遇战起不来才收舞台回板防卡死。
- **①文案锚定**：view 卡片中栏原是**中心锚+无内容框**——多行文本以点为中心向四周长、悬出卡外被截断。修：左缘锚点 `(0,0.5)` + 显式内容框（徽标右缘到出战键左缘·`setContentSize(rowW-327, rowH-20)`）+ `Overflow.SHRINK`+自动换行——银2条/金3条词缀+产出行在窄屏也**保证收进卡内**（超长自动缩字号·灰盒可接受）；顺带删掉原先的 `horizontalAlign` 强转 hack、完美护航提示缩短为"｜满血护航+25%"。
- **③DEV-TEMP 两键**（hub 工具行改 5 槽 132 宽·上线前整行删）：**「刷悬赏」**=重掷今日4张（换种子 `fakeKey=dayKey*1000+salt` 重抽·只换当前批不动更早积压·**暗保底计数照常推进**〔连3批无金后下批必金·toast 实时报数〕·salt 绑启动时间避免重启后与上次会话 DEV 批撞卡 id）；**「必遇袭」**=武装/解除"下张护航打赢必触发遇袭"（仅内存·用掉即灭；15% 概率打4次不中很正常没法验收）。均已记 README DEV-TEMP 待删清单。
- **④档位显示**：板头"星域档 ${tier}"→`${tier+1}`（玩家侧从1起·内部 tier=已通关星域数 0 起·纯显示层不动逻辑）。
- **测试/门禁**：`npm run gate` 全绿（108 文件 1179 测·typecheck 0·configs 44 表）零回归。表现层无 vitest（项目约定），靠深度自检+真机复验。
- **自检结论**：①路由深度自检——逐路径追（主线出战不受影响〔bounty 分支有标记守卫·教程期标记恒空〕/悬赏胜/负/遇袭链/失败重试/取消清标记），构造反例：遇袭不双触发（pending 进场前清空）、悬赏结算藏掉的"选关/下一关"两键在下次主线结算恢复（openResultPopup 每次重设）、二次播放前置条件核实（startPlayback 全量重置+不查 active）；②**自检抓到并修了一个问题**：DEV 重掷 salt 仅内存重启归零→重启后重掷与上次会话 DEV 批撞卡 id（板上现重复 id 卡）→salt 绑启动时间实际避撞；③"这张悬赏已失效"死角（卡失效+标记还在→反复报错）确认只有 DEV 重掷能造成且有 playing/面板遮挡双重守卫、玩家取消可退出——可接受不加码。自检无其余异常。
- **🔎 Ron 真机复验点**：① **出战能亲眼看到战斗全过程**（我方飞入→对轰→结算才出弹窗；挂「狂热弹药」的突击舰应打得又快又脆）；② 遇袭：点「必遇袭」武装→打赢护航→返回→**同一战斗画面直接接遭遇战**（能看全程）→打完单键回板（**此条已被修复批②的风险抉择流程取代**）；③ 卡片词缀/产出文字全部在卡框内（金卡3条也收得进）；④ 板头显示"星域档 1"（新档）；⑤「刷悬赏」连点几次：品质分布在变、连续无金 3 批后第 4 批必出金（toast 报数）。

### ✅ 块2 悬赏板真机验收修复批②（2026-07-04·2件·第②件=Ron 拍板设计变更）
> ①悬赏备战底部按钮组与主线分流；②**遇袭改"风险抉择"**（设计变更·GDD S10.8 遇袭段同步重写）。改动=`S7StarportBounty.ts`（+折损纯函数）/`S7DemoController.ts`/`S7BountyBoardView.ts`（+notice 行）/ 测试 / GDD / 附录B / README。
- **①备战两键分流**：悬赏备战底部藏主线专属「选择关卡」、「返回星港」改**「返回悬赏板」**（点击回悬赏板而非基地），保留「开始战斗」——两键；主线备战三键不动（`openPrebattle` 按悬赏模式切·`onPrebattleBack` 分流）。**顺查主线元素泄漏**：信息行已挂【悬赏】+词缀全文✓、敌情预览已切悬赏敌阵✓、"暂无遭遇点选择关卡"提示在主线专属路径✓、教程高光只在主线（悬赏锁到引导后）✓——无其他泄漏。
- **②遇袭=风险抉择（取代"返回自动接打"）**：护航胜且触发遇袭 → 结算窗两键：**「⚔ 正面迎战」**（右键·同舞台接打：胜=额外小包照旧；**败=折损本单护航入账的 30%（占位·挂细表）**——实现取"先足额入账、败后按本单量回收"：`ambushLossPenalty` 纯函数逐键 `floor(本单×30%)`，**回收量恒 ≤ 刚入账量→数学上碰不到既有存量**；量小实物 floor 后=0 天然免扣〔金卡信标×1 不会被扣走〕；明细文案"遇袭损失 -XX（只折本单护航收益·库存未动）"）/ **「🛡 躲避袭击」**（中键·零损失返航·板上现中性提示"舰队绕道返航，本单收益已入账"·不嘲讽）。`bountyAmbushPending` 改带本单入账上下文；「必遇袭」DEV 键照常配合新流程（武装→打赢护航→出两键）。
- **测试**：+4 条（折损 floor30% 数值/量小实物免扣/**折损恒≤本单入账不变量**/空表边界 + 比例常量防无声改动断言）；迎战胜=既有小包轮换测试覆盖；躲避=零核心状态变更（无可测·逻辑追踪+真机验）。`npm run gate` 全绿（108 文件 **1183 测**·typecheck 0·configs 44 表）零回归。
- **自检结论**：①"绝不碰存量"用不变量测试+入账时序双保险（先足额入账→败后回收 ≤ 入账量；结算与遭遇战之间玩家只面对结果窗、无消费入口→钱包不可能低于刚入账量，防御钳 0 兜底）；②路由反例——迎战键只在"悬赏+遇袭 pending"时可见（主线右键路径有 bountyActiveCardId 守卫·普通悬赏右键隐藏）、躲避后 pending 清空不复现、迎战后 pending 清空不二次触发、遭遇战起不来=收舞台回板不罚不卡死；③备战两键切换用 openPrebattle 每次重设（悬赏↔主线交替开不残留文案/显隐）；④遇袭提示文案与两键语义对齐（迎战风险讲清·躲避不嘲讽）。自检无异常。
- **🔎 Ron 真机复验点（批②）**：① 点悬赏卡出战→备战底部只有**「返回悬赏板」+「开始战斗」两键**（无"选择关卡"）、点返回回的是悬赏板；主线出战备战仍三键；② 「必遇袭」武装→打赢护航→结算窗见**两键「🛡 躲避袭击」「⚔ 正面迎战」**；③ 选躲避→回板见绿字"舰队绕道返航，本单收益已入账（零损失）"、钱包=护航全额；④ 选迎战且打赢→同舞台看全程→额外小包入账；⑤ 选迎战且打输（可换弱阵容）→明细见"遇袭损失 -XX 合金…（只折本单·库存未动）"、扣的量≈本单三成、金卡实物没被扣走。

### ✅ 块3 深空回廊（2026-07-05·细案 Ron 拍板·三步走 步1/2a/2b 已 commit·步3 文档同步）
> 无尽塔长线循环（GDD S10.7 / 附录B B5.2）。解锁=首Boss（n030）通关后。设计真源 GDD S10.7（本块已重写含双层奖励修订 + 受控入口实现注记）。分 3 步：步1 纯逻辑（commit `220eca7`），步2a 受控引擎入口+战斗服务（commit `c0a4f2c`），步2b 塔页view+出战流程（commit `ffc87c4`），步3 文档同步。

- **步1·纯逻辑（纯TS·可测·不碰引擎）**：
  - `core/s7/S7DeepCorridor.ts`（新）：**层生成器**（`corridorLayerPlan(layer,palette,bosses)`·种子=层号·全服同层同阵·纯函数）——敌阵从主线148敌人库的1x1敌人行确定性抽+按层缩放积木（占位）；**回响Boss**（`corridorEchoBoss`·层%25→主线7 Boss〔n030/n060/n084/n102/n120/n138/n150〕顺序轮换·轮完加倍率·enemyBlocks 缩放）；**双层奖励**（`corridorLayerReward` 每层首通小奖 + `corridorMilestoneReward` 每5层里程碑·深层出稀有/史诗信标 + `doubleCorridorReward` 全键翻倍无排除）；**存档态**`{highestClearedLayer,claimedMilestones[]}`+默认/规范化；进度（`nextCorridorLayer`/`clearCorridorLayer` 只推进"下一层"·重打已通层零奖）；里程碑领取状态机（`availableCorridorMilestones`/`canClaim`/`claimCorridorMilestone`）；解锁门控（`corridorUnlocked`=首Boss已通）。占位数值全走 exported const（照 S7StarportBounty 成例·不建配置表·10戏法机制不齐整硬塞表别扭·Ron 同意）。
  - `core/s7/S7CorridorTricks.ts`（新·对标 S7CommissionAffix）：10 戏法→纯数据作用清单（`corridorTrickEffect`）——**零新增机制类型**全复用既有积木：铁甲潮=敌护甲 modifier·护盾矩阵=敌 battle_start 触发现成 `eff_state_shield`·蜂群=敌数×2+血攻削弱 modifier·乱流=我方 skillHaste 负 affix（CD×1.5）·后排火力=敌摆高列·持久战=加 `bu_enemy_support` 奶妈·精锐/孤胆/闪电战/静默空域=上阵上限3/1/限时40/禁核标志。每戏法带规则文案+解法（塔页/备战明示用）。
  - 存档 **v21→v22**：加 `corridor` 字段 + `normalizeS7Corridor`（加性·上线全新档无老档可迁）。
- **步2a·受控引擎入口 + 战斗服务（并行加法·护栏①②）**：
  - 引擎扩展 `S7AutoBattleTypes`+`S7AutoBattleEngine`：`S7AutoBattleRunRequest` 加**三可选字段** `inlineEnemyUnits`（内联敌阵）/`enemyEffectBlocks`（敌方积木）/`timeLimitSecOverride`（限时覆盖）；`execute()` 两处 guarded 分支（限时>0才覆盖·内联给了则一次性铺敌否则走 `loadSpawnPlans`）+ 新方法 `placeInlineEnemies`（复用 `spawnUnit(derived)`/`canPlace`/`deriveUnit`·敌方同口径消费积木·t=0记spawn_wave供演出登场）。**`loadSpawnPlans`/`processSpawnPlan`/`spawnUnit`/`placePlayerUnits` 一律未动**；主线/悬赏不设三字段=逐字节零行为变化。
  - `core/s7/S7CorridorBattleService.ts`（新）：`runCorridorBattle`=复用组装器以 n001 为上下文/时基载体建玩家单位（**不碰组装器/主线**）+ 引擎请求叠加三字段；敌阵来源=普通/戏法层生成敌阵·回响Boss层复用Boss节点 encounter→spawn 形状（`bossNodeInlineEnemies`）+轮次缩放；我方回廊效果=乱流 playerBlocks 注 extraBlocks + 静默空域禁核（strip coreId）；上阵超上限抛 `S7CorridorLineupCapError`。
- **步2b·塔页view + 控制器出战流程（灰盒·守 B0.6 十二条）**：
  - `ui/view/S7CorridorTowerView.ts`（新·独立view·守工作流程⑧）：层数牌+当前挑战层高亮大卡（类型/戏法规则/解法/敌情）+上方几层剪影（戏法⚔/Boss👑/里程碑🎁标记）+里程碑宝箱区（可积攒·「开箱」+「开箱×2📺」）+挑战大钮+返回+DEV跳层行。宿主接口 `S7CorridorHost`。
  - `S7DemoController` 接线：hub「回廊」入口（首Boss后解锁·refresh门控）；回廊备战模式（`corridorPrepLayer`·藏「选择关卡」·返回「返回回廊」·信息行=层号/戏法规则/敌情·敌情预览画回廊生成敌阵）；`launchCorridorBattle`（真打·就地舞台播放·胜=首通推进+发小奖+`tower_up`·遇里程碑提示回塔开箱·败=停留原层不罚给养成建议）；结算单键「返回回廊」（`corridorActiveLayer` 路由·与悬赏/主线三套标记互斥）；里程碑塔页手动开（`tower_milestone`）+看广告翻倍（广告点位#10 `corridor_milestone_double`·mock）。
  - 音效：`SoundEventTypes` 补 `tower_up`/`tower_milestone`（附录C 包A事件代码侧登记）。广告：`S7AdGateway` 补点位 `corridor_milestone_double`（#10·逐块补齐到10·测试6→7同步）。
- **怎么测的**：新增 `s7_deep_corridor.test.ts`（层生成确定性/戏法层应用/回响Boss轮换+倍率/双层奖励+翻倍/里程碑发放与领取/解锁门控/迁移）+ `s7_corridor_tricks.test.ts`（10戏法各产出正确积木/规则）+ `s7_corridor_battle_integration.test.ts`（**反例三件套**+并行加法零回归+内联敌阵+回响Boss复用+上阵上限+禁核）+ `s7_save` v22迁移 + `s7_ad_gateway` 点位6→7。`npm run gate` 全绿（111文件 **1235测** · typecheck0 · configs44表）零回归。
- **深度自检·反例三件套抓到并修的问题（引擎触碰硬要求·护栏②）**：① **铁甲潮开/关**跑同一敌阵→护甲拖慢击杀 durationSec 不同（戏法没生效会红）；② **闪电战**限时覆盖真在40s判负（reason=timeout·durationSec=40）·120→120s——**第一版失败**：单盾卫舰被头目在40s前打死(all_players_down)没到限时→改成给玩家加大血量撑过窗口打不死头目，才真验到限时覆盖本身；③ **同层同种子逐字节一致**。并行加法零回归=gate机器验收（护栏①）。表现层模式隔离逐路径追（回廊/悬赏/主线三套标记互斥·出战入口互斥·结算返回互斥）无异常。
- **DEV-TEMP 新增（上线前删·已记 README 清单）**：回廊塔页 DEV 行（跳戏法/跳Boss/跳孤胆/+25层·拨已通最高层到目标层-1·验戏法层/回响Boss/深层孤胆·否则爬不到没法验收）。
- **遗留/交接**：① **数值全 v0.1 占位**（层缩放曲线/戏法各修正量/回响Boss倍率/每层小奖+里程碑奖励量/深层阈值/孤胆深层门槛100）→ 第三块数值校准（"每层小奖从里程碑预算拆·总量不变"注释已挂·§3体检表不动）；② **回响Boss 简化**（复用Boss节点敌阵形状+缩放·灰盒**未接3阶段phase**·真boss phase留数值/演出阶段）；③ 敌阵调色板=1x1常规敌人（排除2x2头目）·真实回廊敌阵涂装/深层专属敌留美术；④ **4 个新增待 Cocos 认 `.meta`**：`S7DeepCorridor.ts`/`S7CorridorTricks.ts`/`S7CorridorBattleService.ts`/`S7CorridorTowerView.ts`；⑤ 塔身滚动/剪影/星云边框等真实塔页视觉留美术阶段（现=色块大卡+描边+emoji标记）。
- **🔎 Ron 真机自验点**：① 新档打通首Boss(n030)→ hub「回廊」从锁变亮；② 进塔页→挑战下一层→备战（信息行【回廊】+敌情预览·底部两键「返回回廊」+「开始战斗」·无「选择关卡」）→真打→胜单键返回→层+1、钱包+小奖；③ DEV跳戏法→看戏法生效（铁甲潮敌肉/后排火力敌靠后/乱流我方技能慢）；④ DEV跳Boss(25)→大卡显回响Boss；DEV跳孤胆→带3舰被拦「本层限1舰」；⑤ 打到第5层→塔页里程碑区→「开箱」入账/「开箱×2📺」看广告翻倍；⑥ 打输→单键返回→层不变钱包不变给养成建议；⑦ Cocos 认 4 新 meta。下一块=**④每日推演**（细案先过 Ron）。

### ✅ 块4 每日推演（2026-07-05·细案 Ron 拍板·三修订·三步走 + 作战大厅架构 + 真机修复批 + 步3文档）
> 全服同题战术残局（GDD S10.9 / 附录B B5.3）。解锁=打通 n040 首通后。Ron 2026-07-05 三修订：①解锁 n040〔替代关5〕②战队包制〔6-8包舰+员绑定〕③验解器三闸。hub 架构修订：并入「作战大厅」容器双页签。步1 commit `9419eed`、步2 commit `05fecfc`、修复批 commit `c574098`、步3 文档同步。

- **步1·纯逻辑+验解器+5样题（commit 9419eed）**：
  - `core/s7/S7DailyPuzzle.ts`（新）：每日轮换（`s7DayKey` 整数天序 % 题数·全服同题）/存档态 v23（当日题号+已解+尝试·跨天重置）/解锁门控（`dailyPuzzleUnlocked`=n040 已通·沿用回廊成例）/首胜发奖占位（星贝+通用星舰碎片·exported const）。
  - `core/s7/S7DailyPuzzleBattleService.ts`（新）：战队包→组装器建**临时内存态单位**（归一 C 阶 Lv10·shipLevel/pilotLevel 强制 10·不落存档不碰 box）+ 块3 受控引擎入口注内联敌阵——**零引擎改动**（主线/悬赏/回廊路径一字未动）。战斗种子对本题固定（全服同题·确定性）。
  - `core/s7/S7DailyPuzzleSolver.ts`（新）：三道闸（作者解回放胜/蒙特卡洛乱选率<30%/候选数∈[6,8]）+ 输出乱选率·确定性固定种子固定 200 采样。
  - 题库配置表 `daily_puzzle_param`（新·注册进 ConfigTypes/Loader/两校验器·5 道手写样题+手写 .meta）：覆盖 后排点名/护盾/召唤/治疗/爆发 五类威胁，全过三闸（乱选率 backline23%/shield20%/summon17%/heal29%/burst26%）。授权时摸清引擎机制，逐题调成"贴 120s 超时的紧竞赛"（护盾/治疗/召唤靠机制硬否决·后排/爆发靠位置+紧竞赛）。
  - **验解器落地（Ron 认可）**：静态那道闸(c)+结构进 `validate:configs:s7`（.mjs+ConfigValidatorS7 镜像·.mjs 跑不了 TS 引擎属本工程既定形状），引擎两闸(a/b)进 vitest 遍历题库测试——两门同在 `npm run gate`。存档 v22→v23。
  - **怎么测的+防假过**：纯逻辑 9 条+验解器三闸+**防假过反例 3 条**（作者解错的题必被拦/太简单题必被拦/候选越界必被拦）+校验器拒坏配置 7 条+存档 v23 迁移/往返。**变异验证**：把验解器改成"永远判胜"→反例①+主测试立刻变红（证防假过非摆设·已还原）。gate 全绿（113 文件 1258 测·configs 45 表·2.8 秒远低于 3 分钟护栏）。

- **步2·作战大厅+推演页+出战流程（commit 05fecfc）**：
  - **hub 架构（Ron 2026-07-05 拍板）**：hub「悬赏」入口→「作战大厅」容器（`S7CombatHallView`·两页签 悬赏板+每日推演）；悬赏 view **最小改造折进页签**（+topY 让位页签栏 +hideClose 大厅统一提供返回·**真打逻辑一字未动**·摁住回归风险）；回廊维持独立。
  - `ui/view/S7DailyPuzzleView.ts`（新·守⑧）：题头/威胁/敌阵沙盘/候选战队包横排/我方 3×3 选摆（点包→点格放·点已放格拿回）/开打/未解锁态。
  - `S7DemoController` 接线：openCombatHall（日刷两页签+清DEV·reopen不日刷）/makePuzzleHost/buildPuzzleViewData/launchPuzzleBattle（就地激活战斗舞台播放·摆位已在推演页做**不走主线备战屏**·查证 startPlayback 只需调用方先激活 prebattleNode）/首胜发一次奖/结算单键「返回推演」（puzzleActiveId 与悬赏/回廊/主线互斥）/DEV跳题·清摆。音效 puzzle_start/solve。

- **✅ 真机修复批（commit c574098·Ron 真机反馈 3 件·步2 通过后）**：
  - **①推演页布局重排**（主病灶：沙盘/候选/九宫格重叠）：四区自适应顺排（题头→敌阵沙盘→候选包→我方摆位+开打）·区间零重叠留呼吸·候选 2 行不压缩·底部 DEV 行与内容隔开·逐条守 B0.6。
  - **②九宫格转向**（对齐实际战斗 `fieldPlayerPos`/`fieldEnemyPos`）：整体转 90°→纵深=竖轴/横向=行·我方**上=前排贴敌**·敌方**下=前排贴我**·前后排标签跟换；**纯视觉+点击映射**——**引擎坐标/题库 slotRef/验解器全不动**（验解器直接用引擎坐标不经 view=安全性铁证）。**②防再犯收尾（Ron 拍板）**：把"格子→屏幕位置"朝向抽成唯一真源 `ui/S7BattleFieldOrient.ts`（`s7FieldVisualCell`/`s7FieldUniformPos`·纯TS可测）；`fieldPlayerPos/fieldEnemyPos` 改调它（输出逐字节不变=零回归）、推演页两个格子改调它（删内联转向数学）；今后阵位类界面一律走它、禁自写（B0.6 #13 + B0.7 战场空间公约）。+朝向测试 7 条。悬赏/回廊备战本就走 fieldEnemyPos=已间接走真源。
  - **③a 上场自动收**：备战「上阵界面」点上场后自动收起（下场保持开）·主线/回廊/悬赏共用备战都受益·与引导 step29 既有 closeBoarding 一致不打断。
  - **③b 分玩法阵容记忆**（四把钥匙）：`S7Squad` +`lineups{mainline/corridor/escort/drill}` 各记编队摆位·跨玩法不串·装配跟船仍全局；openPrebattle 按模式（悬赏卡主题分护航/演习）载入·上下场/拖动换位后存记忆；存档加性（老档首进各玩法从当前 formation 播种再分叉）。+S7Squad 测试 4 条（四钥匙不串/迁移播种/规范化round-trip/老档补空）。
  - gate 全绿（113 文件 1262 测·configs 45）零回归。**FPS 疑问答复**：推演页静态 UI·Cocos 无变化不重绘=空闲低 FPS 属正常非卡死·update 循环仍跑·点击才 refresh 重建。

- **步3·文档同步**：GDD S10.9 按三修订重写+实现注记 / B5.3 战队包制照实 / **B1.1 按 10 栋+中心三角〔船坞+训练舱居中·星核展厅顶点〕+内圈〔补给站/商人/作战大厅〕外圈〔打捞港/居住舱/研究塔/回廊〕+封顶 10 栋铁律重写** / B0.6 加③两先例（上场即收/分玩法记忆）/ B1.2 加分玩法记忆+上场自动收 / README 解锁口径关5→n040+DEV-TEMP+④完成。

- **自检结论**：①测试防假过=变异验证证反例非空；②"零引擎改动"机器零回归验收（1262 测全绿·未碰引擎/组装器/主线/悬赏/回廊）；③归一 C-Lv10 由组装器成长积木实现（作者解真能赢=单位建对）；④九宫格转向逐条核对与战斗坐标一致·验解器不受影响；⑤分玩法记忆边界（老档播种迁移/引导编辑全走存记忆点/跨玩法不串）逐路径追+反例（回主线仍原阵不被回廊串改）；⑥模式互斥（puzzleActiveId 只在开打设·单键返回清·不跨场泄漏）。自检无异常。
- **DEV-TEMP 新增（上线前删·已记 README）**：推演页 DEV 行（跳题〔内存覆盖今日题·不碰真档〕/清摆）。
- **遗留/交接**：① **数值全 v0.1 占位**（威胁强度 enemyHpPct/AtkPct 是灰盒调难度旋钮·发奖量·蒙卡采样数）→ 第三块数值校准〔届时 5 样题难度会随全局数值重调·题库30-50道 Codex 走量〕；② heal 样题乱选率 29% 余量偏薄（确定性稳过·记一笔）；③ 真实全息棋局沙盘/战队包卡涂装/妙手庆祝特效留美术阶段（现=色块格+emoji+威胁高亮）；④ **7 个新增待 Cocos 认 `.meta`**：`S7DailyPuzzle.ts`/`S7DailyPuzzleBattleService.ts`/`S7DailyPuzzleSolver.ts`/`S7CombatHallView.ts`/`S7DailyPuzzleView.ts`/`S7BattleFieldOrient.ts`/`daily_puzzle_param.sample.json`；⑤ 候选包横向滑动（窄屏 >8 包时）留美术/polish（现 2 行 4 列固定不压缩·8 包够放）。
- **🔎 Ron 真机复验点**：① 关5后 hub 见「作战大厅」→ 双页签「悬赏板/每日推演」+返回星港；**②悬赏没被折页签改坏**（最关键·卡/积压/分页/出战都在框内→真打→单键返回→遇袭两键照旧）；③推演(n040后)：四区不重叠→点包选中→点格摆位（上=前排）→点已放格拿回→摆满5开打亮→真打；④推演胜=单键「返回推演」见"今日已解"+奖励；败=再想想不限次；⑤未解锁(n040前)推演页显锁定提示；⑥DEV跳题循环看 5 题渲染+可解；⑦**分玩法记忆**：主线摆一套→回廊摆另一套→回主线仍是主线那套（跨玩法不串）；⑧上场后底部弹窗自动收；⑨回廊入口仍独立；⑩Cocos 认 7 新 meta。下一块=**⑤广告网关补齐剩余点位+零红点核查**（细案先过 Ron）。

### ✅ 块5 广告网关补齐 + 广告券 + 星港趣事 + 零红点核查（2026-07-05·任务单=Ron 已拍定稿·2.5块收官）
> 十点位补齐 10/10 + 全点位统一收口（GDD S13 块5 定稿版）+ 新资源广告券 + 今日补给箱/战败安慰包/星港趣事三个小系统 + 零红点/五原则全面核查。三 commit：步1+2 `b8e36f8`、深度自检修复 `2bdf7ff`、步3 文档。gate 基线 1269 → **1299 全绿**（+30）。

- **步1+2·统一收口 + 三个新系统（commit b8e36f8）**：
  - `core/s7/S7AdPointPolicy.ts`（新·唯一收口）：十点位**每日上限表**（Ron 决策①：全 1·唯一例外 #7 货舱多选 null 不限）+ **按钮三态**（可用→隐藏〔用尽&无券·非置灰〕→券态〔持券·标「广告券×N」〕·强引导期全隐）+ 广告券扣/退/结算纯函数；`S7AdGateway` 点位 7→**10**（+`daily_supply_chest`/`triple_pick_extra`/`defeat_consolation_double`）；`S7AdDailyCounter` +`adDailyRecord`（券路径/不限次点位无上限记数）。控制器抽统一 helper `adButtonState`/`applyAdButton`/`runAdPoint`（券态先扣→播→ok记数发奖·失败/被关**退券**·长文案 `clampBtnLabel` 防溢出），10 点位全部走它、无一处自写显隐。
  - **广告券（决策③）**：钱包 13→**14 键** `adTicket`（**不升版 v23**——normalize 对缺失键补 0 与 v6"旧键合并"不同属纯加性，已用测试钉死"v23 无券档加载=0·migrated=false·有值 round-trip 保留"；两套校验器词表+zhRes 同步）；商人 alwaysOffers 上架（**120 星贝占位·每日限购 1 占位**·挂第三块；恒排补给券 o0 之后不动教程 step43 高亮）；新增 `keepBoughtOnRefresh` 模板旗——**手动刷新不清广告券已购**（否则"免费/广告刷新重开一茬店"可绕过日限；跨天才重置）；强引导期商人货架**不渲染广告券行**。
  - **#5 打捞广告改行为（决策④）**：`salvageAdComplete`=看广告**直接完成当前打捞**（不论品质/剩余时长·原"减2小时"作废）；删 `salvageAdSpeedup`/`S7SalvageState.adSpeedup`/`S7SalvageConfig.adSpeedup`/`salvageDayKey`（老档遗留字段 normalize 静默丢弃·日界语义已在 s7DayKey 测试覆盖）；商人同款：删 `adRefreshRemaining`/`adPerCycle`（内部广告计数一律让位统一政策）。**#6 赞助券=+10/次**（`S7_SPONSOR_SUPPLY_TICKETS` 占位常量·每日1次）。
  - **#3/#4 首通两键（决策⑥·全部关卡）**：三选一"选完后"同屏并排浮现「📺固定奖励×2」+「📺再选一个」+「继续」——两键各自每日1次/券态/新手隐（选完那刻按三态现算·**两键全隐则直接走**不留空"继续"步·教程期两键必隐=行为与旧"选完直接关"一致、step40 毕业选卡照常续步）；#4=看完广告**剩两张恢复可点、玩家再选一张**（第二张也锁定✓·`canPickExtra` 纯函数守"只从剩两项发"·「继续」拦"已看广告未再选"防白看）；#3 翻倍口径不变（只翻固定软货币·唯一核/选中项/星辉货舱不翻）。行动宝藏/开箱等其它三选一不带两键。
  - **#9 战败安慰包（决策⑦·仅主线）**：`core/s7/S7DefeatConsolation.ts`（新）——主线战败结算卡下方附"安慰补给"小块：基础包（合金30/驾驶记录20 占位）**白送直接入账**每日≤3（计数走 adDaily 载体 key `defeat_consolation_pack`）、**当日首败额外附 1 张普通信标**、第 4 败起零包只显鼓励文案（柔和·带调性断言）；旁「📺 安慰双倍」每日1次=基础包再补一份（+X→+2X 回执·reward_claim）。悬赏/回廊/推演战败不给（不走主线 sortie 路径）；强引导期整块不给（新手全隐+教程零白送经济）。
  - **#2 今日补给箱**：`core/s7/S7DailySupplyChest.ts`（新）——hub 活动区旁礼盒岛（细条·B1.1）；**按 s7DayKey 确定性预掷**（种子=`supply_chest_<dayKey>_<当日第几开>`·杀进程重进不换奖·券重开换序号不发重份）；内容=S10.10 定死结构（合金/驾驶记录/星贝 ±20% 抖动 + 12% 普通信标 或 25% 通用碎片×4·全占位常量）；开箱=统一广告流+`supply_chest_open` 音效；当日已开→隐入背景（券态恢复）。
  - **星港趣事（决策⑧）**：`core/s7/S7StarportAnecdote.ts`（新）——**种子文案 12 条已写**（Q萌幽默日常·测试自动扫禁忌词「广告/买/充值/错过/倒计时/催促/卖惨」类；正式池 ≥30 条 Codex 后续）；按 dayKey 确定性掷"今日有无+哪条"（15% 占位）·每日≤1（adDaily 载体 key）·强引导不触发·**回港报告在场时让位**（开场主动弹窗≤1 独占）；触发点=开场无报告/领完报告/主线战斗返回 hub（悬赏/回廊/推演单键返回落点是大厅/塔页·不触发）；命中=微量奖励（星贝/星矿个位数）先入账+`trivia_pop`+弹泡。UI=独立 `ui/view/S7AnecdoteBubbleView.ts`（守⑧·不锁屏不吞别处触摸·只有泡本体可点·点掉即走）。
  - 音效：+`supply_chest_open`/`trivia_pop`（附录C 同步登记）；安慰双倍走既有 `reward_claim`（基础包与 battle_defeat 同刻不另配防叠音）；**删孤儿 `dispatch_done`**（委托→悬赏重构后秒结算已亡·代码+附录C 同步）；顺带附录C 补登 `puzzle_start`（代码有文档漏）。
  - DEV-TEMP：hub 工具行 5→**8 槽**（+「+5券」「重置广告」〔清今日全部每日计数·否则每日1次没法反复验〕「触发趣事」〔绕过概率与日限·池子轮换·真实入账链路〕）；打捞行 DEV「秒成」与 #5"看广告立即完成"并存（工具样式区分）。
- **步3·文档同步**：GDD S13.2 十点位表按块5 定稿重写（每日上限列全 1/#7 不限·隐藏规则·广告券段·#5 改行为·#6 ×10·#3#4 全关卡·#9 仅主线·实现注记）；§3 经济体检表**补广告券行**（流入=商人星贝购买·sink=自身消耗·本质星贝→广告机会转换器不冲稀缺线）+ 总结论改"仅块5 新增 1 个货币类型"；S10.10 补给箱/趣事实现注记 + 礼物清单"每日委托"过时提法改悬赏板；附录B B1.1（补给箱券态+趣事弹泡实现+DEV 工具行 8 槽）/B1.5（两键定稿·战败安慰已实现·⑤流程更新）；附录C 见上；README（交接节/2.5块⑤完成/决策速记块5 段/DEV-TEMP 清单）。
- **怎么测的（+30 条·如实重定基·含防假过反例 3+ 条）**：新增 `s7_ad_point_policy.test.ts`（上限表完整性+逐点位矩阵+三态+券扣退）/`s7_daily_supply_chest.test.ts`（确定性/结构带宽/彩蛋三分支活着+概率量级带）/`s7_defeat_consolation.test.ts`（首败信标/2-3败无信标/**第4败零包**/鼓励文案调性/计数联动连败4场+次日重置）/`s7_starport_anecdote.test.ts`（确定性/1000天命中率带/文案池量级/**禁忌词扫描**/DEV 轮换取模）；`canPickExtra` 3 例进 s7_level_reward；商人（广告券常驻+价格限购常量/keepBought 三连"免费刷/广告刷/跨天"/广告刷新去内部上限）；打捞（立即完成+可收菜/not_found/already_done/**无隐藏上限反例**）；存档（salvage/merchant 新形状+老字段丢弃+**adTicket 不升版双验**）；计数器（adDailyRecord 超限照记+跨天重计）；网关 10 点位重定基。**防假过反例**：①"把每日上限改 0 该点永不可用"→限次点位"未用=available"矩阵红；②"券失败不退"→`settleAdTicketAttempt` 不变量红；③"安慰包第 4 败仍发包"→`defeatConsolationPack(4)===null` 红；另有"商人手动刷新绕过日限会被测出""打捞/商人还藏内部上限会被测出"。
- **零红点 + 五原则核查对照表（任务单 I·逐条核查结论）**：
  | 核查项 | 结论 |
  |---|---|
  | I-1 零红点 | ✅ 10 点位全部按钮/礼盒零红点零闪动零放大（全为静态色块按钮·教程闪烁只作用于教程目标）；现存合法非广告提示=建筑「可升↑」绿字、邮件「领取×N」、背包 Badge、活动可领提示——均非广告系统 |
  | I-2 用尽即隐 | ✅ 六个老点位（回港/赞助/打捞/商人刷新/货舱/回廊里程碑）+ 四新点位全部并入统一三态：用尽&无券=按钮**不出现**（无一处置灰）；唯一"置灰"存在于非广告系统（抽卡券不足/商人买不起=既有合法置灰） |
  | I-3 新手期全隐 | ✅ 统一三态一处判（`s7AdButtonState` 首查 strongGuideActive）：10 点位+商人广告券商品行+货舱文案提示全隐；安慰包整块教程期不给（零白送经济）；玩家第一次见广告=引导结束后首次回港报告（既有行为不变） |
  | I-4 开场弹窗≤1 | ✅ 回港报告独占开场；趣事让位（报告在场不触发·领完才轮到，且趣事=非模态轻泡不占决策点）；其余 9 点位全为结算/界面内**并排按钮**（S13.1 决策点口径：不计）——开场主动弹窗恒 ≤1 |
  | I-5 零广告区 | ✅ 每日推演页零广告键（view 内注释即写明）；趣事零广告（纯白送）；侧边栏礼未接（阶段五）；"已通关自由重刷"现状=灰盒跳关工具（正式重刷系统随 §8 主线地图后做·届时按"重刷零奖励"口径复核） |
  | I-6 文案扫 | ✅ 全部广告文案零倒计时零"你错过了"零"仅剩"；广告类不显示剩余次数（商人 header 已去"广告刷新剩余X"·免费刷新非广告照显）；仅存 2 处"今日次数已用完"字样=不可达防御分支（quota_gone 极端竞态·回港那条为块1 Ron 认可的"退普通领取"兜底文案），正常路径永远走"隐藏"看不到 |
  | I-7 券态正确 | ✅ 无券不显（hidden）·有券显「广告券×N」（张数实时取钱包）·强引导期券态也不显（三态首查引导）；逐点位矩阵测试钉死 |
- **自检结论（深度自检·抓到并修 3 件真问题，commit 2bdf7ff）**：①**趣事触发点错挂**——`dismissBattleScene` 也被悬赏/回廊/推演"单键返回"链路调用（落点=大厅/塔页非 hub），原挂点会把弹泡弹到作战大厅之上 → 挪到 `onResultGoHome` 主线分支；②**#4 白看广告漏洞**——已看广告未再选时点「继续」会丢掉再选机会 → 「继续」拦下+提示（与首选"必须选1"同精神）；③**打捞行按钮触摸区重叠**——广告键与 DEV「秒成」重叠（原布局就有 35px 重叠·本次一并收干净）。其余逐路径追：券消耗时序（内存扣→ok 才 persist→失败退回·杀进程不亏券）/十点位"点过即消失"逐个过/安慰包上下文跨模式不泄漏（isSpecial 门+dismiss 清）/三选一教程四路径（forcedPick/step40/普通/Boss）行为与旧版一致/补给箱券重开序号确定性/趣事"报告在场让位"。**测试假过自查**：防假过反例 3 条均先验证"破坏实现会红"的机制（矩阵测试对上限敏感/不变量对退券敏感/null 断言对发包敏感）；重定基逐条核实"为什么对"（商人 2 常驻数量/打捞新形状/网关 10 点位/钱包 14 键——全部对应真实设计变更非改数凑绿）。自检无其余异常。
- **影响的配置/存档说明**：存档 v23 **不升版**（adTicket 纯加性·normalize 兜底·新老 v23 档互读零歧义）；老档 `salvage.adSpeedup`/`merchant.adRefreshRemaining` 字段加载时静默丢弃（无数据损失——旧计数本就该日清）；商人老货架**当日不含广告券**（次日 4 点自动重铺出现·或任意手动刷新立即出现）；`commission`/其它配置表零改动（广告券走 alwaysOffers 常量·未建新表）。
- **遗留/交接**：①**数值全占位挂第三块**：十点位上限表/广告券定价120与日限1/赞助×10/补给箱量与概率/安慰包量/趣事15%与奖励量——统一按"广告整体加速≤25-30%"回头配平；②**#4 armed 态杀进程**=广告已计数但再选未落（同类暴露=三选一必选屏本身·灰盒可接受·正式演出块若做"结算断点续传"一并覆盖）；③趣事正式文案池 ≥30 条待 Codex（种子 12 条在 `S7StarportAnecdote.ts` 平文件数组·加行即扩池）；④巡逻派驻 UI（S10.10 另排）/败因归类 B1.5b（战斗演出块）不在本块；⑤"已通关自由重刷"的零广告复核留 §8 主线地图块；⑥**5 个新增待 Cocos 认 `.meta`**：`S7AdPointPolicy.ts`/`S7DailySupplyChest.ts`/`S7DefeatConsolation.ts`/`S7StarportAnecdote.ts`/`S7AnecdoteBubbleView.ts`。
- **🔎 Ron 真机复验点（任务单验收 11 点）**：①hub 见活动区下方「🎁今日补给箱」条→点→看广告→入账飘字→礼盒消失（次日 4 点再现·「重置广告」可立即复验）；②普通关首通=三选一选完后见「📺固定奖励×2」+「📺再选一个」两键→各用一次→当日打其它首通关**两键都不再出现**；③「再选一个」看完广告→剩两张变可点→选中的确实是剩余两张之一→两张都带✓；④主线战败：结算卡下方安慰小块入账文案+当日首败多 1 普通信标+「📺安慰双倍」；「重置广告」后连败 4 次→第 4 次只有鼓励文案无包无键；⑤打捞进行中点「📺看广告立即完成」→任务直接可收菜（今日第 2 趟打捞该键不再出现）；⑥赞助补给一次 +10 补给券、当日用过即隐；⑦商人买「广告券×1」（120星贝·限1）→已用完的点位按钮**重现且带「广告券×N」**→用券再看再拿、券-1；广告失败（真机没广告填充时）券不减；⑧「触发趣事」→弹泡（头像+趣话+入账）→点掉即走；自然触发=15% 低概率日≤1 碰运气；⑨「重置教程」走新手流程：全程（含商人货架/货舱/三选一/打捞/抽卡）**见不到任何广告入口与广告券商品**，毕业后首次回港报告才第一次见「📺全部翻倍」；⑩全界面扫一遍：广告入口零红点零闪动（建筑可升↑/邮件领取数=合法非广告）；⑪Cocos 认 5 新 meta（uuid 不被改写=认了）。

- **✅ 完善批·今日补给箱开箱结果确认弹窗（2026-07-06·Ron 拍板·commit `f3fcd03`）**：原开箱只飘字自动消失、玩家易没看清 → 改**结果确认弹窗**（半透明遮罩+小卡片「🎁今日补给箱」+逐行明细〔emoji+名称+数量〕+「收下」·吞触摸必点收下才关·B0.6⑤）。取舍：一次性回执小弹窗照大奖特写弹窗 `buildGrandRewardPopup` 成例**并入 S7DemoController**（非独立功能界面·不新建 view·守⑧对"新增界面"的口径）；奖励入账时机不变（`creditGains` 在弹窗前·弹窗纯回执·杀进程不丢奖）；广告券重开走同一 `runAdPoint` 成功分支=同样弹窗。新增 `supplyChestResultNode/Label`+`buildSupplyChestResultPopup/openSupplyChestResultPopup/onSupplyChestResultClaim/resEmoji`。表现层无 vitest（项目约定）·`npm run gate` 全绿零回归（1299 测/typecheck 0/configs 45）。**自检**：只成功分支弹（失败/退券仍 hubToast）、明细用 SHRINK 锚容器防溢出、点空白不关。**Ron 真机**：看广告开箱→弹窗列明细→点收下才关→钱包对账一致；「+5券」重开同样弹窗。

战斗引擎 + 全套基建（config/save+云、ads+mock、analytics、防重、Node 模拟器、vitest、微信 build、灰盒 UI）可直接复用；驾驶员/星核/基地**需新增**；插件/货币/活动**需改架构**；总体**中等改造量，远小于重做**。
**敌方格子**：3×7 → **5×7（可配置，最终 C1b 真机定）**，保 7 列；目标语义用"最近/最远·第一/最后排"。

## ⚠️ 完整遗留清单
1. **货币映射已整体拍板（2026-06-18）+ 已落地（块6余项 2026-06-19）**——保留 starOre=星矿/hullAlloy=星舰合金/pilotToken=驾驶记录/starCargo=星贝/supplyTicket=补给券/coreFrag=星核碎片/fullCore=完整星核/shipBlueprint=通用星舰碎片；废弃 battleLog+pluginMat+coreMat；✅ 信标 beacon→拆 3 档(beaconCommon/Rare/Epic)、✅ 新增 starGem(星空宝石)/pilotShardUniversal(通用驾驶员碎片) **均已进钱包 `S7_RESOURCE_KEYS`(13 键)**；独立结构(非货币键)：✅ 专属碎片(S7ExclusiveShardInventory)/✅ 宝箱×3(S7ChestInventory) 已建、居民/工人(6b-4b)已建。**剩发放/消费侧接线** → C1b/块7。
2. **`tools/simulate-s7-progression.mjs`**（B1 毕业预算模拟器，手动脚本、不在 npm test/校验门内）建立在已删的"插件强化 1-15 级"上 → 已失效，待第二块插件经济重做时统一重写。
3. **大量"消费侧未接"（随对应系统建好接入，多在 C1b）**：上线弹窗领离线、建筑升级 UI、战斗结算用研究塔/星核展厅加成、~~星舰/驾驶员升级用"船坞/训练舱×5 等级上限"~~（**2026-07-03 取消建筑卡等级：改由阶级决定 C20/B40/A60/S80/SS100，×5 已废除**）、工人折扣 `applyWorkerDiscount` 叠在建筑成本上。
4. **居民/工人"数量从哪来"**：主线救回 + 7 天扩张（内容侧未做）。（Ron 在考虑追加"打捞"为来源，见 README 讨论。）
5. core01-06 已注册但无战斗质变；过载核心"Boss首杀掉落"(获取)属奖励系统未做（块3 只做装备即生效）。
6. n001 仍分批刷怪（与 v1.0 §8 一次性冲突，留内容块）。
7. 插件运行时(4a)按"槽位"给占位修正，**每条插件词条精确效果/数值留第二块**（届时 `S7PluginEffects` 改按 pluginId 索引）。
8. 引擎已消费全部 8 条 affix；**占位语义**(暴击倍率/各系数)精确值留第二块。
9. 插件/驾驶员经"装配器 lineup"接通；**快照→适配器对插件未透传品质**（要编队/装配存档，留后）。
10. 插件合成比例 3→1(已定)，但回收价/折损率定点留第二块；`recycle_normal_plugin.currencyGroup` tag 订正留第二块。
11. `npm run typecheck` 须先用 Cocos 打开生成 `temp/declarations/cc*` 才过（缺 cc 类型，与逻辑无关）。
12. **新增 `core/s7/*.ts` 共 16 个待 Cocos 打开认 `.ts.meta`**：S7PluginEffects/S7PilotEffects/S7PluginInventory/S7PluginCraftService/S7PluginRecycleService/S7BuildingState/S7BuildingUpgradeService/S7BuildingCost/S7BuildingEffects/S7OfflineProduction/S7Population/**S7ExclusiveShardInventory/S7ChestInventory(块6余项)/S7ActivityProgress(块7a)/S7NodeSettlement/S7RunSession(C1b)**。用 Cocos 3.8.8 打开做导入/构建/真机确认仍需 Ron 手动一次。
13. 建筑/离线/人口数值全 v0.1、各种占位语义 → 待原型/真机校准 + 第二块定点。
14. **信标打捞数值表未做**（v1.0 §10.2 只有结构+示意数，缺"每档信标×时间档→必得/概率得/数量"真表）→ 排入第二块。
15. **商人小站升级口径已改、代码待跟**（2026-06-19）：设计已改为"基础商品 lv1 全开、升级只给 稀有刷新解锁(星空宝石 lv2-3 起可刷)/稀有刷新概率/格数(1→3)/可购买次数·限量/回收价·手动刷新"（v1.0 §10.3 + 建筑数值6b-3），但 `core/s7/S7BuildingEffects.ts` 商人段仍是旧"商品槽位 2→6 / 刷新 1-3" → 接商人界面（C1b）时按新口径改、补测试。
