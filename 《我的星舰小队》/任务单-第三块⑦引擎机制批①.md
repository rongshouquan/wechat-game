# 任务单 · 第三块⑦ · 引擎机制批①（M1 限时状态框架 + M2 周期 DoT/HoT + M3 叠层）

> **给执行会话**：推荐 **Fable 5 · 高思考档**。本单=任务单⑥"两拍制"的第二拍前置：⑥工序0 盘出引擎缺九族新机制（`第三块-数值校准/数值细表-战斗篇-v0.md` §8b），本批做**优先级最高的前三族**，吃掉约 50 条真源效果的运行载体。必读（按序）：本单 → 细表-战斗篇 §8b M1/M2/M3 行与 §1-§7 中标 ❌M1/M2/M3 的逐条明细（=需求清单·各条注明真源出处）→ **附录D 各真源对应原文**（效果语义唯一依据）→ `git show ffb70f3`（8a 小旋钮批=受控并行加法的实装范式先例：可选通道/缺省逐字节不变/双份校验器/手推期望值测试）→ `S7AutoBattleEngine.ts`。
> **授权链**：数值域全权委托；开工前置第一性审查（审本单，不合理先交回勿硬做）。

## 范围（只做三族 · 防"机制批蚕食成大重构"）

1. **M1 · 限时属性修正状态框架**：参数化的 加攻/加攻速/减速/虚弱/破防/增伤/减伤%/免伤 限时状态（幅度+时长+来源全配置驱动；既有 berserk 收编为其特例）——载体条目：状态库 6 种软控/增益、号角号全线、骁「先锋」/翎「夺势」/蛰「斩链」、时光糖/幸运扭蛋、磐石「张盾」/山岳「不动」减伤、海盗船长「鼓动」/磁暴塔。
2. **M2 · 周期结算 DoT/HoT**：燃烧/持续回血/区域灼烧（每 tick 或每秒结算、可叠层口径接 M3、幅度时长参数化）——载体：贯日灼烧/群蜂A燃烧/污染体/污染潮、甘霖「再生」/春风「微风」/回血模块。
3. **M3 · 叠层框架**：层数+每层数值挂钩+断条件（断击清空/时限/上限）——载体：炎「过热」、源「专注」、铁壁「坚甲」、迷雾「侵蚀」、蜂针「淬针」、贪吃星、战鼓、砺「愈坚」。

**不做**（守边界）：M4-M9 六族（嘲讽重定向/净化极性/光环/连锁概率/致盲/零散单点）一律不碰——即使"顺手"也不做，列到的需求如实留 §8b 原位；发现 M1-M3 内有条目实装成本超预期（如需动 tick 主序）→ 该条如实交回不硬做，其余继续。

## 硬规格

1. **受控并行加法（8a 范式逐条遵守）**：全部新机制=可选配置通道，**缺省缺席时引擎行为逐字节不变**——机器证据=既有 1004 测试**零改动零重定基**原样全过；tick 九步结算序不动（状态到期/周期结算挂进既有步骤的语义内，不新增步骤或改变步序）。
2. **效果语义严格按附录D 真源原文**，不脑补不扩展；歧义列清单交回。**技能暴击口径已裁（2026-07-07 Ron 拍板）：全体输出默认皆可暴击**——过载线圈词条已改"技能暴击率专项"（插件真源在案），不做任何 effectKind 暴击门控。
3. **参数全配置化**：每种状态的幅度/时长/叠层上限/断条件全部走配置字段——任务单⑥第一段要拿这些旋钮给 16 种状态定数，硬编码=返工。
4. **测试**：每族配手推精确期望值测试（如"燃烧 3 层×每秒 X=第 N tick 血量恰为 Y""断击后层数归零再叠"），进 gate；测试如实定基不放水。
5. **与⑥并行的工作区纪律**：本单改动区=引擎/积木/类型/校验器/新测试文件+细表-战斗篇记账新章；**不碰** `battle_unit_stat_param` 等数值 JSON（⑥第一段在填）、不碰初值表正文 §17（⑥日志）；里程碑勤 commit 防撞车。
6. **交付分段（防断）**：允许两段——第一段=M1（最大族）落地+gate 绿+commit；第二段=M2+M3（可换会话接力）。每段独立记账（细表-战斗篇新开"机制批①日志"章）。

## Ron 复验点（总控转达）

总控验收后 Ron 看两句话："机制批①落地：M1/M2/M3 三族 X 条载体通路全通（既有测试零改动全绿=旧行为未动的机器证据）；⑥第一拍实证解锁 buff/减益/叠层全域，状态库 16 种数值有了真实运行载体。"
