# 规则体系·全域盘点与改造（2026-07-11）

> **背景**：Ron 痛点＝项目一个多月积累的规则常"读到却没遵守"（实证：某任工程总控接手只读交接单/README/日志等二手账本、没通读真源，违反铁律 `controller-must-read-truth-sources`，被 Ron 一问才认账）。
> **Ron 授权**（2026-07-11）：全域盘点所有规则、逐条诊断"多容易被读到却忽略"并执行对应改进；**不新增任何规则**（要加须先请 Ron 拍板）；**不删任何规则**（建议删除只列清单候拍）。
> **本文=一次性审计记录+改造台账**，改完即归档，不是需要持续维护的活文档。

---

## 一、诊断框架（为什么会"读到却不遵守"）

模型处理规则的三个事实：
1. **没有"良心常驻"**——每轮把上下文重读一遍再行动；规则在开头、任务在结尾，干得越深规则权重越低。**读到≠决策瞬间在场**。
2. **形态决定遵守率**：道理型（"别靠二手转述"）＜触发型（"接手后第一步做X"）＜**闸口型（"报到帖第一节必须是XX表，没表不许往下"）**——闸口型的违规是肉眼可见的，Ron 扫一眼就能抓。本体系里执行最好的恰是既有的两条闸口型（§一.8 自检结论、七.1 DEV-TEMP 清单），实证有效。
3. **住址决定加载率**：全局 CLAUDE.md=每会话必加载（A级）；记忆库=只有索引行必加载、正文靠碰运气召回（B级）；项目文档=要主动打开才进上下文（C级）。**铁律住 B/C 级=住在不一定打开的抽屉里**。

**遵守率 = 决策瞬间是否在场 × 事后是否可核查。** 改造全部围绕这两个乘数：
- 改形态：铁律改写成"触发点+第一动作+必交产出物"；
- 搬家：红线级规则集中进 CLAUDE.md ⚑段"路由表+铁律"（A级住址）；场景规则嵌进"干活时必然打开的文档"（续接单第0步）；
- 索引钩子化：记忆库索引行改"当X→做Y"格式（索引是记忆库唯一必加载部分，一行就得能当触发器）；
- 修过时：指针/事实性过时逐条修正。

---

## 二、逐条诊断表

标注说明——**形态**：道理/触发/闸口；**险**（被读到却忽略的风险）：高/中/低；**处置**：保留/升闸口/进路由表/合并/修过时/钩子化/候拍。

### 第1层：全局 CLAUDE.md（住址A·必加载）

| # | 规则 | 形态 | 险 | 诊断与处置 |
|---|---|---|---|---|
| ⚑1 | 接手先读README、设计读GDD-v2.0 | 触发 | 中 | 触发点对但无产出物 → **进路由表**第1行；数值真源指针补《数值细表真源》（07-10已立档，⚑段没跟上=指针过时，**修**） |
| ⚑2 | 走轻流程不走Codex重流程 | 道理 | 低 | 保留（背景性，误违代价低） |
| ⚑3 | F:\Codex只是历史来源 | 道理 | 低 | 保留 |
| ⚑4 | 包体红线3.8/4.0MB+接入记录 | 触发 | 中 | 保留为项目铁律7（美术接入期自然在场） |
| ⚑5 | Cocos禁展开Set/Map | 触发 | 低 | 保留（自带排查触发器，全体系样板级写法） |
| 一.0 | 执行前先质疑 | 触发 | **高** | 与一.0bis是配套对，且被 `ron-wants-real-debate`（辩到掰清）升级过但文本没跟上 → **三条合并**为"三栏对齐"闸口（③栏=本次命中规则清单=决策点强制回忆器，直治"读到没想起"） |
| 一.0bis | 执行前先对齐理解与打算 | 触发 | **高** | 同上合并；升级为闸口型（对齐帖三栏缺一不可） |
| 一.1-6 | 按确认方案/不猜/不可行先指出/自己修Bug/3次问/能力边界 | 道理·触发 | 低 | 保留原样 |
| 一.7 | 测试不为绿而绿 | 触发 | 中 | 保留；产出物（重定基逐处"旧→新→为什么对"）**进路由表**"改代码"行 |
| 一.8 | 完成前自检 | **闸口✓** | 低 | 保留（本体系执行最好的规则，闸口型有效的活证据）；进路由表兜底行 |
| 二.1-2/4 | 分阶段/工作量适中/大改拆段 | 道理 | 低 | 保留 |
| 二.3 | 每阶段前汇报+估工时+估Token | 触发 | — | **与 Ron 07-06 后拍的契约⑨"轻仪式·不强制分步汇报"冲突**（新拍板已在README契约生效，本条文本滞后）→ 按新拍改写：大块/高风险才事前对齐，常规活一次交付完整汇报。**已列"需Ron知悉"** |
| 三.1 | 不贴大段代码 | 道理 | 低 | 保留 |
| 三.2 | 汇报模板六要素 | **闸口✓** | 低 | 保留；**并入七.3**（内容重复：两处都要求"下一步/待办"） |
| 三.3 | 大白话只报结果 | 道理 | 中 | 保留（Ron 07-11 刚完善过；A级住址+高频场景，可接受） |
| 四.1-6 | 设计分工与循环判据 | 章程 | 低 | 保留；四.1 补一句已全权委托域指针（UI/数值专业/调度——记录既有拍板，非新规） |
| 五.1-2 | Git节点提交/代码可运行 | 触发 | 低 | 保留；git前自检已在路由表兜底行 |
| 六.1-6 | 产品体验原则 | 价值观 | 低 | 保留 |
| 七.1 | DEV-TEMP标记+汇报专列 | **闸口✓** | 低 | 保留；进路由表"改代码"行 |
| 七.2 | 配置表变更影响说明 | 触发 | 中 | **修过时**：例举文件名 races.js/items.js/levels.js 是旧Canvas项目的，星舰小队实际是 configs/s7 各表 → 泛化措辞；产出物进路由表"数值改动"行 |
| 七.3 | 汇报结尾补下一步/待办 | 触发 | — | **与三.2重复 → 合并进三.2**（见"需Ron知悉"） |
| 七.4 | 资源占位规范 | 道理 | 低 | 保留（编号顺移为七.3） |
| 二部分 | 微信小游戏专属规则（技术栈=纯Canvas/CommonJS/禁npm） | 章程 | — | **范围冲突**：星舰小队=微信小游戏但用Cocos+TS+npm（Ron 拍板覆盖，记忆 my-starship-squad-workspace 有案）→ 加适用范围注记："技术栈条款适用原生Canvas项目；引擎项目以⚑段为准，平台底线仍参考适用"。**已列"需Ron知悉"** |

### 第2层：记忆库（住址B·仅索引必加载）

**索引级问题（本次全修）**：
1. **两条记忆不在索引里**＝永远不会自动加载：`resource-pacing-shapes`（节奏形状先过Ron——真实工作规则！）、`xingdao-not-haidao`（星盗口径铁律）→ **补入索引**；
2. 索引行全是"描述式"→ **改"当X→做Y"钩子式**并按 协作契约/项目铁律/设计观/美术线/状态快照 分组；
3. `tutorial-rework-handoff` 索引行还挂着旧待办、正文早已"完成封板"→ **索引行修正**。

**逐条**（38文件；仅列有处置动作的，其余=保留+钩子化索引）：

| 记忆 | 诊断 | 处置 |
|---|---|---|
| controller-must-read-truth-sources | **铁律只住B级=事故直接原因**（截图那次） | **升CLAUDE.md路由表**（真源核读表闸口）；正文加升级注记 |
| consensus-before-drafting-task-docs | 铁律只住B级 | **升CLAUDE.md项目铁律4** |
| ron-owns-locking | 铁律只住B级（曾发生越权代锁） | **升CLAUDE.md项目铁律1** |
| ron-flags-over-adding | 铁律只住B级（同会话三犯=高危惯性） | **升CLAUDE.md项目铁律3** |
| ron-wants-real-debate | 是对一.0的升级但CLAUDE.md文本没跟上 | **并入新一.0**（辩到掰清入正文） |
| resource-pacing-shapes | 不在索引+只住B级 | 补索引+**进路由表**"数值改动"行 |
| xingdao-not-haidao | 不在索引（幸有总纲铁律A7/README兜着） | 补索引+**升CLAUDE.md项目铁律5**（一行成本，三线通用口径） |
| strict-v1-no-improvise | 名字和正文还说"v1.0"，真源已升级GDD-v2.0（README已改，记忆没跟上） | **修过时**：规则本体=严格按"现行真源"，补换代注记 |
| numeric-truth-b1-v1-ignore-codex | "B1=数值真源"已过时——07-10立档《数值细表真源》为总入口、B1转历史档 | **修过时** |
| gdd-v2-is-active-design-truth | 正文断言"README指针仍指旧v1.0已过时"——实查README第3行已指v2.0，断言本身过时 | **修过时** |
| my-starship-squad-workspace | "唯一真源=v1.0已冻结"过时 | **修过时**（指针指向现行真源） |
| legacy-flow-cleanup-timing | 所记"清理时机"已于07-06执行完毕（README ⚠️节：流程版已清账 26f567b） | 正文加"✅已执行"注记；**列建议删除候拍** |
| tutorial-rework-handoff | 正文OK，索引行过时 | 索引行修正 |
| 其余26条 | 内容仍准确 | 保留原文+钩子化索引 |

### 第3层：项目载体文档（住址C·要主动打开）

| 文档 | 诊断 | 处置 |
|---|---|---|
| 总控续接-对锚与阶梯批期.md | **§一"开工动作"列的全是账本**（README/任务单在不在/§16e/git log），一份真源都没有——**这正是截图事故的直接病根：新总控照着它做，天然违反真源铁律**。规则与载体脱节的活标本 | **加装"第0步·真源核读闸"**（通读命中真源全文+报到帖第一节=核读表，没表不拟单不讲发现） |
| 美术线-会话续接单-20260711.md | 接手三步读的是流程文档（README活线头/总纲/提示词规范），没含"做哪族先读哪份真源" | **加装轻量"第0步·真源核读闸"** |
| README-流程与进度.md 风格契约十条 | 住址正确（总控必读）、形态尚可 | **不动**（尊重双总控产权：改README只动自己活线头段，我两个都不是）。顺手发现：§🔒速记里"数值真源B1"字样已过时（现=细表真源），**建议工程总控下次刷新时改**，本次不代改 |
| 美术线-总纲.md（铁律A1-A11/七步/Q1-Q10） | 形态好：铁律有编号、七步有守门标准 | 不动 |
| 美术线-提示词规范.md §5 | **全体系最佳实践**：脚本文件头必须贴检查单自审、一项不过不许发批＝标准闸口型 | 不动（当样板） |
| 进度日志"调参纪律"（≤5轮停手/gate退出码/&&短路） | 住在账本里，但数值线续接书契约⑦已复述兜底 | 不动；gate退出码已进路由表"改代码"行 |
| GDD-v2.0体系/附录A-D/数值细表真源 | 设计内容非规则 | 不进诊断范围，只核对指针（已核：CLAUDE.md数值指针过时已修） |

---

## 三、执行记录（本次实际改了什么）

1. **`C:\Users\Administrator\.claude\CLAUDE.md` 重写**（旧版全文=附录A，该文件不在git内故留纸面备份）：
   - ⚑段新增**路由表**（7行：接手/总控续接/任务单/数值/代码/美术批/完工兜底）+**项目铁律8条**（闸口式；1-5=从记忆库升级的既有拍板，6-8=原⚑段三条原样收编）；
   - 一.0+一.0bis+辩论升级 → 合并为**"三栏对齐"**闸口（①理解 ②打算 ③命中规则清单）；
   - 二.3 按契约⑨改写；三.2 并入七.3；七.2 修过时文件名；第二部分加适用范围注记；
   - 头部补记 Ron 07-11 令：**新增/修改协作规则须 Ron 点头，Claude 不得自行立新规**。
2. **`MEMORY.md` 索引重写**：补2条失踪记忆、全部钩子化（当X→做Y）、按五组分类、修 tutorial 过时行。
3. **记忆正文修过时6处**：controller-must-read（升级注记）/ strict-v1 / numeric-truth / gdd-v2 / my-starship-workspace / legacy-flow（✅注记）。
4. **两份续接单加装"第0步·真源核读闸"**（工程版+美术版）。
5. 本文档落 `F:\Claude Code\` 根并提交 git。

---

## 四、候 Ron 拍板清单（本次一律未执行，按你的令只列不动）

1. **建议删除**：记忆 `legacy-flow-cleanup-timing`（所记清理已于07-06全部执行完毕，规则使命终结；正文已标✅注记，留着也无害只占索引一行）。
2. **建议新增（无）**——盘点没发现非要新立不可的规则；三栏对齐/核读表均为你已批准方案的落地，不算新规。
3. **需你知悉的两处口径修正**（都是"按你更晚的拍板把旧文本改齐"，不是我立新意）：
   - 二.3 旧文"每个新阶段都先报工时+Token估算" → 改为"大块/高风险任务才事前对齐方案与工作量，常规活一次交付完整汇报"（依据=你07-06拍的契约⑨"轻仪式·不强制分步汇报"）。若你想保留"每阶段必估Token"，说一声我改回来。
   - 第二部分技术栈条款加了适用范围注记（原文与星舰小队现状直接矛盾，实践中你早已拍板Cocos覆盖）。
4. **移交项**：README §🔒速记"数值真源B1"字样过时，建议由工程总控下次刷新活线头时顺手改（我不代改，避免越双总控产权）。

---

## 五、第二轮改造（2026-07-11 晚 · Ron 全批 S1-S6＋A1-A7＋三条新规）

- **精简**：第二部分（原生 Canvas 46 行）迁出→`F:\Claude Code\微信小游戏-原生Canvas规则.md`（CLAUDE.md 留指针＋AppID 档案）；一.4+5 合并、一.6 压缩、二.1+2+4 合并、六.1-4 合并、七.3 资源占位随迁——CLAUDE.md 净瘦约 45 行；自检条款编号 一.8→一.7（文内引用已同步改）。
- **补缺**：A1 任务单范式→路由表"出/改任务单"行；A2 双总控体制→⚑段；A3 第一性原理默认工作方式→一.0；A4 验收靠实物→新路由行；A5 提交链 && 短路→"改代码"行；A6 候拍事项列正文→三.2；A7 初见口径→《数值细表真源》新 §15。
- **新规三条（Ron 拍板）**：①美术·看图台必达（每批含失败批，Read 渲染≠发了）；②美术·后台跑图纪律（边界=Ron 点名清单/开新批=方案先讲拍了才跑/安排内不闲置/清单空停机来问/状态三报/停批口径="自认挑不出毛病"）——落 总纲§1＋记忆 art-autonomous＋美术续接单＋路由表美术行；③全员·探讨环节不动手（铁律4 扩写＋记忆 consensus 补注）。
- **删除**：记忆 `legacy-flow-cleanup-timing`（Ron 拍板·文件已删、索引行已清）。
- **背景**：美术总控 07-11 擅自跑图实案（Ron 排三项工作不含跑图）触发②③；美术总控已自记"适用边界"块于记忆，与本轮落档互补不冲突。

---

## 六、第三轮补丁（2026-07-11 深夜 · Ron 拍板 P1-P4＋美术预估时长）

- **P1 新规落户规程**→CLAUDE.md 头部"规则怎么用"：未来 Ron 立/改任何规则，接令会话必须当场"条文化（触发点+动作+产出物）→落对住址→复述确认"——治"方法论没变成规则"的缺口，保证未来新规自动享受本次优化。
- **P2 续接书/交接单必含第0步核读闸**→路由表任务单行：防"用完即删"把闸口一起删没（新门出厂必须带锁）。
- **P3 纯问答豁免**→一.0：问答/查询/讲解直接答、不需对齐帖；答疑≠授权动手。
- **P4 README 防冗杂**→路由表新行"批次关账/阶段收口"：已关账流水迁归档、活线头只留"当前批＋待办/候拍＋现行口径"＋指针；Ron 并令两总控**立即各做一次性瘦身**（随贴话下发）。
- **美术补规·预估时长**：每次跑图/训练开跑都顺手报预估时长（对"大任务才报估算"的美术域例外）→路由表美术行＋总纲§1⑤＋记忆＋美术续接单。

---

## 七、第四轮补丁（2026-07-11 夜 · Ron 全拍 A-E · 两实案触发）

- **A 效率优先·标准流程对照**→CLAUDE.md 新硬规则一.8（当场直说哪里低效＋标准做法＋方案；两个强制自查点=接新线/多轮不收敛；护栏=不砍质量闸、只管成批浪费级低效）。触发实案=母版机：美术线多批直接上族上个体，总控明知"先定母版再拓展"是行业标准却没提。
- **B**→一.0 ①栏加"发现现行做法低效/不合标准流程也要讲"（钉进每次动手的必经之门）。
- **C**→新记忆 `efficiency-first-challenge-workflow`（存母版机实案）＋索引钩子。
- **D 基线手术制**→提示词规范 §5 新第 11 项（prompt 声明基线＋"＋X句−Y句每句一行理由"·净增为正须点名）＋总纲 §3＋出图方法论索引行——把"约束少而精"从道理变成可数闸口。触发实案=总控引用得出"约束少而精"却连续两批堆词。
- **E 开工信号三条亮线**→CLAUDE.md 铁律4（全员）＋总纲后台跑图纪律⑦＋记忆 art-autonomous④：末尾问句=整条探讨／"认同"≠开工令／预声明条件须全部严格成立。触发实案=L51：Ron 回"1对、2-4先不定"+末尾问认不认同，总控自判条件成立开跑。

---

## 附录A：改造前 CLAUDE.md 全文备份（2026-07-11 快照）

```markdown
# 用户开发偏好记忆

你负责项目顶层设计、数值体系、全套工程代码、合规风控、复杂排错重构、长周期规划。我是总决策人——你的方案先用大白话精简解释、让我确认，确认后再执行。觉得某事适合 Codex 做就主动提。

> 本文件分两部分：第一部分【全局通用规则】适用所有项目；第二部分【微信小游戏专属规则】仅做微信小游戏时生效。单个游戏的玩法设计见各项目目录内的设计文档。

---

## ⚑ 当前活跃项目：《我的星舰小队》（微信小游戏 · Cocos Creator 3.8.x + TS）

> 工程目录 `F:\Claude Code\《我的星舰小队》`（Ron 自有玩法设计副本）。接任何任务，**动手前先读** `README-流程与进度.md` 接进度；看设计读 **`GDD-v2.0.md` 体系（现行设计真源：§5 星舰5定位型 + 附录D 6份内容真源）**；`系统玩法设计-v1.0.md` 仅"未迁入 v2.0 的 §0-17 正文"仍有效（§5 等已被取代）。

- **本项目不走 Codex 那套重流程**（状态看板/技术债台账/阶段闸口/完成报告模板）——走轻流程，规则以本文件 + 项目内 README/设计文档为准。
- `F:\Codex\星舰小队\game` 只是本项目 `game\` 的**历史来源**（已复制进来），偶尔参考用；不是维护对象，别读它的 `CLAUDE.md`/项目管理文档当规则。
- **美术/动画/音频/UI/字体/特效/Cocos 资源接入**（目前还没到）：主包包体红线 **3.8MB 警戒 / 4.0MB 阻断**；资源接入需记录 预计/实际体积、授权来源、`.meta` 状态。具体规格到该阶段在项目内另立文档。
- **⚠️ Cocos 微信构建硬坑（必守）**：纯 TS 里**禁止 `[...someSet]` / `[...someMap]` 展开非数组可迭代对象**——微信构建（Babel 降级）会编成**空数组**，本地 `npm test`（esbuild）全绿、微信端却炸（实战栽过：`new Set([...shipIds,...coreIds])` → 表空 → 51 条误报）。改用 `Array.from(set)` / `set.forEach(...)`；展开数组安全。排查"本地过、微信报一堆莫名错"优先怀疑此项。详见记忆 `cocos-no-spread-set-map`。

---

# 第一部分：全局通用规则

## 一、职责边界

0. **【硬规则·执行前先质疑】** 收到任何任务指令，动手前先评估：对指令有疑问、或认为有更优方案/方向，必须先讲清（疑点、风险或更优建议），等我确认再继续，不要默认指令一定对。指令可能由我或 Codex 下达、方向也可能有误，你有责任先把疑问和建议讲清。确认无疑问、也没更好建议，才直接执行。
- **【硬规则·执行前先对齐理解与打算】** 每次动手前，先用大白话告诉我：你对**任务目标的理解和判断**、以及**打算怎么做**，让我确认。即使没有疑问也要讲，避免理解偏差导致白做。（与上一条配套：上一条管"有疑问要提"，这一条管"没疑问也要先对齐"。）
1. 按经我确认的方案推进；未经确认不擅自改方向、扩需求。
2. 不确定需求先问我，不许猜。
3. 发现我的方案技术上不可行/不兼容，必须先指出并问我，不擅自猜实现。
4. 不要把调试丢给我——自己查、自己测、自己修 Bug。
5. 同一问题最多自行修 3 次，3 次仍败再问我。
6. **能力边界**：我无法实际操作手机/模拟器去"玩"游戏——试玩、感受手感、判断画面好不好看好不好玩，始终要你亲自完成；我能做代码检查、运行结果核验、设计逻辑把关。别说"我去测试一下/体验一下"，应提示你去测、并说明我如何配合（查代码、核日志等）。
7. **【硬规则·测试验收要真实，不为绿而绿】** 修 Bug 或机制改动需"重定基"期望值时，必须按代码**真实正确行为**核实每个新期望"为什么对"；**绝不为让测试过就瞎改数字、注释断言、放宽校验或删测试**。拿不准就停下来问我，别改成能过的样子。
8. **【硬规则·完成前必做自检，复杂工作做深度自检】** 任何一块工作"做完"前必须主动自检一遍，**不可跳过**；自检是**主动找自己的错**，不是复述做了啥。最低清单：① **测试假过没**——每个改动的测试真触达被测路径吗？把功能改坏它会变红吗？② **假设核实没**——对配置值/外部效果/第三方行为的假设是查证的还是猜的？③ **边界/异常**——0/空/缺失/越界/除零/处理顺序想过没？④ **重定基期望**——每个改过的期望核实"为什么对"没（呼应第7条）？⑤ **遗留**——死代码、未清的 DEV-TEMP、误导性旧注释/旧测试名清了没？**复杂/高风险工作（架构、战斗/存档核心、多文件联动、机制替换）必须深度自检：主动构造反例推翻自己的结论**（如专门验"功能若坏会不会被发现"），不只看测试绿没绿；简单机械改动可轻量自检。自检结论写进完成报告（§三.4）。**教训：自检已多次查出真问题（假过测试、错误假设），漏掉会让小问题堆到后期难查——宁可慢也要认真自检。**

## 二、阶段管理规则

1. 分阶段开发，不一次性生成大项目/大改动。
2. 每阶段工作量适中（半小时左右可接受），只做必要功能，不做多余扩展。
3. **每个新阶段开始前先向我汇报**：要做什么、预估工时、预估 Token 消耗；我确认后才能开始。
4. 多文件、改动面大的需求，主动拆多阶段，逐一确认再动手。

## 三、输出限制

1. 不在对话里生成多余代码或调试日志，避免一次性贴大段代码。
2. **每次汇报必须含**：完成了什么 / 改了哪些文件 / 如何测的 / 发现并修了哪些问题 / **自检结论**（按 §一.8 查了哪几项、纠正了什么、还留了什么；没问题也写"自检无异常"）/ 是否已提交 Git / 下一步做什么。
3. **只汇报结果、不直播过程、用大白话**：工作中别逐步输出"我先读 X 再查 Y 再改 Z"这种过程；到节点给结果。汇报像聊天一样讲清，少堆术语，让外行也看懂。**凡要我拍板的、向我解释的、要我知悉/了解的内容，一律用大白话简洁讲清楚，少用行话；必须用行话时当场用一句人话解释**（2026-07-11 完善）。

## 四、设计协作模式

1. 分工（均由我 Ron 拍板，方案先用大白话讲清、确认后再实现）：**玩法策划、广告策略以我为主**；**关卡设计、UI 以你为主**；顶层设计、数值、养成、长周期规划由你主导提出。无论谁起草，最终我拍板。
2. 设计不确定点先与我确认，不替我做设计决策。
3. 我交给你设计时，你可先评估技术可行性、提确认问题，再动手。
4. **多模块联动的系统性设计**（新手引导/抽卡/养成等）：我尽量先一次讲完整体流程；你收到后先通读、评估可行性、提疑点和潜在矛盾，再拆成可独立完成的阶段逐一确认实施（设计整体化、实现分阶段，避免边做边改返工）。
5. 我是游戏设计新手，很多玩法只到"想要什么"层面、细节（获取渠道/养成节奏/数值曲线）没想清。发现这种"只有方向、缺细节"时，**主动指出哪些要细化**并给专业建议，但最终决策我定。
6. **"完整循环"判断标准**：我交给你一套系统设计（宝物/研究所/养成等）时，帮我判断它能不能"转得起来"——① 玩家做动作后能否较快获正反馈？② 正反馈是否驱动下一步动作（环环相扣非断点）？③ 长期是否越来越强、越有成就感（而非枯燥卡住）？发现断点、缺驱动力或长期掉动力，明确指出并给改进建议。

## 五、Git 与交付

1. 每完成一个重要节点必须提交 Git。
2. 代码简单、稳定、可维护，优先保证可直接运行，而非只在特定环境跑。

## 六、通用产品/体验原则

1. 适合碎片时间、随用随停：切画面或离开应用再回来能无缝继续，不因离开界面/时间长而重新加载。
2. 必须有爽感和强正反馈，越用越爽。
3. 难度上升时玩家能力也明显增强，达成目标有成就感。
4. 失败/退出后愿意再来一次。
5. 盈利/转化是重要目标，但以不破坏体验为前提。
6. **"赌博式"机制（抽卡/抽奖）**：适合的项目可加，能带强期待感和爽感；但坚持"良性、正向、适度"——不恶意诱导长时间在线或非理性付费，让玩家"心甘情愿"看广告/付费而非被套路，目标是长期满意、回头不觉得"被坑"。

## 七、开发协作效率补充

1. **临时调试代码标记**：所有临时调试代码（强制重置存档、跳校验等）统一加 `// DEV-TEMP:`，并在汇报中专列"本次新增临时调试代码，待删"，避免带入正式版。
2. **配置表变更影响说明**：改 races.js / items.js / levels.js 等配置表时，主动说明影响哪些已有数值/存档，便于我判断是否要重置存档或数据迁移。
3. **跨阶段衔接说明**：每段汇报结尾补"建议下一步从哪继续 / 还有哪些待办"，便于跨会话/上下文重置后快速恢复。
4. **资源占位规范**：原型阶段用色块代美术时，统一占位命名和样式规则（按种族/类型区分颜色、文字标注用途），便于后期替换不遗漏。

---

# 第二部分：微信小游戏专属规则

> 仅做微信小游戏项目时生效。

## 一、项目基本规则

1. 项目类型：微信**小游戏**（非小程序）。
2. 根目录 `F:\Claude Code\`，每款游戏用独立子文件夹（如 `F:\Claude Code\breakout\`）。
3. AppID：`wx5809349b6eb8275a`。
4. `project.config.json`：`compileType` 必须 `"game"`；不要写 `libVersion`。
5. 技术栈：纯微信小游戏框架 + Canvas 2D API；CommonJS（`require`/`module.exports`）；不用第三方库、不用 npm/Webpack/Vite/React/Vue；用 Git。

## 二、真机兼容强制规则

1. **顶部安全区规避（必须）**：顶部刘海屏/状态栏/胶囊按钮区禁止放任何 UI 控件或展示内容（标题、关卡信息、返回/计时/加速/暂停等）。统一用 `wx.getMenuButtonBoundingClientRect()`（或 `statusBarHeight`）算安全偏移，所有顶部栏绘制起始 Y 在偏移之下。建议项目初期封统一工具函数共用。
2. **画布清晰度适配（必须）**：建 canvas 必须按 `pixelRatio` 分配物理像素——`canvas.width/height = screenWidth/screenHeight * pixelRatio`，并 `ctx.scale(pixelRatio, pixelRatio)`，绘制坐标仍按逻辑像素写。否则高清屏画面（美术/文字/UI）会糊、有锯齿（是适配问题非素材问题）。项目初始化阶段就配好。
3. **禁止使用**：`window`/`document`/DOM API；`canvas.addEventListener/removeEventListener`；`localStorage`；浏览器专属 Image/Audio 写法；Node.js 专属 API。
4. 触摸事件统一在 `Main.js` 处理（`wx.onTouchStart/onTouchMove/onTouchEnd`）；各场景只暴露 `onTouchStart/onTouchMove/onTouchEnd`。
5. 画布尺寸用 `wx.getSystemInfoSync().screenWidth/screenHeight`，禁用 `windowWidth/windowHeight`。

## 三、代码质量要求

1. 文件命名统一小写英文 + 下划线；资源路径大小写完全一致。
2. 每个模块职责清晰，不生成无用文件。
3. 所有代码能直接导入微信开发者工具运行；简单、稳定、可维护；优先保证真机可运行（非只模拟器能跑）。

## 四、性能优化硬规则

保证性能良好，防发烫、掉帧、卡顿、耗电快。**每阶段完成后必须关注**：

1. **Canvas 绘制**：减少每帧绘制对象；静态内容用离屏 Canvas 缓存；只重绘变化区域；避免 `shadowBlur` 等高开销特效。
2. **计算**：避免每帧不必要循环/复杂计算；碰撞检测、路径计算做剪枝；粒子受控、动画从简。
3. **定时器/动画**：用 `requestAnimationFrame` 驱动游戏循环，不用 `setInterval` 驱动核心逻辑；场景切换及时清定时器。
4. **内存**：及时释放无用对象/资源；游戏循环内别频繁创建对象（用对象池）；场景销毁清事件监听和引用。
5. **资源加载/目录**：按需加载，不一次性全加载；图片/音频复用；**项目早期就建最终资源目录结构**（如 `assets/characters/ backgrounds/ items/ effects/ ui/`），后续严格按此加载，禁止中途调整目录。

**自测标准：每阶段完成后必须在真机上确认不卡顿、不发烫、帧率稳定。**

## 五、原型与验证优先级

1. **原型优先**：前期用色块和简单 UI，不等美术，先验证玩法是否成立、好不好玩。
2. **数据驱动**：种族、宝物、羁绊、关卡等尽量用配置表驱动，调平衡只改配置不改逻辑。

## 六、每次开始新游戏前

先输出：① 你理解的项目目标 ② 准备采用的目录结构 ③ 第一阶段做什么 ④ 是否有必须确认的问题。没有必须确认的问题，再开始第一阶段。
```
